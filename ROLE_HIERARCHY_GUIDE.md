# Руководство по иерархии ролей

## 🎯 Система иерархии ролей

Система управления правами реализована с четкой иерархией:

### Иерархия ролей (от высшей к низшей)

1. **Директор** (уровень 100)
   - Полный доступ ко всем функциям
   - Может назначать все роли: admin, manager, sales, marketer, employee
   - Может изменять любые роли и права

2. **Администратор** (уровень 80)
   - Управление системой
   - Может назначать роли: manager, sales, marketer, employee
   - **НЕ может** назначать роль director
   - **НЕ может** изменять роль других директоров

3. **Менеджер** (уровень 60)
   - Управление записями, клиентами, календарем
   - **НЕ может** назначать роли

4. **Продажник** (уровень 40)
   - Работа с клиентами, Instagram чат
   - **НЕ может** назначать роли

5. **Таргетолог** (уровень 30)
   - Аналитика, рассылки
   - **НЕ может** назначать роли

6. **Сотрудник** (уровень 20)
   - Только свой календарь и записи
   - **НЕ может** назначать роли

## ✅ Что реализовано

### Backend

1. **Определение ролей** (`backend/core/config.py`)
   - Каждая роль имеет список ролей, которыми может управлять (`can_manage_roles`)
   - Функция `can_manage_role(manager_role, target_role)` проверяет иерархию

2. **API проверки** (`backend/api/roles.py`)
   - GET `/roles` - возвращает только те роли, которые может назначать текущий пользователь
   - Фильтрация на основе иерархии

3. **API изменения роли** (`backend/api/users.py`)
   - POST `/users/{user_id}/role` - проверяет права перед изменением
   - Директор может изменить любую роль
   - Админ может изменить только нижестоящие роли
   - Нельзя изменить свою собственную роль

### Frontend

1. **UI управления ролями** (`frontend/src/pages/admin/Users.tsx`)
   - Показывает только доступные роли для назначения
   - Информационное сообщение о иерархии
   - Предупреждение, если нет прав для изменения ролей

## 🔐 Примеры использования

### Директор
```
✅ Может назначить роль "Администратор" пользователю
✅ Может назначить роль "Директор" пользователю
✅ Может изменить роль другого директора
```

### Администратор
```
✅ Может назначить роль "Менеджер" пользователю
✅ Может назначить роль "Продажник" пользователю
❌ НЕ может назначить роль "Директор" пользователю
❌ НЕ может изменить роль директора
```

### Менеджер / Продажник / Таргетолог / Сотрудник
```
❌ НЕ может назначать роли другим пользователям
❌ Кнопка изменения роли недоступна или показывает предупреждение
```

## 📊 Изменения в БД

Были изменены роли следующих пользователей:

- **admin** (Администратор): admin → **director**
- **tursunay** (Tursunay): admin → **director**

Теперь оба пользователя имеют права директора и могут:
- Назначать любые роли
- Управлять всеми пользователями
- Изменять права доступа

## 🛡️ Защита

1. **API уровень**
   - Проверка прав перед каждым изменением роли
   - Невозможно обойти через прямой API запрос
   - Логирование всех изменений ролей

2. **UI уровень**
   - Показываются только доступные опции
   - Информативные сообщения об ограничениях
   - Предотвращение случайных ошибок

3. **Бизнес-логика**
   - Нельзя изменить свою собственную роль
   - Нельзя удалить самого себя
   - Админ не может удалять директоров

## 🚀 Как использовать

### Изменить роль пользователя

1. Войдите под учетной записью **director** или **admin**
2. Перейдите на `/admin/users`
3. Найдите нужного пользователя
4. Нажмите кнопку **Shield** (синяя)
5. Выберите новую роль из доступных
6. Подтвердите изменение

### Проверить свои права

```python
# В backend
from core.config import can_manage_role

# Проверка: может ли admin назначить роль director?
can_manage_role('admin', 'director')  # False

# Проверка: может ли director назначить роль admin?
can_manage_role('director', 'admin')  # True
```

## ⚠️ Важные замечания

1. **Директор - единственная роль с полным доступом**
   - Будьте осторожны при назначении этой роли
   - Директора могут изменять друг друга

2. **Админ не может стать директором самостоятельно**
   - Только существующий директор может назначить новых директоров

3. **Логирование**
   - Все изменения ролей логируются в `activity_log`
   - Можно отследить, кто и когда изменил роль

## 🔧 Техническая информация

### Файлы, задействованные в системе

**Backend:**
- `backend/core/config.py` - определение ROLES, функции проверки
- `backend/api/roles.py` - API для получения ролей
- `backend/api/users.py` - API для изменения ролей
- `backend/db/__init__.py` - функции работы с БД

**Frontend:**
- `frontend/src/pages/admin/Users.tsx` - UI управления пользователями
- `frontend/src/services/api.ts` - API клиент

### Структура ROLES

```python
ROLES = {
    'director': {
        'name': 'Директор',
        'permissions': '*',  # Все права
        'can_manage_roles': ['admin', 'manager', 'sales', 'marketer', 'employee'],
        'hierarchy_level': 100
    },
    'admin': {
        'name': 'Администратор',
        'permissions': [...],
        'can_manage_roles': ['manager', 'sales', 'marketer', 'employee'],
        'hierarchy_level': 80
    },
    # ... другие роли
}
```

## 📝 История изменений

- **2025-11-16**: Реализована система иерархии ролей
- **2025-11-16**: Обновлены роли admin и tursunay на director
- **2025-11-16**: Добавлена фильтрация ролей в API
- **2025-11-16**: Обновлен UI с информацией об ограничениях
