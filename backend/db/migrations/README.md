# 📋 Система миграций базы данных

## Структура

```
migrations/
├── run_all_migrations.py    # 🚀 Главный файл - запускает все миграции по порядку
├── schema/                   # Изменения структуры БД (новые таблицы, колонки)
├── data/                     # Заполнение/обновление данных
└── maintenance/              # Обслуживание и проверки
```

## 🚀 Автозапуск

Миграции **автоматически запускаются** при старте `main.py` (строка 335-337):

```python
from db.migrations.run_all_migrations import run_all_migrations
run_all_migrations()
```

## Порядок выполнения

### 1️⃣ Инициализация базовых таблиц
- `db.init_database()` - создает основные таблицы

### 2️⃣ Миграции структуры (schema/)

#### Клиенты
- `add_client_interests` - таблица интересов клиентов (#5 - горячие клиенты)
- `add_client_accounts` - учетные записи клиентов
- `add_client_notes` - заметки о клиентах
- `add_bot_modes` - режимы работы бота (autopilot/assistant/manual)
- `add_temperature_field` - температура клиента (hot/warm/cold)

#### Записи (Bookings)
- `add_waitlist` - лист ожидания
- `add_master_field` - поле мастера в записях
- `add_employee_id_to_bookings` - ID сотрудника в записях

#### Услуги
- `add_service_courses` - курсы услуг

#### Сотрудники
- `create_employees` - таблица сотрудников
- `create_employee_services` - связь сотрудников и услуг
- `add_employee_translations` - переводы имен (RU/AR) - ⚠️ **УСТАРЕЛО** (заменено на автотранслитерацию)
- `create_employee_schedules` - расписание сотрудников
- `add_employee_service_provider` - провайдер услуг
- `add_employee_birthdays` - дни рождения сотрудников
- `add_salary_system` - система зарплат

#### Чат и коммуникации
- `add_chat_features` - фичи чата
- `add_telegram_username` - Telegram username
- `add_language_column` - язык клиента
- `add_notes_field` - поле заметок

#### Настройки
- `add_universal_settings` - универсальные настройки
- `add_manager_consultation` - консультации менеджера (#Бот помоги#)

#### Пользователи и права
- `add_permissions_system` - система прав доступа
- `add_user_position` - должность пользователя
- `enhance_permissions_system` - улучшение прав
- `add_user_photo` - фото пользователя
- `create_director_approvals` - согласования директора
- `add_user_subscriptions` - подписки пользователей
- `add_subscription_channels` - каналы подписок
- `create_broadcast_history` - история рассылок
- `create_positions_table` - таблица должностей
- `add_password_reset_fields` - сброс пароля
- `add_email_verification_token` - подтверждение email

### 3️⃣ Миграции данных (data/)

- `migrate_salon_settings` - настройки салона
- `migrate_bot_settings` - настройки бота (персональность, возражения, эмодзи)
- `migrate_services` - заполнение услуг (77 услуг M.Le Diamant)
- `update_existing_users_roles` - обновление ролей пользователей

### 4️⃣ Заполнение начальных данных (опционально)

- `seed_employees` - создание сотрудников (SIMO, MESTAN, LYAZZAT, GULYA, JENNIFER, Tursunay)
- `link_employees_to_services` - связывание сотрудников с услугами

## 🔧 Обслуживание (maintenance/)

- `link_employees_to_services` - связать сотрудников с услугами
- `check_translations` - проверка переводов
- `check_schedules` - проверка расписаний

## ✨ Устаревшие миграции

### ⚠️ add_employee_translations.py
**УСТАРЕЛО!** Заменено на универсальную автотранслитерацию в `utils/transliteration.py`

Раньше:
- Ручные переводы: SIMO → Симо (вручную в БД)
- Требовало обновления для каждого нового сотрудника

Теперь:
- Автоматическая транслитерация: SIMO → Симо (автоматически)
- Работает для любых имен без настройки

## 📝 Как добавить новую миграцию

1. Создайте файл в нужной папке:
   - `schema/` - если меняете структуру БД
   - `data/` - если обновляете данные
   - `maintenance/` - если это обслуживание

2. Добавьте функцию:
```python
def migrate():
    conn = sqlite3.connect(DATABASE_NAME)
    c = conn.cursor()

    # Ваш код миграции
    c.execute("ALTER TABLE...")

    conn.commit()
    conn.close()
```

3. Добавьте в `run_all_migrations.py`:
```python
structure_migrations = [
    # ...
    ('schema.your_migration', 'migrate'),
]
```

## 🚨 Важно

- Миграции **идемпотентны** - можно запускать много раз без ошибок
- Проверяйте наличие колонок/таблиц перед изменениями:
```python
c.execute("PRAGMA table_info(table_name)")
columns = [col[1] for col in c.fetchall()]
if 'new_column' not in columns:
    c.execute("ALTER TABLE...")
```

- Используйте `log_info/log_warning/log_error` для логирования
- Тестируйте миграции перед деплоем!

## 🔍 Диагностика

Проверить состояние БД:
```bash
GET /admin/diagnostics
```

Запустить конкретную миграцию (только development):
```bash
POST /admin/run-migration/{migration_name}
```
