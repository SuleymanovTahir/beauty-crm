# 🧠 Smart Assistant - Умный AI-ассистент с памятью о клиенте

## 📋 Обзор

SmartAssistant - это интеллектуальная система персонализации общения с клиентами, которая:

- **Запоминает** предпочтения клиентов
- **Анализирует** паттерны поведения и записей
- **Предсказывает** следующую запись
- **Персонализирует** приветствия и предложения
- **Обучается** на каждой новой записи

## 🗄️ Структура базы данных

### Таблица `client_preferences`

Хранит явные предпочтения клиентов:

```sql
CREATE TABLE client_preferences (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    client_id TEXT UNIQUE NOT NULL,
    preferred_master TEXT,              -- Любимый мастер
    preferred_service TEXT,             -- Любимая услуга
    preferred_day_of_week INTEGER,      -- Предпочитаемый день недели (0=Пн, 6=Вс)
    preferred_time_of_day TEXT,         -- Время дня: 'morning', 'afternoon', 'evening'
    allergies TEXT,                     -- Аллергии
    special_notes TEXT,                 -- Особые заметки
    auto_book_enabled INTEGER DEFAULT 0,
    auto_book_interval_weeks INTEGER DEFAULT 4,
    created_at TEXT,
    updated_at TEXT
)
```

### Таблица `conversation_context`

Хранит контекст многоступенчатых диалогов:

```sql
CREATE TABLE conversation_context (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    client_id TEXT NOT NULL,
    context_type TEXT NOT NULL,         -- 'booking_in_progress', 'waiting_for_answer', etc.
    context_data TEXT,                  -- JSON с деталями контекста
    created_at TEXT,
    expires_at TEXT                     -- Когда контекст истекает
)
```

### Таблица `client_interaction_patterns`

ML-паттерны взаимодействий:

```sql
CREATE TABLE client_interaction_patterns (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    client_id TEXT NOT NULL,
    interaction_type TEXT,              -- 'preferred_master', 'time_pattern', etc.
    pattern_data TEXT,                  -- JSON с данными паттерна
    confidence_score REAL,              -- Уверенность в паттерне (0.0 - 1.0)
    last_updated TEXT
)
```

## 🔧 API Endpoints

### 1. Получить предпочтения клиента

```bash
GET /api/clients/{client_id}/preferences
```

**Ответ:**
```json
{
  "preferences": {
    "preferred_master": "Jennifer",
    "preferred_service": "Маникюр",
    "preferred_time_of_day": "afternoon",
    "allergies": "Нет",
    "special_notes": "Любит яркие цвета"
  },
  "has_preferences": true,
  "history_count": 5
}
```

### 2. Обновить предпочтения

```bash
POST /api/clients/{client_id}/preferences
Content-Type: application/json

{
  "preferred_master": "Jennifer",
  "preferred_service": "Маникюр",
  "preferred_time_of_day": "afternoon",
  "allergies": "Нет аллергий",
  "special_notes": "VIP клиент"
}
```

### 3. Получить персонализированное приветствие

```bash
GET /api/clients/{client_id}/smart-greeting
```

**Ответ:**
```json
{
  "greeting": "Привет, Анна! 💖 Давно не виделись! Прошло уже 45 дней. Пора бы нам встретиться! 😉",
  "client_id": "anna_123",
  "client_name": "Анна"
}
```

### 4. Получить умное предложение записи

```bash
GET /api/clients/{client_id}/smart-suggestion
```

**Ответ:**
```json
{
  "suggestion": {
    "service": "Маникюр",
    "master": "Jennifer",
    "time_of_day": "afternoon",
    "recommended_date": "2025-11-25",
    "confidence": 0.85
  },
  "message": "Как обычно, Маникюр к мастеру Jennifer? 😊 Могу записать на 2025-11-25!",
  "client_id": "anna_123",
  "client_name": "Анна"
}
```

## 💡 Как работает SmartAssistant

### 1. Персонализированные приветствия

SmartAssistant анализирует:
- Является ли клиент новым (нет истории записей)
- Сколько дней прошло с последнего визита
- Частоту посещений

**Примеры:**

```python
# Новый клиент
"Привет, Анна! 👋 Рады видеть тебя впервые! Я помогу тебе записаться на процедуру."

# Был недавно (< 7 дней)
"Привет, Анна! 😊 Как впечатления от последнего визита?"

# Давно не был (< 30 дней)
"Привет, Анна! 💖 Давно не виделись! Как дела?"

# Очень давно (> 30 дней)
"Привет, Анна! 🌟 Соскучились! Прошло уже 45 дней. Пора бы нам встретиться! 😉"
```

### 2. Умные предложения записей

Система анализирует:
- Историю записей клиента
- Сохраненные предпочтения
- Паттерны времени (утро/день/вечер)
- Средний интервал между визитами

**Уровни уверенности:**

- **Высокая (> 0.8)**: Прямое предложение
  ```
  "Как обычно, Маникюр к мастеру Jennifer? 😊 Могу записать на 2025-11-25!"
  ```

- **Средняя (0.5 - 0.8)**: Предложение с вопросом
  ```
  "Давно не делала Маникюр! Записать к Jennifer как в прошлый раз?"
  ```

- **Низкая (< 0.5)**: Общий вопрос
  ```
  "Что будем делать на этот раз? 💅"
  ```

### 3. Машинное обучение

При каждой новой записи SmartAssistant:

1. **Анализирует паттерны**:
   - Если клиент 3+ раза был у одного мастера → сохраняет как предпочтение
   - Вычисляет средний интервал между визитами
   - Определяет предпочитаемое время дня

2. **Обновляет уверенность**:
   ```python
   confidence = min(visit_count / 10.0, 1.0)
   ```

3. **Сохраняет в БД**:
   ```python
   assistant.learn_from_booking({
       'service': 'Маникюр',
       'master': 'Jennifer',
       'datetime': '2025-11-20 15:00'
   })
   ```

## 🔌 Интеграция с ботом

SmartAssistant автоматически интегрирован в AI-бота (`bot/core.py`):

```python
# В методе generate_response()

# Персонализированное приветствие
if self.should_greet(history):
    assistant = SmartAssistant(instagram_id)
    greeting = assistant.get_personalized_greeting(client_name)
    additional_context += f"ПЕРСОНАЛИЗИРОВАННОЕ ПРИВЕТСТВИЕ:\n{greeting}"

# Умное предложение записи
suggestion = assistant.suggest_next_booking()
if suggestion and suggestion['confidence'] > 0.6:
    additional_context += f"УМНОЕ ПРЕДЛОЖЕНИЕ:\n{suggestion_message}"
```

Бот получает эти подсказки в промпте и использует их для ответов!

## 📊 Использование в коде

### Backend (Python)

```python
from services.smart_assistant import SmartAssistant

# Создать ассистента для клиента
assistant = SmartAssistant(client_id="anna_123")

# Получить персонализированное приветствие
greeting = assistant.get_personalized_greeting("Анна")
print(greeting)

# Получить умное предложение
suggestion = assistant.suggest_next_booking()
if suggestion:
    print(f"Услуга: {suggestion['service']}")
    print(f"Мастер: {suggestion['master']}")
    print(f"Дата: {suggestion['recommended_date']}")
    print(f"Уверенность: {suggestion['confidence']*100}%")

# Сохранить предпочтения
assistant.save_preferences({
    'preferred_master': 'Jennifer',
    'preferred_service': 'Маникюр',
    'preferred_time_of_day': 'afternoon'
})

# Обучиться на основе записи
assistant.learn_from_booking({
    'service': 'Маникюр',
    'master': 'Jennifer',
    'datetime': '2025-11-20 15:00'
})
```

### Frontend (TypeScript)

```typescript
// Получить предпочтения клиента
const response = await fetch(`/api/clients/${clientId}/preferences`, {
  credentials: 'include'
});
const data = await response.json();

if (data.has_preferences) {
  console.log('Любимый мастер:', data.preferences.preferred_master);
}

// Получить умное предложение
const suggestionResponse = await fetch(`/api/clients/${clientId}/smart-suggestion`, {
  credentials: 'include'
});
const suggestion = await suggestionResponse.json();

console.log('Предложение:', suggestion.message);
console.log('Рекомендуемая дата:', suggestion.suggestion.recommended_date);

// Обновить предпочтения
await fetch(`/api/clients/${clientId}/preferences`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  credentials: 'include',
  body: JSON.stringify({
    preferred_master: 'Jennifer',
    preferred_service: 'Маникюр',
    preferred_time_of_day: 'afternoon'
  })
});
```

## ✅ Что уже реализовано

- ✅ Таблицы БД для предпочтений, контекста и паттернов
- ✅ SmartAssistant класс с полным функционалом
- ✅ API endpoints для работы с предпочтениями
- ✅ Интеграция с AI-ботом для персонализации
- ✅ Автоматическое обучение при создании записей
- ✅ Персонализированные приветствия
- ✅ Умные предложения записей
- ✅ ML-анализ паттернов клиента

## 🚀 Следующие шаги (TODO)

### Frontend UI для клиентов:

1. **Страница предпочтений клиента** (`/client/preferences`)
   - Форма для указания любимого мастера
   - Выбор предпочитаемого времени
   - Аллергии и особые заметки
   - Автоматическая запись (вкл/выкл)

2. **Карточка клиента в админке**
   - Показывать сохраненные предпочтения
   - ML-предложения на основе паттернов
   - График посещений

3. **Умные подсказки в форме записи**
   - Автозаполнение любимого мастера
   - Предложение времени на основе истории
   - "Обычная услуга" для постоянных клиентов

## 🎯 Преимущества

- **Персонализация**: Каждый клиент чувствует индивидуальный подход
- **Экономия времени**: Предзаполненные формы записи
- **Увеличение конверсии**: Умные напоминания в нужное время
- **ML-аналитика**: Прогнозирование потребностей клиента
- **Лояльность**: Клиенты ценят, что вы их помните

## 📝 Примеры использования

### Сценарий 1: Постоянный клиент возвращается

```
Клиент: Привет!

Бот (использует SmartAssistant):
"Привет, Анна! 💖 Давно не виделись! Прошло уже 32 дня.
Как обычно, Маникюр к мастеру Jennifer? 😊
Могу записать на 25 ноября в обед!"
```

### Сценарий 2: Новый клиент

```
Клиент: Хочу записаться

Бот:
"Привет! 👋 Рады видеть тебя впервые!
Я помогу тебе записаться. Какая процедура интересует?"
```

### Сценарий 3: Клиент был недавно

```
Клиент: Здравствуйте

Бот:
"Привет, Анна! 😊 Как впечатления от последнего маникюра?
Хочешь записаться на что-то еще?"
```

## 🔒 Безопасность и конфиденциальность

- Все данные клиента хранятся локально в SQLite
- API требует аутентификации (session_token)
- Предпочтения доступны только авторизованным пользователям
- Логирование всех операций с предпочтениями

## 📈 Метрики для оценки эффективности

После внедрения отслеживайте:

1. **Процент повторных записей** - должен вырасти
2. **Время на оформление записи** - должно сократиться
3. **Конверсия предложений** - сколько клиентов приняли умное предложение
4. **Средний интервал между визитами** - должен сократиться
5. **NPS (Net Promoter Score)** - удовлетворенность клиентов

---

**Создано:** 2025-11-16
**Версия:** 1.0
**Статус:** ✅ Полностью реализовано (Backend)
