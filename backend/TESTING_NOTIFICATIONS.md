# Тестирование уведомлений и напоминаний

## Подготовка к тестированию

### 1. Настройка переменных окружения

Убедитесь, что в `.env` файле заданы следующие переменные:

```env
# Instagram
PAGE_ACCESS_TOKEN=ваш_токен
INSTAGRAM_BUSINESS_ID=ваш_бизнес_id

# Telegram
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# Email (опционально)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=ваш_пароль
SMTP_FROM=ваш_email@gmail.com
```

### 2. Запуск миграций

```bash
# Запустите миграцию для добавления telegram_chat_id
python3 backend/run_migration_telegram_chat_id.py

# Запустите миграцию для preferred_messenger (если нужна)
python3 backend/run_migration_preferred_messenger.py
```

### 3. Настройка мастеров

Для тестирования уведомлений мастерам, необходимо:

1. Убедиться, что у мастера в таблице `users` заполнены поля:
   - `email` - для email уведомлений
   - `telegram_username` - username в Telegram
   - `telegram_chat_id` - chat ID в Telegram

Вы можете обновить это вручную:

```sql
UPDATE users
SET email = 'master@example.com',
    telegram_username = '@username',
    telegram_chat_id = '123456789'
WHERE username = 'jennifer';  -- имя мастера
```

Чтобы получить `telegram_chat_id`, мастер должен:
1. Написать боту сообщение `/start`
2. Бот сохранит его chat_id автоматически

## Тестирование уведомлений для мастеров

### Тест 1: Создание записи с уведомлением мастера

1. Создайте новую запись на странице `/admin/bookings`
2. Выберите мастера (например, "Jennifer")
3. Заполните все поля и сохраните
4. Проверьте:
   - В консоли сервера должно быть сообщение об отправке уведомления
   - Мастер должен получить уведомление в Telegram (если настроен)
   - Мастер должен получить email (если настроен)

**API запрос для тестирования:**

```bash
curl -X POST http://localhost:8000/api/bookings \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "instagram_id": "test_client",
    "name": "Test Client",
    "service": "Massage 60 min",
    "date": "2025-11-17",
    "time": "14:00",
    "phone": "+1234567890",
    "master": "Jennifer"
  }'
```

### Тест 2: Проверка логов уведомлений

```bash
curl http://localhost:8000/api/notifications/master/logs \
  -b "session_token=ваш_токен"
```

Вы должны увидеть список отправленных уведомлений с их статусами.

## Тестирование напоминаний клиентам

### Тест 3: Отправка напоминания вручную

**Для Instagram (например, @stz_192):**

```bash
curl -X POST http://localhost:8000/api/notifications/reminders/send \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "client_id": "8017607155639936",
    "booking_id": 1,
    "messenger": "instagram"
  }'
```

Проверьте Instagram Direct Messages - клиент должен получить напоминание.

### Тест 4: Массовая отправка напоминаний

Отправить напоминания всем клиентам с записями на следующие 24 часа:

```bash
curl -X POST http://localhost:8000/api/notifications/reminders/send-batch \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "hours_before": 24
  }'
```

### Тест 5: Проверка логов напоминаний

```bash
curl http://localhost:8000/api/notifications/reminders/logs \
  -b "session_token=ваш_токен"
```

## Тестирование рассылок (broadcast)

### Тест 6: Отправка рекламного сообщения

**Всем активным клиентам:**

```bash
curl -X POST http://localhost:8000/api/notifications/broadcast \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "message": "🎉 Специальное предложение! Скидка 20% на все услуги до конца месяца!",
    "filter": "active",
    "messenger": "all"
  }'
```

**Только VIP клиентам:**

```bash
curl -X POST http://localhost:8000/api/notifications/broadcast \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "message": "💎 VIP предложение! Эксклюзивная скидка 30% только для вас!",
    "filter": "vip",
    "messenger": "all"
  }'
```

**Только в Instagram:**

```bash
curl -X POST http://localhost:8000/api/notifications/broadcast \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "message": "Привет! Не забудьте подписаться на наш Instagram для новостей!",
    "filter": "all",
    "messenger": "instagram"
  }'
```

## Тестирование preferred_messenger

### Тест 7: Установка предпочтительного мессенджера

```bash
curl -X POST http://localhost:8000/api/clients/8017607155639936/preferred-messenger \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "preferred_messenger": "instagram"
  }'
```

Доступные варианты: `instagram`, `telegram`, `whatsapp`, `tiktok`, `null`

### Тест 8: Напоминание через preferred_messenger

После установки preferred_messenger, все напоминания будут отправляться через выбранный канал:

```bash
curl -X POST http://localhost:8000/api/notifications/reminders/send \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "client_id": "8017607155639936",
    "booking_id": 1
  }'
```

Система автоматически выберет preferred_messenger для этого клиента.

## Специальный тест для @stz_192

### Instagram ID для @stz_192

Согласно данным, ID Instagram для @stz_192: `8017607155639936`

### Тестирование напоминания

1. Создайте запись для @stz_192:

```bash
curl -X POST http://localhost:8000/api/bookings \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "instagram_id": "8017607155639936",
    "name": "stz_192",
    "service": "Маникюр",
    "date": "2025-11-18",
    "time": "15:00",
    "phone": "",
    "master": "Jennifer"
  }'
```

2. Отправьте напоминание:

```bash
curl -X POST http://localhost:8000/api/notifications/reminders/send \
  -H "Content-Type: application/json" \
  -b "session_token=ваш_токен" \
  -d '{
    "client_id": "8017607155639936",
    "messenger": "instagram"
  }'
```

3. Проверьте Instagram Direct Messages для @stz_192

## Мониторинг и отладка

### Проверка логов сервера

```bash
tail -f backend/logs/application.log
```

### Проверка базы данных

```bash
# Проверить таблицу notification_logs
sqlite3 backend/salon_bot.db "SELECT * FROM notification_logs ORDER BY created_at DESC LIMIT 10;"

# Проверить таблицу reminder_logs
sqlite3 backend/salon_bot.db "SELECT * FROM reminder_logs ORDER BY created_at DESC LIMIT 10;"
```

### Возможные проблемы

1. **Уведомление не отправлено**
   - Проверьте наличие токенов в .env
   - Проверьте права доступа пользователя
   - Проверьте логи сервера

2. **Telegram chat_id не найден**
   - Мастер должен написать боту `/start`
   - Обновите поле `telegram_chat_id` в таблице users

3. **Instagram сообщение не доставлено**
   - Проверьте PAGE_ACCESS_TOKEN
   - Убедитесь, что клиент ранее писал в Direct
   - Проверьте, что у вас есть права на отправку сообщений

4. **Email не отправлен**
   - Проверьте SMTP настройки
   - Убедитесь, что включен доступ для сторонних приложений (для Gmail)

## Автоматизация (опционально)

Для автоматической отправки напоминаний можно настроить cron job:

```bash
# Отправлять напоминания каждый час
0 * * * * curl -X POST http://localhost:8000/api/notifications/reminders/send-batch -H "Content-Type: application/json" -b "session_token=TOKEN" -d '{"hours_before": 24}'
```

## Результаты тестирования

После выполнения всех тестов, зафиксируйте результаты:

- [ ] Тест 1: Уведомление мастера при создании записи
- [ ] Тест 2: Логи уведомлений мастеров
- [ ] Тест 3: Ручное напоминание клиенту
- [ ] Тест 4: Массовая отправка напоминаний
- [ ] Тест 5: Логи напоминаний клиентам
- [ ] Тест 6: Рекламная рассылка
- [ ] Тест 7: Установка preferred_messenger
- [ ] Тест 8: Напоминание через preferred_messenger
- [ ] Специальный тест: Напоминание для @stz_192 в Instagram
