# 🧪 Комплексное тестирование всей системы

## Что это?

`comprehensive_test.py` - это ОДИН файл который проверяет **ВСЕ** функции системы и показывает **МАКСИМАЛЬНО ПОДРОБНЫЕ** ошибки.

## ✨ Особенности

- ✅ **Все тесты в одном файле** - не нужно искать по разным местам
- ✅ **Максимально подробные ошибки** - каждая ошибка описана с решением
- ✅ **Цветной вывод** - красиво и понятно
- ✅ **Проверка всех функций** - база данных, пользователи, сотрудники, должности, настройки, услуги
- ✅ **Интеграция с main.py** - можно запускать при старте приложения

## 🚀 Способы запуска

### 1. Запуск напрямую (рекомендуется)

```bash
cd backend
python comprehensive_test.py
```

### 2. Запуск при старте приложения

В файле `main.py` раскомментируйте строки 366-375:

```python
# ДО:
# try:
#     from comprehensive_test import run_comprehensive_test
#     ...

# ПОСЛЕ (раскомментировать):
try:
    from comprehensive_test import run_comprehensive_test
    log_info("🧪 Запуск комплексного тестирования...", "startup")
    test_success = run_comprehensive_test()
    if not test_success:
        log_warning("⚠️ Некоторые тесты провалены (см. вывод выше)", "startup")
    else:
        log_info("✅ Все тесты пройдены!", "startup")
except Exception as e:
    log_error(f"⚠️ Ошибка запуска тестов: {e}", "startup")
```

Теперь тесты будут запускаться каждый раз при старте приложения!

## 📊 Что тестируется

### 🗄️ База данных
1. ✅ База данных существует и не пустая
2. ✅ Все необходимые таблицы созданы
3. ✅ Структура таблицы users правильная

### 👤 Пользователи
4. ✅ В системе есть пользователи
5. ✅ **КРИТИЧЕСКИЙ**: Все пользователи имеют должности (не NULL)
6. ✅ **КРИТИЧЕСКИЙ**: Должности в правильном формате (не ALL CAPS)

### 👥 Сотрудники
7. ✅ В системе есть сотрудники
8. ✅ **КРИТИЧЕСКИЙ**: Все сотрудники имеют должности
9. ✅ **КРИТИЧЕСКИЙ**: Имена сотрудников в правильном формате

### 📋 Должности
10. ✅ Справочник должностей существует
11. ✅ Стандартные должности присутствуют

### ⚙️ Настройки
12. ✅ Настройки салона корректны
13. ✅ booking_url правильный

### 💈 Услуги
14. ✅ В системе есть услуги

### 🔗 Интеграция
15. ✅ Связь users <-> employees работает

## 📈 Пример вывода

### ✅ Успешный тест

```
═══════════════════════════════════════════════════════════════════════
   🧪 КОМПЛЕКСНАЯ ПРОВЕРКА ВСЕЙ СИСТЕМЫ BEAUTY CRM
═══════════════════════════════════════════════════════════════════════

────────────────────────────────────────────────────────────────────────
  👤 ПОЛЬЗОВАТЕЛИ
────────────────────────────────────────────────────────────────────────

  ✅ PASS - Все пользователи имеют должности (CRITICAL)
      ✅ Все пользователи имеют должности:
      ✅ admin            | Администратор             | Администратор
      ✅ simo             | Simo                      | Hair Stylist
      ✅ mestan           | Mestan                    | Hair Stylist
      ✅ lyazzat          | Lyazzat                   | Nail Master
```

### ❌ Проваленный тест

```
  ❌ FAIL - Все пользователи имеют должности (CRITICAL)
      ❌ ОШИБКА: КРИТИЧЕСКАЯ ОШИБКА: 3 пользователей без должности!
      ❌ Пользователей БЕЗ должности: 3
      ✅ Пользователей С должностью: 4

      ❌ ПОЛЬЗОВАТЕЛИ БЕЗ ДОЛЖНОСТИ:
        • ID: 2, Username: simo, Name: Simo, Position: NULL, Role: employee
        • ID: 3, Username: mestan, Name: Mestan, Position: NULL, Role: employee
        • ID: 4, Username: lyazzat, Name: Lyazzat, Position: NULL, Role: employee

      💡 РЕШЕНИЕ:
        1. Проверьте файл backend/db/migrations/data/seed_employees.py
        2. Убедитесь что position добавляется в INSERT для users (строка ~104)
        3. Проверьте что assign_user_positions НЕ запускается ДО seed_employees
        4. Перезапустите приложение с пустой базой данных

      📊 Данные: {
        "users_without_position": [
          {"id": 2, "username": "simo", "full_name": "Simo", ...}
        ]
      }
```

### Итоговый отчет

```
═══════════════════════════════════════════════════════════════════════
   📊 ИТОГОВЫЙ ОТЧЕТ
═══════════════════════════════════════════════════════════════════════

📈 Статистика:
   ├─ Всего тестов: 14
   ├─ Пройдено: 11
   ├─ Провалено: 3
   └─ Время выполнения: 0.45 секунд

❌ ПРОВАЛЕННЫЕ ТЕСТЫ:

  ❌ [Users] Все пользователи имеют должности (CRITICAL)
     Ошибка: КРИТИЧЕСКАЯ ОШИБКА: 3 пользователей без должности!

  ❌ [Users] Должности в правильном формате (не ALL CAPS)
     Ошибка: Найдено 2 должностей в неправильном формате

💡 Подробности об ошибках смотрите выше в разделах тестов

╔════════════════════════════════════════════════════════════════════╗
║                                                                    ║
║                    ❌ ЕСТЬ ПРОВАЛЕННЫЕ ТЕСТЫ                       ║
║                                                                    ║
╚════════════════════════════════════════════════════════════════════╝
```

## 🔍 Подробности об ошибках

Каждая ошибка содержит:

1. **❌ Описание проблемы** - что именно не так
2. **📊 Данные** - какие данные вызвали ошибку
3. **💡 Решение** - как исправить
4. **📁 Файлы** - какие файлы нужно проверить
5. **🔢 Строки** - где именно искать проблему

## 🎯 Критические тесты

Эти тесты помечены как **(CRITICAL)** и **ОБЯЗАТЕЛЬНО** должны проходить:

- ✅ Все пользователи имеют должности
- ✅ Должности в правильном формате (не ALL CAPS)
- ✅ Все сотрудники имеют должности
- ✅ Имена сотрудников в правильном формате

Эти тесты ловят проблемы которые вы описывали!

## 🔧 Если тест падает

### Пример: Пользователи без должностей

**Ошибка:**
```
❌ КРИТИЧЕСКАЯ ОШИБКА: 3 пользователей без должности!
```

**Решение:**
1. Откройте `backend/db/migrations/data/seed_employees.py`
2. Найдите строку ~104:
   ```python
   INSERT INTO users (username, password_hash, full_name, email, role, employee_id, ...)
   ```
3. Убедитесь что есть поле `position` и оно заполняется:
   ```python
   INSERT INTO users (username, password_hash, full_name, email, role, position, employee_id, ...)
   VALUES (?, ?, ?, ?, ?, ?, ?, ?)
   ```
4. Перезапустите приложение с пустой базой:
   ```bash
   rm salon_bot.db
   uvicorn main:app --reload
   ```

## 💻 Интеграция с CI/CD

Можно использовать для автоматической проверки при деплое:

```bash
# В вашем CI/CD скрипте
cd backend
python comprehensive_test.py
if [ $? -ne 0 ]; then
    echo "Тесты провалены! Деплой отменен."
    exit 1
fi
```

## 📝 Добавление новых тестов

Чтобы добавить новый тест:

1. Создайте метод в классе `ComprehensiveTest`:

```python
def test_my_new_check(self) -> TestResult:
    """16. Описание нового теста"""
    result = TestResult("Название теста", "Категория")

    try:
        conn = self.connect_db()
        cursor = conn.cursor()

        # Ваша проверка
        cursor.execute("SELECT ...")
        data = cursor.fetchone()

        if not data:
            result.fail("Что-то не так", [
                "❌ Подробности",
                "💡 Решение"
            ])
        else:
            result.success("Все хорошо")

        conn.close()
    except Exception as e:
        result.fail(f"Ошибка: {str(e)}", [
            f"❌ Traceback:\n{traceback.format_exc()}"
        ])

    return result
```

2. Добавьте тест в список `all_tests` в методе `run_all_tests()`:

```python
# Settings
("⚙️ НАСТРОЙКИ", [
    self.test_salon_settings_exist,
    self.test_my_new_check,  # ← Добавьте сюда
]),
```

## 🎉 Преимущества

- ✅ **Один файл** - не нужно искать тесты по разным местам
- ✅ **Подробные ошибки** - всегда понятно что не так и как исправить
- ✅ **Быстрый запуск** - просто `python comprehensive_test.py`
- ✅ **Интеграция** - можно запускать при старте приложения
- ✅ **Не ломает систему** - тесты только читают данные, не меняют
- ✅ **Цветной вывод** - легко найти ошибки

---

**Запускайте тесты регулярно чтобы убедиться что все работает правильно!** 🚀
