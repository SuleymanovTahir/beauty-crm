# 👥 Клиентская часть - Реализованный функционал

## ✅ Что было сделано (Backend)

### 1. 📊 Миграция базы данных

**Файл:** `db/migrations/add_client_accounts.py`

Добавлены следующие возможности в БД:

#### Новые колонки в таблице `clients`:
- `email` - email клиента
- `password_hash` - хэш пароля
- `birthday` - дата рождения (YYYY-MM-DD)
- `created_at` - дата регистрации
- `last_login` - последний вход
- `is_verified` - подтвержден ли email

#### Новые таблицы:

**`password_reset_tokens`** - токены восстановления пароля
```sql
- id (PRIMARY KEY)
- client_email (TEXT)
- token (TEXT UNIQUE)
- created_at (TEXT)
- expires_at (TEXT)
- used (INTEGER DEFAULT 0)
```

**`client_notifications`** - уведомления для клиентов
```sql
- id (PRIMARY KEY)
- client_instagram_id (TEXT)
- client_email (TEXT)
- notification_type (TEXT) -- 'birthday', 'news', 'reminder', 'promotion'
- title (TEXT)
- message (TEXT)
- sent_at (TEXT)
- read_at (TEXT)
- created_at (TEXT)
```

**`salon_news`** - новости салона
```sql
- id (PRIMARY KEY)
- title_ru, title_en, title_ar (TEXT)
- content_ru, content_en, content_ar (TEXT)
- image_url (TEXT)
- published_at (TEXT)
- created_at (TEXT)
- is_active (INTEGER DEFAULT 1)
```

---

### 2. 🔐 API для клиентской авторизации

**Файл:** `api/client_auth.py`

#### Endpoints:

##### `POST /public/client/register`
Регистрация нового клиента
```json
{
  "email": "client@example.com",
  "password": "securepassword",
  "name": "Иван Иванов",
  "phone": "+971501234567",
  "birthday": "1990-05-15"
}
```

##### `POST /public/client/login`
Вход клиента
```json
{
  "email": "client@example.com",
  "password": "securepassword"
}
```
Возвращает токен сессии и данные клиента.

##### `POST /public/client/request-password-reset`
Запрос на восстановление пароля
```json
{
  "email": "client@example.com"
}
```
Отправляет email со ссылкой для сброса пароля.

##### `POST /public/client/reset-password`
Сброс пароля по токену
```json
{
  "token": "recovery_token_here",
  "new_password": "newpassword"
}
```

##### `GET /public/client/my-bookings?client_id=xxx`
Получить историю записей клиента

##### `GET /public/client/my-notifications?client_id=xxx&unread_only=false`
Получить уведомления клиента

##### `POST /public/client/notifications/{id}/mark-read`
Отметить уведомление как прочитанное

---

### 3. 📅 Публичные API для онлайн-записи

**Файл:** `api/public.py` (расширен)

#### Endpoints:

##### `GET /public/employees`
Список активных сотрудников
```json
{
  "employees": [
    {
      "id": 1,
      "username": "master1",
      "full_name": "Ляззат",
      "role": "employee",
      "specialization": "Nail Artist"
    }
  ]
}
```

##### `GET /public/available-slots?date=2025-11-15&employee_id=1`
Доступные слоты для записи на конкретную дату
```json
{
  "date": "2025-11-15",
  "slots": [
    {"time": "10:00", "available": true},
    {"time": "10:30", "available": false},
    {"time": "11:00", "available": true}
  ]
}
```

##### `POST /public/book`
Создать онлайн-запись
```json
{
  "service_id": 1,
  "employee_id": 1,
  "date": "2025-11-15",
  "time": "10:00",
  "name": "Иван Иванов",
  "phone": "+971501234567",
  "email": "client@example.com",
  "notes": "Предпочитаю утро"
}
```

##### `GET /public/news?limit=10&lang=ru`
Получить новости салона

---

### 4. 🎂 Автоматические поздравления с днем рождения

**Файл:** `scheduler/birthday_checker.py` (расширен)

#### Функционал:

1. **Проверка дней рождения клиентов**
   - Запускается каждый день в 10:00
   - Проверяет таблицу `clients` на совпадение даты рождения

2. **Автоматическая отправка поздравлений**
   - Отправка в Instagram (если instagram_id доступен)
   - Сохранение уведомления в БД
   - Предотвращение дублирования (проверка отправки за день)

3. **Персонализированное сообщение**
   ```
   🎉🎂 С Днём Рождения, {name}! 🎂🎉

   Команда {salon_name} поздравляет вас с этим особенным днём!
   Желаем счастья, здоровья, красоты и исполнения всех желаний! ✨

   🎁 Специально для вас - скидка 15% на любую услугу в день рождения!

   Будем рады видеть вас! 💖
   ```

4. **Новые функции:**
   - `get_client_birthdays_today()` - получить клиентов с ДР сегодня
   - `send_birthday_congratulations()` - отправить поздравления
   - `start_client_birthday_checker()` - запустить планировщик

---

## 🚀 Как использовать

### Запуск миграции

```bash
cd backend
python db/migrations/add_client_accounts.py
```

Вы увидите:
```
======================================================================
👥 МИГРАЦИЯ: Система клиентских аккаунтов
======================================================================
✅ Добавлена колонка clients.email
✅ Добавлена колонка clients.password_hash
✅ Добавлена колонка clients.birthday
✅ Создана таблица password_reset_tokens
✅ Создана таблица client_notifications
✅ Создана таблица salon_news
======================================================================
✅ Миграция завершена
======================================================================
```

### Проверка работы API

После запуска `python main.py` вы можете протестировать endpoints:

```bash
# Регистрация
curl -X POST http://localhost:8000/public/client/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "name": "Тест",
    "birthday": "1990-05-15"
  }'

# Логин
curl -X POST http://localhost:8000/public/client/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'

# Доступные слоты
curl "http://localhost:8000/public/available-slots?date=2025-11-15"

# Онлайн-запись
curl -X POST http://localhost:8000/public/book \
  -H "Content-Type: application/json" \
  -d '{
    "service_id": 1,
    "date": "2025-11-15",
    "time": "10:00",
    "name": "Иван",
    "phone": "+971501234567",
    "email": "test@example.com"
  }'
```

---

## 📋 Что еще нужно сделать (Frontend)

### 1. Публичная страница онлайн-записи (`/booking`)

**Функционал:**
- Выбор услуги из списка
- Выбор мастера (опционально)
- Выбор даты в календаре
- Выбор времени из доступных слотов
- Форма с контактными данными
- Подтверждение записи

**Примерная структура:**
```tsx
// frontend/src/pages/public/Booking.tsx

export default function Booking() {
  // 1. Получаем услуги: fetch('/public/services')
  // 2. Получаем мастеров: fetch('/public/employees')
  // 3. Выбор даты -> запрос слотов: fetch('/public/available-slots?date=...')
  // 4. Форма с данными клиента
  // 5. Отправка: POST '/public/book'
  // 6. Redirect на /success
}
```

### 2. Личный кабинет клиента (`/client/cabinet`)

**Функционал:**
- Форма входа / регистрации
- После входа - личная информация:
  - Имя, email, телефон, дата рождения
  - Кнопка "Изменить пароль"
- История записей (таблица или карточки)
- Уведомления (список с фильтром непрочитанных)
- Новости салона

**Примерная структура:**
```tsx
// frontend/src/pages/public/ClientCabinet.tsx

export default function ClientCabinet() {
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [client, setClient] = useState(null);

  if (!isLoggedIn) {
    return <LoginForm onLogin={handleLogin} />;
  }

  return (
    <div>
      <ProfileSection client={client} />
      <BookingHistory clientId={client.id} />
      <Notifications clientId={client.id} />
      <SalonNews />
    </div>
  );
}
```

### 3. Восстановление пароля (`/reset-password`)

**Функционал:**
- Страница запроса восстановления (ввод email)
- Страница сброса пароля (по токену из email)

**Примерная структура:**
```tsx
// frontend/src/pages/public/ResetPassword.tsx

export default function ResetPassword() {
  const { token } = useParams(); // Из URL: /reset-password?token=xxx

  if (!token) {
    return <RequestResetForm />;
  }

  return <NewPasswordForm token={token} />;
}
```

---

## 🎨 Рекомендации по дизайну

1. **Публичная страница записи:**
   - Современный step-by-step wizard (шаги: услуга → мастер → дата → время → контакты)
   - Используйте Calendar component из shadcn/ui
   - Визуализация доступных слотов (зеленые = свободно, серые = занято)
   - Показывать цену выбранной услуги

2. **Личный кабинет:**
   - Карточки для каждой записи (дата, услуга, мастер, статус)
   - Badge для статусов (confirmed, completed, cancelled)
   - Уведомления с иконками (🎂 birthday, 📰 news, 📅 reminder)
   - Возможность отменить запись (если дата не прошла)

3. **Мобильная адаптация:**
   - Все страницы должны быть mobile-first
   - Touch-friendly кнопки и элементы
   - Responsive calendar

---

## 🔧 Технические детали

### Безопасность

1. **Хэширование паролей:** SHA-256 (в продакшене лучше использовать bcrypt)
2. **Токены восстановления:** URL-safe random tokens, срок действия 24 часа
3. **Email verification:** Добавлена колонка `is_verified` (пока не используется)

### Планировщики

Все планировщики запускаются автоматически при старте `main.py`:
- `start_birthday_checker()` - дни рождения сотрудников
- `start_client_birthday_checker()` - поздравления клиентов

### Email отправка

В `client_auth.py` есть функция `send_reset_email()`, но пока она только печатает ссылку в консоль.

Для продакшена нужно настроить SMTP:
```python
import smtplib
from email.mime.text import MIMEText

server = smtplib.SMTP('smtp.gmail.com', 587)
server.starttls()
server.login("your@email.com", "password")
server.send_message(msg)
server.quit()
```

---

## 📝 Тестирование

### Тестовый сценарий:

1. Зарегистрировать клиента с датой рождения = сегодня
2. Проверить, что в 10:00 придет поздравление
3. Создать онлайн-запись через API
4. Проверить, что запись появилась в БД и в истории клиента

### Проверка birthday bot:

```sql
-- Добавить тестового клиента с ДР сегодня
INSERT INTO clients (instagram_id, name, email, birthday, first_contact, last_contact, status)
VALUES ('test_user', 'Тестовый клиент', 'test@test.com', '1990-11-13', datetime('now'), datetime('now'), 'new');

-- Проверить, что поздравление отправится
-- (или изменить в коде время проверки с 10:00 на текущее время для теста)
```

---

## 🚀 Следующие шаги

1. ✅ Backend API готов
2. ✅ Миграция БД готова
3. ✅ Birthday bot готов
4. 🔲 Создать публичную страницу онлайн-записи (Frontend)
5. 🔲 Создать личный кабинет клиента (Frontend)
6. 🔲 Создать страницу восстановления пароля (Frontend)
7. 🔲 Добавить email отправку (настроить SMTP)
8. 🔲 Добавить админ-панель для управления новостями салона

---

**Все изменения закоммичены и запушены!** 🎉
