# Управление настройками бота

## Скрипт fill_bot_settings.py

Скрипт для заполнения и проверки настроек бота в таблице `bot_settings`.

### Использование

#### Проверить текущее состояние полей:
```bash
python fill_bot_settings.py --check
```

#### Заполнить только пустые поля (по умолчанию):
```bash
python fill_bot_settings.py
```

#### Перезаписать все поля (с подтверждением):
```bash
python fill_bot_settings.py --all
```

#### Перезаписать все поля (без подтверждения):
```bash
python fill_bot_settings.py --force
```

### Заполняемые поля

Скрипт заполняет следующие критические поля настроек бота:

1. **price_explanation** - объяснение ценообразования
2. **objection_handling** - обработка возражений клиентов
3. **negative_handling** - работа с недовольными клиентами
4. **example_dialogues** - примеры хороших и плохих диалогов
5. **context_memory** - правила использования контекстной памяти
6. **avoid_repetition** - избегание повторений в диалоге
7. **conversation_flow_rules** - правила ведения диалога
8. **personality_adaptations** - адаптация под разные типы клиентов
9. **smart_objection_detection** - умное распознавание возражений

### Пример вывода

```
📋 Проверка полей bot_settings:
================================================================================
✅ price_explanation: заполнено (217 символов)
✅ objection_handling: заполнено (893 символов)
❌ negative_handling: ПУСТО
...

📊 Итого:
   Заполнено: 7
   Пусто: 2
```

### Требования

- Python 3.6+
- SQLite3
- База данных `salon_bot.db` в текущей директории

### Безопасность

- Скрипт работает только с существующей записью (id=1) в таблице bot_settings
- При перезаписи всех полей требуется подтверждение (если не используется --force)
- Автоматическое обновление поля `updated_at`
