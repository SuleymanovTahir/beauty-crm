# Тестирование и диагностика

Эта папка содержит скрипты для тестирования и диагностики системы уведомлений и напоминаний.

## Структура

```
scripts/testing/
├── api/                    # Тесты API эндпоинтов
│   ├── test_notifications_api.py     # Тест API уведомлений
│   ├── test_save_notifications.py    # Тест сохранения настроек
│   └── test_reminders_api.py         # Тест API напоминаний о записях
├── database/               # Тесты и исправления БД
│   └── fix_notification_settings_table.py  # Исправление схемы таблицы
├── fixes/                  # Скрипты для исправления проблем
└── README.md              # Этот файл
```

## Использование

### 1. Запуск тестов вручную

```bash
cd /path/to/backend

# Тест API уведомлений
python3 scripts/testing/api/test_notifications_api.py

# Тест сохранения настроек
python3 scripts/testing/api/test_save_notifications.py

# Тест API напоминаний о записях
python3 scripts/testing/api/test_reminders_api.py
```

### 2. Запуск тестов при старте сервера

Откройте `main.py` и раскомментируйте нужные тесты в секции "ТЕСТИРОВАНИЕ УВЕДОМЛЕНИЙ":

```python
# ================================
# ТЕСТИРОВАНИЕ УВЕДОМЛЕНИЙ
# ================================

# 1. Тест API уведомлений
from scripts.testing.api.test_notifications_api import test_database_tables, test_notifications_endpoint
log_info("🔔 Тестирование API уведомлений...", "startup")
test_database_tables()
test_notifications_endpoint()

# 2. Тест напоминаний о записях
from scripts.testing.api.test_reminders_api import test_booking_reminder_settings_table
log_info("⏰ Тестирование напоминаний...", "startup")
test_booking_reminder_settings_table()
```

### 3. Исправление проблем

```bash
# Исправить схему таблицы notification_settings
python3 scripts/testing/database/fix_notification_settings_table.py
```

## Описание тестов

### test_notifications_api.py
- ✅ Проверяет существование таблиц БД
- ✅ Тестирует GET /api/notifications/settings
- ✅ Проверяет прямой вызов API функций
- ✅ Выполняет HTTP запросы

**Результат:** 4/4 теста должны пройти

### test_save_notifications.py
- ✅ GET запрос для получения настроек
- ✅ POST запрос для сохранения настроек
- ✅ Проверка что настройки сохранились

**Результат:** Все статусы 200 OK

### test_reminders_api.py
- ✅ Проверяет таблицу booking_reminder_settings
- ✅ Тестирует создание таблицы
- ✅ HTTP запросы к API
- ✅ Тест переключения напоминаний

**Результат:** 4/4 теста должны пройти

## Что делать при ошибках

### Ошибка: "no such table: notification_settings"
```bash
python3 scripts/testing/database/fix_notification_settings_table.py
```

### Ошибка: "Connection refused"
Backend сервер не запущен. Запустите:
```bash
python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### Ошибка: "500 Internal Server Error"
Запустите диагностику:
```bash
python3 scripts/testing/api/test_notifications_api.py
```

Скрипт покажет точную причину ошибки.

## Примеры использования

### Быстрая диагностика перед запуском
```bash
# Запустить все тесты
python3 scripts/testing/api/test_notifications_api.py
python3 scripts/testing/api/test_reminders_api.py

# Если все ✅ PASS - можно запускать сервер
python3 -m uvicorn main:app --reload
```

### Проверка после изменений в БД
```bash
# После миграций или изменений схемы
python3 scripts/testing/api/test_notifications_api.py
```

### Отладка проблем с сохранением
```bash
# Полный тест сохранения настроек
python3 scripts/testing/api/test_save_notifications.py
```

## Вывод тестов

Все тесты выводят подробную информацию:
- ✅ PASS - тест пройден
- ❌ FAIL - тест провален
- ⚠️  WARNING - предупреждение

Пример успешного вывода:
```
======================================================================
ИТОГИ ТЕСТИРОВАНИЯ
======================================================================
✅ PASS - БД таблицы
✅ PASS - Прямой вызов API функции
✅ PASS - Booking reminders эндпоинт
✅ PASS - HTTP запрос

Пройдено: 4/4
```
