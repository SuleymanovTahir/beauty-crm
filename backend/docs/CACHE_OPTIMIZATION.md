# 🌍 Translation Cache Optimization

## Проблема
Ранее система создавала **отдельный JSON файл для каждого перевода**, что приводило к созданию тысяч маленьких файлов (1056+ файлов).

## Решение
Теперь используется **один консолидированный файл** `translations_cache.json` вместо тысяч маленьких файлов.

### Преимущества:
- ✅ **Быстрее**: Один файл загружается быстрее, чем 1000+ файлов
- ✅ **Меньше места**: Нет overhead от метаданных файловой системы
- ✅ **Проще управлять**: Один файл вместо тысяч
- ✅ **Легче бэкапить**: Просто скопировать один файл

## Миграция (уже выполнена)

```bash
# 1. Мигрировать старые файлы в новый формат
python3 scripts/translations/migrate_cache.py

# 2. Удалить старые файлы
cd scripts/translations/.cache
find . -name "*.json" ! -name "translations_cache.json" -type f -delete
```

## Результат

**До**: 1056 файлов  
**После**: 1 файл (96 KB)

## Автоматическая очистка

Добавлена команда в инструкции деплоя для автоматической очистки `__pycache__` каждое воскресенье в 3:00.

## Использование

Система автоматически:
1. Загружает кэш при инициализации `Translator()`
2. Использует кэш для уже переведенных текстов
3. Сохраняет новые переводы в память
4. Записывает все на диск при вызове `translator.save_cache_to_disk()`

Кэш сохраняется автоматически после выполнения `npm run db:i18n:auto`.
