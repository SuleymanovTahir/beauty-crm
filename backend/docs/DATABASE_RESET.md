# 🔄 Полная Очистка и Пересоздание Базы Данных PostgreSQL

Этот документ содержит инструкции для полной очистки базы данных и её пересоздания с нуля.

---

## ⚠️ ВНИМАНИЕ

**Эта процедура удалит ВСЕ данные из базы данных!**

Используйте только для:
- Тестирования миграций
- Разработки
- Исправления критических проблем со схемой

**НЕ ИСПОЛЬЗУЙТЕ на продакшене без бэкапа!**

---

## 📋 Пошаговая Инструкция

### 1️⃣ Активация Виртуального Окружения

```bash
cd /Users/tahir/Desktop/beauty-crm/backend
source venv/bin/activate
```

Проверка активации:
```bash
which python3
# Должно показать: /Users/tahir/Desktop/beauty-crm/backend/venv/bin/python3
```

---

### 2️⃣ Полное Пересоздание Базы Данных

Этот шаг удалит саму базу данных и создаст её заново пустой.

```bash
# Вариант 1: Использование Python скрипта (рекомендуется)
python3 scripts/maintenance/recreate_database.py
```

```bash
# Вариант 2: Использование системных команд (если скрипт не сработал)
dropdb beauty_crm
createdb beauty_crm
```

**Что делает скрипт:**
- Подключается к системной базе `postgres`
- Отключает всех активных пользователей от `beauty_crm`
- Удаляет базу данных (`DROP DATABASE`)
- Создает новую чистую базу (`CREATE DATABASE`)

**Ожидаемый результат:**
```
✅ База данных 'beauty_crm' удалена
✅ База данных 'beauty_crm' создана
```

---

### 3️⃣ Предоставление Прав Пользователю

После пересоздания базы данных нужно предоставить права пользователю `beauty_crm_user`.

```bash
python3 scripts/maintenance/grant_db_permissions.py
```

**Что делает скрипт:**
- Создает пользователя `beauty_crm_user` (если не существует)
- Предоставляет все права на базу данных
- Предоставляет права на схему `public`
- Предоставляет права на таблицы и последовательности
- Устанавливает права по умолчанию для будущих объектов

**Ожидаемый результат:**
```
✅ Все права успешно предоставлены пользователю 'beauty_crm_user'!
```

---

### 4️⃣ Создание Таблиц (Миграции)

Запуск из Python REPL или скрипта:

```python
# Вариант 1: Из Python консоли
python3
>>> from db.migrations.run_all_migrations import run_all_migrations
>>> run_all_migrations()
```

```bash
# Вариант 2: Напрямую через скрипт
python3 -c "from db.migrations.run_all_migrations import run_all_migrations; run_all_migrations()"
```

**Что происходит:**
- Создаются все таблицы базы данных
- Применяются все миграции схемы
- Заполняются базовые данные (настройки салона, позиции и т.д.)

**Ожидаемый результат:**
```
✅ Все миграции выполнены успешно
```

---

### 5️⃣ Применение Фиксов (Асинхронно)

```python
# Из Python консоли с async context
python3
>>> import asyncio
>>> from main import run_all_fixes
>>> asyncio.run(run_all_fixes())
```

```bash
# Вариант 2: Через temporary скрипт
python3 -c "import asyncio; from main import run_all_fixes; asyncio.run(run_all_fixes())"
```

**Что делает:**
- Исправляет данные после миграций
- Проверяет целостность данных
- Применяет необходимые патчи

**Ожидаемый результат:**
```
✅ Все фиксы применены
```

---

### 6️⃣ Запуск Тестов

```bash
# Запуск всех тестов
python3 -c "from tests.run_all_tests import run_all_tests; run_all_tests()"
```

```bash
# Или через pytest
pytest tests/
```

**Что проверяется:**
- Целостность схемы базы данных
- Корректность миграций
- Работа основных функций
- SQL синтаксис PostgreSQL

**Ожидаемый результат:**
```
✅ Все тесты пройдены
```

---

## 🔍 Проверка Результата

### Проверка Таблиц

```python
python3
>>> from db.connection import get_db_connection
>>> conn = get_db_connection()
>>> c = conn.cursor()
>>> c.execute("SELECT tablename FROM pg_tables WHERE schemaname = 'public' ORDER BY tablename")
>>> tables = c.fetchall()
>>> print(f"Создано таблиц: {len(tables)}")
>>> for t in tables: print(f"  - {t[0]}")
>>> conn.close()
```

### Проверка Данных

```python
>>> from db.settings import get_salon_settings
>>> settings = get_salon_settings()
>>> print(settings)
```

---

## 🚀 Полная Команда (Одна Строка)

Для автоматизации всего процесса:

```bash
cd /Users/tahir/Desktop/beauty-crm/backend && \
source venv/bin/activate && \
python3 scripts/maintenance/recreate_database.py && \
python3 scripts/maintenance/grant_db_permissions.py && \
python3 -c "from db.migrations.run_all_migrations import run_all_migrations; run_all_migrations()" && \
python3 -c "import asyncio; from main import run_all_fixes; asyncio.run(run_all_fixes())" && \
python3 -c "from tests.run_all_tests import run_all_tests; run_all_tests()"
```

---

## 🐛 Типичные Ошибки

### Ошибка: `ModuleNotFoundError: No module named 'psycopg2'`

**Решение:**
```bash
# Убедитесь что виртуальное окружение активировано
source venv/bin/activate

# Установите psycopg2
pip install psycopg2-binary
```

### Ошибка: `connection to server failed`

**Решение:**
```bash
# Проверьте что PostgreSQL запущен
brew services list | grep postgresql

# Запустите PostgreSQL если остановлен
brew services start postgresql@14
```

### Ошибка: `FATAL: database "beauty_crm" does not exist`

**Решение:**
```bash
# Создайте базу данных
createdb beauty_crm
```

---

## 📝 Примечания

1. **Время выполнения:** Весь процесс занимает 1-3 минуты
2. **Бэкапы:** Перед очисткой продакшена сделайте бэкап:
   ```bash
   pg_dump beauty_crm > backup_$(date +%Y%m%d_%H%M%S).sql
   ```
3. **Восстановление:** Для восстановления из бэкапа:
   ```bash
   psql beauty_crm < backup_20241204_123000.sql
   ```

---

## ✅ Чек-лист После Очистки

- [ ] Все таблицы удалены
- [ ] Миграции выполнены успешно
- [ ] Фиксы применены
- [ ] Тесты пройдены
- [ ] Настройки салона загружены
- [ ] API сервер запускается без ошибок
- [ ] Фронтенд может подключиться к бэкенду

---

**Последнее обновление:** 2025-12-04
