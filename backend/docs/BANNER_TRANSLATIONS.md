# 🌍 Banner Translation System

## Обзор

Система автоматического перевода заголовков и подзаголовков баннеров на 9 языков.

## Поддерживаемые языки

- 🇷🇺 Русский (ru) - исходный язык
- 🇬🇧 Английский (en)
- 🇦🇪 Арабский (ar)
- 🇪🇸 Испанский (es)
- 🇩🇪 Немецкий (de)
- 🇫🇷 Французский (fr)
- 🇮🇳 Хинди (hi)
- 🇰🇿 Казахский (kk)
- 🇵🇹 Португальский (pt)

## 🧠 Автоматическое определение языка

Система **автоматически определяет язык** исходного текста.
Вы можете вводить текст на **любом языке** (английском, арабском, испанском и т.д.) в поле `title_ru` или `subtitle_ru`.

Система:

1. Определит язык введенного текста (например, английский)
2. Переведет его на все остальные языки
3. **Переведет его на русский** (если исходный текст был не на русском)
4. Сохранит все переводы в соответствующие колонки

Пример:

- Вводите в `title_ru`: "Luxury Spa Experience"
- Система определяет: English
- Результат в БД:
  - `title_ru`: "Роскошный спа-опыт" (переведено)
  - `title_en`: "Luxury Spa Experience" (оригинал)
  - `title_ar`: "تجربة سبا فاخرة" (переведено)
  - ...и так далее

## Структура базы данных

Таблица `public_banners` содержит следующие поля для переводов:

**Заголовки:**

- `title_ru`, `title_en`, `title_ar`, `title_es`, `title_de`, `title_fr`, `title_hi`, `title_kk`, `title_pt`

**Подзаголовки:**

- `subtitle_ru`, `subtitle_en`, `subtitle_ar`, `subtitle_es`, `subtitle_de`, `subtitle_fr`, `subtitle_hi`, `subtitle_kk`, `subtitle_pt`

## Автоматический перевод

### Полный цикл (рекомендуется)

```bash
# Извлечь, перевести и синхронизировать все переводы
npm run db:i18n:auto
```

### Пошаговый процесс

1. **Извлечение** - найти тексты, требующие перевода:

```bash
python3 scripts/translations/db_extract.py
```

2. **Перевод** - перевести через Google Translate:

```bash
python3 scripts/translations/db_translate.py
```

3. **Синхронизация с JSON** - обновить frontend локали:

```bash
python3 scripts/translations/db_sync.py
```

4. **Синхронизация с БД** - сохранить переводы в базу:

```bash
python3 scripts/translations/db_sync_to_db.py
```

## Frontend интеграция

Компонент `Hero.tsx` автоматически:

1. Загружает баннеры через API `/api/public/banners`
2. Определяет текущий язык пользователя
3. Отображает соответствующий перевод заголовка и подзаголовка

## Добавление нового баннера

1. Создайте баннер в админ-панели с текстом на русском языке
2. Запустите автоматический перевод:

```bash
npm run db:i18n:auto
```

3. Переводы будут созданы автоматически для всех языков

## Кэширование

Все переводы кэшируются в `scripts/translations/.cache/translations_cache.json` для ускорения повторных переводов.

## Проверка переводов

```bash
# Проверить переводы в базе данных (PostgreSQL)
psql $DATABASE_URL -c "SELECT id, title_ru, title_en, title_ar FROM public_banners WHERE is_active = TRUE"
```
