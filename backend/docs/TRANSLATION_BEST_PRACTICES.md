# Translation Best Practices

## 🎯 Принцип: Правильные данные с самого начала

### ❌ Плохо (приводит к ошибкам):
```python
{
    'name': 'Under arms',  # Только английский
    'name_ru': None,       # Нет перевода
    # LibreTranslate переводит → "Под оружием" ❌
}
```

### ✅ Хорошо (правильно с самого начала):
```python
{
    'name': 'Under arms',
    'name_ru': 'Эпиляция подмышек',  # Правильный перевод сразу
    'name_ar': 'الإبطين',
}
```

---

## 📋 Правила для seed_test_data.py

### 1. Всегда указывать русский перевод
```python
# ❌ НЕ ТАК:
'name': 'Head massage',
'name_ru': None,

# ✅ ТАК:
'name': 'Head massage',
'name_ru': 'Массаж головы',
```

### 2. Использовать правильную терминологию
```python
# ❌ НЕ ТАК:
'name_ru': 'Стоун-массаж',  # Калька с английского

# ✅ ТАК:
'name_ru': 'Массаж горячими камнями',  # Понятно клиентам
```

### 3. Проверять контекст
```python
# ❌ НЕ ТАК:
'name': 'Brazilian',  # Без контекста
'name_ru': 'Бразильский',  # Что бразильское?

# ✅ ТАК:
'name': 'Brazilian Bikini',
'name_ru': 'Бразильская эпиляция',  # Понятно
```

---

## 🔧 Автоматическая проверка

### Добавить валидацию в seed_production_data.py:

```python
def validate_service_data(service):
    """Validate service has proper translations"""
    warnings = []
    
    # Check Russian translation exists
    if not service.get('name_ru'):
        warnings.append(f"⚠️  {service['name']}: missing name_ru")
    
    # Check for common mistranslations
    bad_patterns = ['под оружием', 'руководитель', 'бразильский']
    if service.get('name_ru'):
        for pattern in bad_patterns:
            if pattern in service['name_ru'].lower():
                warnings.append(f"⚠️  {service['name']}: suspicious translation '{service['name_ru']}'")
    
    return warnings

# Use in seed_production_data():
for s in SERVICES_DATA:
    warnings = validate_service_data(s)
    for w in warnings:
        print(w)
```

---

## 📝 Workflow для новых услуг

1. **Добавляем в seed_test_data.py**:
   ```python
   {
       'key': 'new_service',
       'name': 'New Service',
       'name_ru': 'Новая услуга',  # ← Обязательно!
       'name_ar': 'خدمة جديدة',
       # ...
   }
   ```

2. **Проверяем перевод**:
   - Спросить носителя языка
   - Проверить на понятность клиентам
   - Избегать калек с английского

3. **Запускаем миграции**:
   ```bash
   python db/migrations/run_all_migrations.py
   ```

4. **Автоперевод остальных языков**:
   ```bash
   npm run db:i18n:auto
   ```

---

## 🎯 Итог

**Защита от ошибок:**
1. ✅ Правильные переводы в исходных данных
2. ✅ Валидация при импорте
3. ✅ Универсальная система очистки (без хардкода)
4. ✅ Автоматический перевод только для недостающих языков

**Не полагаться на автоперевод для основных языков (ru, en, ar)!**
