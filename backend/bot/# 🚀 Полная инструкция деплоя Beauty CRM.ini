# 🚀 Полная инструкция деплоя Beauty CRM
---

## ⚠️ ВАЖНО: Последовательность запуска

**КРИТИЧЕСКИ ВАЖНО соблюдать порядок:**

### 1️⃣ Сначала создать базу данных PostgreSQL

База данных должна быть создана **ДО** запуска приложения!

```bash
# Установить PostgreSQL (если еще не установлен)
# Mac: Скачать Postgres.app с https://postgresapp.com/downloads.html
# Linux: sudo apt install postgresql postgresql-contrib

# Mac: После установки Postgres.app добавить в PATH
echo 'export PATH="/Applications/Postgres.app/Contents/Versions/latest/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# Проверить установку
which psql
# Должно показать: /Applications/Postgres.app/Contents/Versions/latest/bin/psql

# Создать базу данных и пользователя
createdb beauty_crm
createuser beauty_crm_user
psql -c "ALTER USER beauty_crm_user WITH PASSWORD 'local_password';"
psql -c "GRANT ALL PRIVILEGES ON DATABASE beauty_crm TO beauty_crm_user;"
```

### 2️⃣ Настроить .env.local

```bash
cat > .env.local << 'EOF'
DATABASE_TYPE=postgresql
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=beauty_crm
POSTGRES_USER=beauty_crm_user
POSTGRES_PASSWORD=local_password
ENVIRONMENT=development
EOF
```

### 3️⃣ Инициализировать схему базы данных

```bash
cd /Users/tahir/Desktop/beauty-crm/backend
source venv/bin/activate
python -c "from db.init import init_database; init_database()"
```

### 4️⃣ Только после этого запускать приложение

```bash
python3 -m uvicorn main:app --reload
```

---

## 🔄 Временное решение: Вернуться к SQLite

Если PostgreSQL еще не установлен, можно временно использовать SQLite:

```bash
cat > .env.local << 'EOF'
DATABASE_TYPE=postgresql
ENVIRONMENT=development
EOF
```

Затем перезапустите сервер.

---

## 📚 Подробная документация

Полная инструкция по миграции на PostgreSQL: `docs/POSTGRES_MIGRATION.md`

---
 на Ubuntu Server

## 📋 Оглавление

### 🔧 Устранение проблем
- [Проблема: npm не устанавливается](#-проблема-npm-не-устанавливается)
- [Обновление Node.js на продакшен-сервере](#-обновление-nodejs-на-продакшен-сервере)

### 📦 Основной деплой
1. [Подготовка сервера](#1-подготовка-сервера)
2. [Загрузка проекта](#2-загрузка-проекта)
3. [Настроить Redis](#3-настроить-redis)
4. [Настройка Backend (FastAPI)](#4-настройка-backend-fastapi)
5. [Настройка Frontend (React/Vite)](#5-настройка-frontend-reactvite)
6. [Настройка Nginx](#6-настройка-nginx)
7. [SSL сертификаты](#7-ssl-сертификаты)
8. [Systemd сервис (автозапуск)](#8-systemd-сервис-автозапуск)

### 🛠️ Рабочие процессы
- [Полезные команды](#9-полезные-команды)
- [Быстрая проверка после деплоя](#-быстрая-проверка-после-деплоя)
- [Workflow обновления проекта](#-workflow-обновления-проекта)
- [Локальная разработка](#-локальная-разработка)
- [Работа с переводами (i18n)](#-работа-с-переводами-i18n)
- [Команды для очистки](#️-команды-для-очистки)
- [Автоматическая очистка (cron)](#-автоматическая-очистка-cron)

### 📞 Справочная информация
- [Данные доступа](#-данные-доступа)
- [Полезные ссылки](#-полезные-ссылки)

---

# 🔧 Устранение проблем

## 📌 Проблема: npm не устанавливается

(Пошаговое решение: лимиты, swap, перезагрузка)

Если на сервере **npm install** не запускается, зависает, вылетает с ошибками типа
`ENOMEM`, `EAGAIN`, `too many open files`, `Killed`,
то проблема почти всегда в двух вещах:

### ❗ 1. Маленький лимит открытых файлов (ulimit)

### ❗ 2. Отсутствие swap (у тебя было: `Swp: 0K/0K`)

Ниже — полная инструкция с командами, которые мы выполняли.

---

### 🔥 Шаг 1 — Проверить текущий лимит файлов

```bash
ulimit -n
```

У тебя было:

```
1024
```

Это слишком мало — npm нормально работает от **65535**.

---

### 🔥 Шаг 2 — Увеличить лимит файлов (NOFILE)

#### 2.1 Открыть файл limits.conf

```bash
sudo nano /etc/security/limits.conf
```

В конец файла дописать:

```
* soft nofile 65535
* hard nofile 65535
```

Сохранить: **Ctrl+O → Enter → Ctrl+X**.

---

#### 2.2 Прописать лимиты для systemd

```bash
echo 'DefaultLimitNOFILE=65535' | sudo tee -a /etc/systemd/system.conf
echo 'DefaultLimitNOFILE=65535' | sudo tee -a /etc/systemd/user.conf
```

У тебя вывод был:

```
DefaultLimitNOFILE=65535
DefaultLimitNOFILE=65535
```

Это правильно.

---

#### ❗ Почему после этого `ulimit -n` всё равно показывает 1024?

Потому что:

* изменения в limits.conf **не работают до перезагрузки**
* systemd-конфиги **тоже требуют перезагрузки**
* текущая SSH-сессия запущена со старым лимитом

Это нормально.

---

### 🔥 Шаг 3 — Проблема №2: у тебя **0 swap**

Ты получил:

```
Swp: 0K/0K
```

И терминал даже выдал:

```
Swp:: command not found
```

Swap нужен обязательно, иначе **Node, npm, Python, Vite, React** могут «убиваться» системой из-за нехватки памяти.

---

### 🔥 Шаг 4 — Создаём swap-файл 2 GB

```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

Проверить:

```bash
free -h
```

Должно появиться:

```
Swap: 2.0G   0B   2.0G
```

Чтобы swap автоматически включался после перезагрузки:

```bash
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

---

### 🔥 Шаг 5 — Перезагрузить сервер (обязательно)

```bash
sudo reboot
```

У тебя вывод был:

```
The system will reboot now!
Read from remote host: Connection reset by peer
```

Это правильно — сервер ушёл в перезагрузку.

---

### 🔥 Шаг 6 — После перезагрузки проверить

#### 6.1 Подключаемся

```bash
ssh ubuntu@91.201.215.32
```

#### 6.2 Проверяем лимит файлов

```bash
ulimit -n
```

Ожидаем:

```
65535
```

#### 6.3 Проверяем swap

```bash
free -h
```

Должно быть:

* swap > 0
* не 0K/0K как раньше

---

### 🎉 После этих шагов npm install начнёт работать стабильно

Когда сделаешь — напиши мне:

* вывод `ulimit -n`
* вывод `free -h`

Если что-то не применилось — я сразу поправлю.

---

## � Обновление Node.js на продакшен-сервере

⚠️ **Проблема**: Vite требует Node.js версии 20.19+ или 22.12+, а на сервере может быть установлена старая версия (например, 18.19.1)

### Проверка текущей версии Node.js
```bash
node -v
```

### Если версия меньше 20.19, выполните обновление:

1. Установить/обновить nvm (Node Version Manager)
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash
```

2. Загрузить nvm в текущую сессию
```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
```

3. Проверить версию nvm
```bash
nvm --version
```

4. Установить Node.js версии 22
```bash
nvm install 22
```

5. Использовать версию 22
```bash
nvm use 22
```

6. Установить версию 22 по умолчанию
```bash
nvm alias default 22
```

7. Проверить версию Node.js (должно показать v22.x.x)
```bash
node -v
```

8. Проверить версию npm
```bash
npm -v
```

### Пересборка frontend после обновления Node.js

9. Перейти в директорию frontend
```bash
cd /home/ubuntu/beauty_crm/frontend
```

10. Очистить старые зависимости
```bash
rm -rf node_modules package-lock.json
```

11. Переустановить зависимости
```bash
npm install
```

12. Собрать проект
```bash
npm run build
```

13. Настроить права доступа
```bash
sudo chown -R www-data:www-data dist/
sudo chmod -R 755 dist/
```

14. Перезапустить сервисы
```bash
sudo systemctl restart beauty_crm
sudo systemctl restart nginx
```

15. Проверить что сайт работает
```bash
curl -I https://mlediamant.com
```

✅ **Готово!** Теперь Node.js обновлён и Vite будет работать корректно.

---

## 📌 Проблема: Ошибка прав PostgreSQL (Permission denied)

Если при запуске миграций возникает ошибка `permission denied for schema public`, выполните следующее:

### 🛠 Способ 1: Автоматическое исправление (Рекомендуется)

```bash
cd ~/beauty_crm/backend
bash scripts/maintenance/setup_postgres_user.sh
```

### 🛠 Способ 2: Ручное исправление

Зайдите в консоль `psql` от имени суперпользователя `postgres`:

```bash
sudo -u postgres psql
```

Внутри консоли выполните эти SQL команды (по одной):

```sql
-- 1. Установите пароль (такой же как в .env.production)
ALTER USER beauty_crm_user WITH PASSWORD 'local_password';

-- 2. Сделайте пользователя суперюзером
ALTER USER beauty_crm_user WITH SUPERUSER;

-- 3. Подключитесь к базе
\c beauty_crm

-- 4. Выдайте права на схему public
GRANT ALL ON SCHEMA public TO beauty_crm_user;
ALTER SCHEMA public OWNER TO beauty_crm_user;

-- 5. Выдайте права на все таблицы
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO beauty_crm_user;
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO beauty_crm_user;

-- 6. Выход
\q
```

После этого запустите миграции:
```bash
python3 db/migrations/run_all_migrations.py
```

---

## 📌 Проблема: Ошибка Permission denied при загрузке файлов (SFTP/FileZilla)

Если при попытке закачать файлы через SFTP вы видите ошибку `open for write: permission denied`:

### 🛠 Решение

Зайдите на сервер и выполните команду, которая вернет права вашему пользователю:

```bash
sudo chown -R ubuntu:ubuntu /home/ubuntu/beauty_crm
sudo chmod -R 755 /home/ubuntu/beauty_crm
```

*После этого повторите загрузку в FileZilla/SFTP.*

---

# 📦 Основной деплой

## 1. Подготовка сервера

Подключение к серверу
```bash
ssh ubuntu@91.201.215.32
```

Обновление списка пакетов
```bash
sudo apt update
```

Установка необходимых пакетов
```bash
sudo apt install python3 python3-pip python3-venv git nginx certbot python3-certbot-nginx net-tools curl redis-server -y
```

Создание директории проекта
```bash
mkdir -p /home/ubuntu/beauty_crm
```

Настройка прав доступа
```bash
sudo chown -R ubuntu:ubuntu /home/ubuntu/beauty_crm
```

Переход в директорию проекта
```bash
cd /home/ubuntu/beauty_crm
```

---

## 2. Загрузка проекта

### С локального компьютера (Mac/Linux)

Загрузка через rsync без venv и node_modules

1. Создай временную папку без venv на локальной машине (Mac):
```bash
rsync -av --exclude='venv' --exclude='.git' --exclude='node_modules' "/Users/tahir/Desktop/beauty-crm/" "/Users/tahir/Desktop/beauty-crm/temp_beauty_crm/"
```

2. Залей временную папку на сервер:
```bash
scp -r "/Users/tahir/Desktop/beauty-crm/temp_beauty_crm" ubuntu@91.201.215.32:/home/ubuntu/beauty_crm
```

3. Удаление временной папки на компьютере после загрузки на сервер:
```bash
rm -rf "/Users/tahir/Desktop/beauty-crm/temp_beauty_crm"
```

### Альтернатива: через scp

Установка OpenSSH на Ubuntu (если нужно)
```bash
sudo apt install openssh-client
```

Загрузка проекта через scp
```bash
scp -r ~/Desktop/beauty-crm ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/
```

### Если ошибка SSH ключей

Удаление старых SSH ключей
```bash
ssh-keygen -R 91.201.215.32
```

---

## 3. Настроить Redis
```bash
# Включить автозапуск
sudo systemctl enable redis-server
# Запустить сервис
sudo systemctl start redis-server
# Проверить статус
sudo systemctl status redis-server
```

---

## 4. Настройка Backend (FastAPI)

Переход в директорию backend
```bash
cd /home/ubuntu/beauty_crm/backend
```

Создание виртуального окружения
```bash
python3 -m venv venv
```

Активация виртуального окружения
```bash
source venv/bin/activate
```

Обновление pip
```bash
pip install --upgrade pip
```

Установка зависимостей проекта
```bash
pip install -r requirements.txt
```

Установка Gunicorn и Uvicorn
```bash
pip install gunicorn uvicorn
```

Проверка установки Gunicorn
```bash
ls venv/bin | grep gunicorn
```

Тестовый запуск сервера (Ctrl+C для остановки)
```bash
venv/bin/gunicorn main:app -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000
```

---

## 5. Настройка Frontend (React/Vite)

Установка nvm (Node Version Manager)
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash
```

Экспорт NVM в текущую сессию
```bash
export NVM_DIR="$HOME/.nvm"
```

Загрузка nvm скрипта
```bash
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
```

Проверка версии nvm
```bash
nvm --version
```

Просмотр доступных LTS версий Node.js
```bash
nvm ls-remote --lts | tail -5
```

⚠️ ВАЖНО: Vite требует Node.js версии 20.19+ или 22.12+
Установка Node.js версии 22 (LTS)
```bash
nvm install 22
```

Использование установленной версии
```bash
nvm use 22
```

Установка версии по умолчанию
```bash
nvm alias default 22
```

Проверка версии Node.js
```bash
node -v
```

Проверка версии npm
```bash
npm -v
```

Переход в директорию frontend
```bash
cd /home/ubuntu/beauty_crm/frontend
```

Удаление старых node_modules (если есть)
```bash
rm -rf node_modules package-lock.json
```

Установка зависимостей frontend
```bash
npm install
```

Сборка frontend для продакшена
```bash
npm run build
```

Проверка создания dist папки
```bash
ls -la dist/
```

Настройка прав доступа для dist (чтобы Nginx мог читать, а ubuntu мог обновлять)
```bash
# 1. Сделать ubuntu владельцем всей папки проекта
sudo chown -R ubuntu:ubuntu /home/ubuntu/beauty_crm

# 2. Выдать правильные права на чтение для Nginx
sudo chmod -R 755 /home/ubuntu/beauty_crm
```

---

## 6. Настройка Nginx

Создание конфигурации Nginx
```bash
sudo nano /etc/nginx/sites-available/beauty_crm.conf
```

**Скопируй и вставь в nano:**
```nginx
# Редирект с HTTP на HTTPS
server {
    listen 80;
    server_name mlediamant.com www.mlediamant.com;
    return 301 https://$host$request_uri;
}

# Основной HTTPS сервер
server {
    listen 443 ssl http2;
    server_name mlediamant.com www.mlediamant.com;

    # SSL сертификаты
    ssl_certificate /etc/letsencrypt/live/mlediamant.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mlediamant.com/privkey.pem;
    
    # SSL настройки (современные профили)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;

    # 🚀 ОПТИМИЗАЦИЯ СКОРОСТИ: Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # Максимальный размер загружаемого файла (фото/видео)
    client_max_body_size 50M;

    # API эндпоинты (Backend)
    location /api/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 🚀 WEB SOCKETS (Notifications, Chat)
    location /api/ws/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    location /api/webrtc/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    # Публичные эндпоинты
    location /public/ {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Instagram webhooks
    location /webhook {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }

    # Прокси для изображений Instagram
    location /proxy/ {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 🚀 ОПТИМИЗАЦИЯ: Статика бэкенда (загрузки) - короткий кэш для обновления фото
    location /static/ {
        alias /home/ubuntu/beauty_crm/backend/static/;
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
        access_log off;
    }

    # 🚀 ОПТИМИЗАЦИЯ: Кэширование хешированных файлов Vite (js, css, images) - на месяц
    location ~* ^/(js|css|images|fonts)/.+\.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?|ttf|eot)$ {
        root /home/ubuntu/beauty_crm/frontend/dist;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # 🚀 ОПТИМИЗАЦИЯ: Остальная статика фронтенда (без хешей)
    location ~* \.(ico|png|svg|txt)$ {
        root /home/ubuntu/beauty_crm/frontend/dist;
        expires 1d;
        add_header Cache-Control "public, must-revalidate";
        access_log off;
    }

    # 🚀 ОПТИМИЗАЦИЯ: React SPA (индекс НИКОГДА не кэшируем для мгновенных обновлений)
    location / {
        root /home/ubuntu/beauty_crm/frontend/dist;
        try_files $uri /index.html;
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
    }
}
```

Удаление дефолтной конфигурации Nginx
```bash
sudo rm /etc/nginx/sites-enabled/default
```

Создание симлинка для активации конфигурации
```bash
sudo ln -s /etc/nginx/sites-available/mlediamant /etc/nginx/sites-enabled/
```

Проверка конфигурации Nginx
```bash
sudo nginx -t
```

Исправление прав доступа
```bash
sudo chown -R ubuntu:www-data /home/ubuntu/beauty_crm/frontend/dist
sudo chmod -R 755 /home/ubuntu/beauty_crm/frontend/dist
```

Чтобы быть уверенным, что Nginx может читать и родительские каталоги:
```bash
sudo chmod 755 /home/ubuntu
sudo chmod 755 /home/ubuntu/beauty_crm
sudo chmod 755 /home/ubuntu/beauty_crm/frontend
```

Перезапуск Nginx
```bash
sudo systemctl restart nginx
```

Проверка статуса Nginx
```bash
sudo systemctl status nginx
```

### Настройка файрвола

Проверка статуса файрвола
```bash
sudo ufw status
```

Разрешить SSH порт 22
```bash
sudo ufw allow 22
```

Разрешить HTTP порт 80
```bash
sudo ufw allow 80
```

Разрешить HTTPS порт 443
```bash
sudo ufw allow 443
```

Включить файрвол
```bash
sudo ufw enable
```

Проверка что порт 8000 слушается
```bash
sudo ss -tuln | grep 8000
```

---

## 7. SSL сертификаты

Установка Certbot для Nginx
```bash
sudo apt install certbot python3-certbot-nginx -y
```

Получение SSL сертификата (введи email: mladiamontuae@gmail.com)
```bash
sudo certbot --nginx -d mlediamant.com -d www.mlediamant.com
```

Проверка конфигурации Nginx после SSL
```bash
sudo nginx -t
```

Перезагрузка Nginx
```bash
sudo systemctl reload nginx
```

Проверка HTTPS работает
```bash
curl -I https://mlediamant.com
```

Проверка таймера автообновления сертификата
```bash
sudo systemctl status certbot.timer
```

Тест обновления сертификата (не меняет сертификаты)
```bash
sudo certbot renew --dry-run
```

---

## 8. Systemd сервис (автозапуск)

Создание файла сервиса
```bash
sudo nano /etc/systemd/system/beauty_crm.service
```

**Скопируй и вставь в nano:**
```ini
[Unit]
Description=Beauty CRM FastAPI with Gunicorn
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/beauty_crm/backend
Environment="PATH=/home/ubuntu/beauty_crm/backend/venv/bin"
# 🚀 ОПТИМИЗАЦИЯ: -w 2 (для серверов с 1 CPU / 1GB RAM)
ExecStart=/home/ubuntu/beauty_crm/backend/venv/bin/gunicorn \
          -w 2 \
          -k uvicorn.workers.UvicornWorker \
          main:app \
          --bind 0.0.0.0:8000 \
          --access-logfile /home/ubuntu/beauty_crm/backend/access.log \
          --error-logfile /home/ubuntu/beauty_crm/backend/error.log
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Перезагрузка конфигурации systemd
```bash
sudo systemctl daemon-reload
```

Включение автозапуска сервиса
```bash
sudo systemctl enable beauty_crm
```

Запуск сервиса
```bash
sudo systemctl start beauty_crm
```

Проверка статуса сервиса
```bash
sudo systemctl status beauty_crm
```

Просмотр логов сервиса
```bash
sudo journalctl -u beauty_crm -f
```

Просмотр access логов
```bash
tail -f /home/ubuntu/beauty_crm/backend/access.log
```

Просмотр error логов
```bash
tail -f /home/ubuntu/beauty_crm/backend/error.log
```

Показывает подробную ошибку
```bash
cat /home/ubuntu/beauty_crm/backend/error.log
```

### Если удаляю все содержимое папки из backend и frontend

Создай папку и нужные файлы:
```bash
mkdir -p logs
touch logs/error.log
touch logs/access.log
chown -R ubuntu:ubuntu logs
```

Потом перезапусти сервис:
```bash
sudo systemctl daemon-reload
sudo systemctl restart beauty_crm
sudo systemctl restart nginx
sudo journalctl -u beauty_crm -f
```

---

# 🛠️ Рабочие процессы

## 9. Полезные команды

### Backend

Перезапуск сервиса
```bash
sudo systemctl restart beauty_crm
```

Остановка сервиса
```bash
sudo systemctl stop beauty_crm
```

Статус сервиса
```bash
sudo systemctl status beauty_crm
```

Логи сервиса последние 50 строк
```bash
sudo journalctl -u beauty_crm -f --lines 50
```

### Frontend

Переход в директорию frontend
```bash
cd /home/ubuntu/beauty_crm/frontend
```

Пересборка frontend
```bash
npm run build
```

Перезапуск после пересборки
```bash
sudo systemctl restart beauty_crm
```

### Nginx

Проверка конфигурации Nginx
```bash
sudo nginx -t
```

Перезапуск Nginx
```bash
sudo systemctl restart nginx
```

Перезагрузка Nginx (мягкая)
```bash
sudo systemctl reload nginx
```

Логи ошибок Nginx
```bash
sudo tail -f /var/log/nginx/error.log
```

Логи доступа Nginx
```bash
sudo tail -f /var/log/nginx/access.log
```

### Процессы

Найти процесс Gunicorn
```bash
ps aux | grep gunicorn
```

Найти процесс Uvicorn
```bash
ps aux | grep uvicorn
```

Проверка порта 8000
```bash
sudo ss -tuln | grep 8000
```

### Screen сессии

Создать новую screen сессию
```bash
screen -S beauty_crm
```

Список всех screen сессий
```bash
screen -ls
```

Вернуться в screen сессию
```bash
screen -r beauty_crm
```

### Удаление/очистка

Удалить папку проекта
```bash
rm -rf /home/ubuntu/beauty_crm
```

Удалить виртуальное окружение
```bash
rm -rf venv
```

Очистка node_modules
```bash
rm -rf node_modules package-lock.json
```

Остановка сервиса перед удалением
```bash
sudo systemctl stop beauty_crm
```

### Исправление прав доступа

#### Проблема: Permission denied при загрузке файлов через FTP/SFTP

Если при загрузке файлов через FTP/SFTP клиент (FileZilla, Cyberduck и т.д.) выдает ошибки:
- `permission denied`
- `no such file or directory`
- `mkdir: permission denied`

**Решение на сервере:**

Вариант 1: Создать/пересоздать папку dist с правами ubuntu (рекомендуется)
```bash
# Удалить старую папку и создать новую с правильными правами
sudo rm -rf /home/ubuntu/beauty_crm/frontend/dist
sudo mkdir -p /home/ubuntu/beauty_crm/frontend/dist
sudo chown -R ubuntu:ubuntu /home/ubuntu/beauty_crm/frontend/dist
sudo chmod -R 755 /home/ubuntu/beauty_crm/frontend/dist
```

Вариант 2: Если папка уже существует, просто дать права ubuntu
```bash
sudo chown -R ubuntu:ubuntu /home/ubuntu/beauty_crm/frontend/dist
sudo chmod -R 755 /home/ubuntu/beauty_crm/frontend/dist
```

Вариант 2: Если не поможет, дать полные права (временно для загрузки)
```bash
sudo chmod -R 777 /home/ubuntu/beauty_crm/frontend/dist
```

После загрузки файлов верните правильные права:
```bash
sudo chown -R www-data:www-data /home/ubuntu/beauty_crm/frontend/dist
sudo chmod -R 755 /home/ubuntu/beauty_crm/frontend/dist
sudo systemctl restart nginx
```

#### Альтернативный способ: rsync (рекомендуется)

Вместо FTP используйте rsync - это надежнее и быстрее:

**Вариант A: С локального компьютера (Mac):**
```bash
# 1. Соберите проект локально
cd ~/Desktop/beauty-crm/frontend
npm run build

# 2. Загрузите dist на сервер через rsync
rsync -avz --delete dist/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/frontend/dist/

# 3. Исправьте права на сервере одной командой
ssh ubuntu@91.201.215.32 "sudo chown -R www-data:www-data /home/ubuntu/beauty_crm/frontend/dist && sudo chmod -R 755 /home/ubuntu/beauty_crm/frontend/dist && sudo systemctl restart nginx"
```

**Вариант B: С сервера на сервер (если уже подключены по SSH):**
```bash
# 1. Перейти в директорию frontend
cd /home/ubuntu/beauty_crm/frontend

# 2. Синхронизировать файлы (если dist уже собран)
rsync -avz --delete dist/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/frontend/dist/

# 3. После синхронизации ОБЯЗАТЕЛЬНО исправить права:
sudo chown -R www-data:www-data /home/ubuntu/beauty_crm/frontend/dist
sudo chmod -R 755 /home/ubuntu/beauty_crm/frontend/dist

# 4. Перезапустить Nginx
sudo systemctl restart nginx

# 5. Проверить что сайт работает
curl -I https://mlediamant.com
```

Преимущества rsync:
- ✅ Автоматически создаст все нужные папки
- ✅ Удалит старые файлы (`--delete`)
- ✅ Сохранит структуру проекта
- ✅ Работает быстрее FTP
- ✅ Не требует исправления прав вручную

Отключение автозапуска сервиса
```bash
sudo systemctl disable beauty_crm
```

Удаление файла сервиса
```bash
sudo rm /etc/systemd/system/beauty_crm.service
```

Перезагрузка systemd после удаления
```bash
sudo systemctl daemon-reload
```

Полная очистка проекта на сервере
```bash
cd /home/ubuntu/beauty_crm/
rm -rf ./.*
rm -rf ./*
mkdir backend
mkdir frontend
```



## 📦 Быстрый деплой проекта

### Данные доступа к серверу

- **IP сервер**: 91.201.215.32
- **Пользователь**: ubuntu
- **Пароль SSH**: McDSAo1NUyCLI/SlaeiZ7dI=
- **Путь на сервере**: /home/ubuntu/beauty_crm/

### 1. Полная синхронизация проекта (первый раз)

Копирует весь проект, исключая venv, node_modules и другие временные файлы:
# С твоего Mac
```bash
rsync -avz --progress \
  --exclude 'venv' \
  --exclude 'node_modules' \
  --exclude '.git' \
  --exclude '__pycache__' \
  --exclude '*.pyc' \
  --exclude '.DS_Store' \
  --exclude 'dist' \
  --exclude '.vite' \
  --exclude 'backend/static/uploads' \
  ~/Desktop/beauty-crm/ \
  ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/
```

### 2. Копирование только папки dist (после сборки frontend)

Сначала соберите frontend локально:
```bash
cd ~/Desktop/beauty-crm/frontend
npm run build
```

Затем скопируйте на сервер:
```bash
rsync -avz --progress --delete \
  ~/Desktop/beauty-crm/frontend/dist/ \
  ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/frontend/dist/
```

### 3. Универсальный шаблон для копирования любой папки

```bash
rsync -avz --progress --delete \
  ~/Desktop/beauty-crm/ПУТЬ_К_ПАПКЕ/ \
  ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/ПУТЬ_К_ПАПКЕ/
```

### 4. Примеры копирования отдельных папок

**Backend API:**
```bash
rsync -avz --progress --delete \
  ~/Desktop/beauty-crm/backend/api/ \
  ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/backend/api/
```

**Backend DB:**
```bash
rsync -avz --progress --delete \
  ~/Desktop/beauty-crm/backend/db/ \
  ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/backend/db/
```

**Frontend src:**
```bash
rsync -avz --progress --delete \
  ~/Desktop/beauty-crm/frontend/src/ \
  ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/frontend/src/
```

**Один файл:**
```bash
scp ~/Desktop/beauty-crm/backend/main.py \
  ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/backend/main.py
```

### 5. После копирования на сервере

Подключитесь к серверу:
```bash
ssh ubuntu@91.201.215.32
# Пароль: McDSAo1NUyCLI/SlaeiZ7dI=
```

Установите зависимости и перезапустите:
```bash
# Backend
cd /home/ubuntu/beauty_crm/backend
source venv/bin/activate
pip install -r requirements.txt

# Frontend (если нужно собрать на сервере)
cd /home/ubuntu/beauty_crm/frontend
npm install
npm run build

# Перезапуск сервисов
sudo systemctl restart beauty_crm
sudo systemctl restart nginx
```

### 6. Быстрая шпаргалка команд

| Что копировать | Команда |
|----------------|---------|
| **Весь проект** | `rsync -avz --progress --exclude 'venv' --exclude 'node_modules' --exclude '.git' ~/Desktop/beauty-crm/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/` |
| **Только dist** | `rsync -avz --progress --delete ~/Desktop/beauty-crm/frontend/dist/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/frontend/dist/` |
| **Только backend** | `rsync -avz --progress --exclude 'venv' --exclude '__pycache__' --delete ~/Desktop/beauty-crm/backend/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/backend/` |
| **Только frontend src** | `rsync -avz --progress --exclude 'node_modules' --delete ~/Desktop/beauty-crm/frontend/src/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/frontend/src/` |
| **Один файл** | `scp ~/Desktop/beauty-crm/ПУТЬ/файл.py ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/ПУТЬ/файл.py` |

**Важно:**
- Слеш `/` в конце пути важен! `папка/` копирует содержимое, `папка` копирует саму папку
- `--delete` удаляет лишние файлы на сервере (полная синхронизация)
- Система попросит пароль: `McDSAo1NUyCLI/SlaeiZ7dI=`


---

## 🎯 Быстрая проверка после деплоя

Проверка Backend API
```bash
curl http://localhost:8000/health
```

Проверка что Frontend собран
```bash
ls -la /home/ubuntu/beauty_crm/frontend/dist/index.html
```

Проверка Nginx работает
```bash
sudo systemctl status nginx
```

Проверка сервис запущен
```bash
sudo systemctl status beauty_crm
```

Проверка порт слушается
```bash
sudo ss -tuln | grep 8000
```

Проверка SSL работает
```bash
curl -I https://mlediamant.com
```

---

## 🔥 Workflow обновления проекта

### Локально собрать frontend

Переход в директорию frontend локально
```bash
cd ~/Desktop/beauty-crm/frontend
```

Сборка frontend локально
```bash
npm run build
```

Загрузка dist на сервер
```bash
rsync -avz --delete dist/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/frontend/dist/
```

Загрузка backend кода на сервер
```bash
rsync -av --exclude='venv' --exclude='node_modules' --exclude='.git' ~/Desktop/beauty-crm/backend/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/backend/
```

### На сервере

Подключение к серверу
```bash
ssh ubuntu@91.201.215.32
```

Переход в backend
```bash
cd /home/ubuntu/beauty_crm/backend
```

Активация venv
```bash
source venv/bin/activate
```

Обновление зависимостей (если изменились)
```bash
pip install -r requirements.txt
```

Перезапуск сервиса
```bash
sudo systemctl restart beauty_crm
```

---

## � Локальная разработка

### Backend (локально)

```bash
cd backend 
rm -rf venv
which python3
which python
unalias python 
unalias pip  
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 -m pip install --upgrade pip
python3 -m uvicorn main:app --reloadп
```

Или короткая версия:
```bash
cd backend 
source venv/bin/activate
python3 -m uvicorn main:app --reload
```
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 -m pip install --upgrade pip

### Frontend (локально)

Полная переустановка:
```bash
cd ..
cd frontend 
rm -rf node_modules package-lock.json dist
npm cache clean --force
echo "registry=https://registry.npmjs.org/
audit=false
fund=false
loglevel=warn" > .npmrc
npm install --no-audit --prefer-offline --verbose
npm run build 
npm run dev
```

Или короткая версия:
```bash
cd frontend
npm run dev
```
cd ..
cd frontend
npm cache clean --force 
npm install --no-audit --prefer-offline --verbose
### Очистка кэша npm

```bash
npm cache clean --force
npm install --no-audit --prefer-offline --verbose
npm run build -- --verbose=false
```

### Проверка версии Node

```bash
node -v
```

Если покажет v24.x.x — это системная.

Принудительно активировать Node 22 глобально:
```bash
npm cache clean --force
nvm alias default 22
nvm use default
```

---

## 🐘 Миграция на PostgreSQL (Решение проблем с блокировками)

Если в логах появляются ошибки `database is locked`, необходимо перейти с SQLite на PostgreSQL.

### Краткая инструкция:

1. **Установка PostgreSQL на сервере:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib -y
```

2. **Создание базы и пользователя:**
```bash
sudo -u postgres psql
# В консоли psql:
CREATE DATABASE beauty_crm;
CREATE USER beauty_crm_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE beauty_crm TO beauty_crm_user;
\q
```

3. **Настройка .env файла:**
```bash
DATABASE_TYPE=postgresql
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=beauty_crm
POSTGRES_USER=beauty_crm_user
POSTGRES_PASSWORD=ваш_пароль
```

4. **Миграция данных:**
```bash
cd /home/ubuntu/beauty_crm/backend
source venv/bin/activate
pip install -r requirements.txt
python scripts/migrate_to_postgres.py
```

👉 **Полная пошаговая инструкция:** [docs/POSTGRES_MIGRATION.md](file:///Users/tahir/Desktop/beauty-crm/backend/docs/POSTGRES_MIGRATION.md)

---

## 🌐 Работа с переводами (i18n)

### Установка зависимостей для переводов

```bash
npm install react-i18next i18next i18next-browser-languagedetector
npm install -D i18next-parser
npm install i18next-parser --save-dev
```

Если выдает `npm WARN deprecated ...` — просто уведомление о старых зависимостях.
1 moderate severity vulnerability — несерьёзная уязвимость, но можно почистить:

```bash
npm audit fix
```

### Команды для работы с переводами

Создать папки
```bash
npm run i18n:create
```

Для парсера и создания файлов с переводом
```bash
npm run i18n:extract
```

Полный цикл синхронизации:
```bash
# 1. Синхронизируем структуру (копирует ru/* во все языки)
npm run i18n:sync

# 2. Сканируем код и добавляем новые ключи
npm run i18n:extract

# 3. Снова синхронизируем если появились новые файлы
npm run i18n:sync
```

Автоматический перевод из базы данных:
```bash
npm run db:i18n:auto
```

Проверка переводов:
```bash
npm run i18n:check
```

Полный набор команд:
```bash
npm run db:i18n:auto
npm run i18n:extract 
npm run i18n:check
npm run i18n:sync
npm run i18n:check
```

### Python скрипты для переводов

```bash
# Проверить русские файлы
python3 check_russian_locales.py

# Синхронизировать после изменения кода
python3 sync_locales.py

# Запустить автоперевод
python3 translate_from_russian.py

# Все сразу
python3 check_russian_locales.py && python3 sync_locales.py && python3 translate_from_russian.py
```

Как проверить прогресс:

Остановить и перезапустить с видимым прогрессом:
```bash
# Остановить текущий
pkill -f translate_from_russian.py

# Запустить заново
cd /Users/tahir/Desktop/beauty-crm
python3 translate_from_russian.py
```

---

## 🗑️ Команды для очистки

1. Удалить все __pycache__ папки
```bash
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
```

2. Удалить кэш переводов
```bash
rm -rf scripts/translations/.cache/
```

---

## 📝 Автоматическая очистка (cron)

Для production сервера

Открыть crontab
```bash
crontab -e
```

Введите 1 (nano - самый простой редактор) и нажмите Enter.

Шаг 2: Добавьте строку в редакторе

После выбора редактора откроется пустой файл. Добавьте эту строку:

```bash
# Добавить строку (очистка каждое воскресенье в 3:00)
0 3 * * 0 cd /path/to/beauty-crm/backend && find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
```

---

## � Git команды

Принудительный push
```bash
git push origin main --force
```

---

# 📞 Справочная информация

## 📞 Данные доступа

- **Сервер:** 91.201.215.32
- **Пользователь:** ubuntu
- **Пароль:** kMJhy5pHKYiKRb2Pb2Dfm3U=
- **Домен:** mlediamant.com
- **Email (SSL):** mladiamontuae@gmail.com

---

## 🔗 Полезные ссылки

Сайт прокси:
https://dashboard.webshare.io/12112026/proxy/list?authenticationMethod=%22username_password%22&connectionMethod=%22direct%22&proxyControl=%220%22&rowsPerPage=10&page=0&order=%22asc%22&orderBy=null&searchValue=%22%22&removeType=%22refresh_all%22

---

## ✅ Готово!

Твой проект доступен по адресу:
- **HTTP:** http://mlediamant.com
- **HTTPS:** https://mlediamant.com
- **API Docs:** https://mlediamant.com/docs
- **Health Check:** https://mlediamant.com/health

🎉 **CRM система успешно развернута!**

---

## 🚀 Команды для полной очистки и перезаливки проекта (ЗАПУСКАТЬ НА MAC)

```bash
# 1. Удалить все содержимое на сервере (запускать на Mac)
ssh ubuntu@91.201.215.32 "rm -rf /home/ubuntu/beauty_crm/*"

# 2. Залить все содержимое с ПК без лишних папок (запускать на Mac)
rsync -avz --progress --exclude 'venv' --exclude 'node_modules' --exclude '.git' ~/Desktop/beauty-crm/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/
```

---

## 🛠️ Если вы УЖЕ подключены к серверу (через SSH):

Если вы уже зашли на сервер и хотите всё очистить:

```bash
# 1. Перейти в папку проекта (если вы не в ней)
cd /home/ubuntu/beauty_crm

# 2. Удалить всё содержимое
rm -rf ./*

# 3. ВЫЙТИ ИЗ СЕРВЕРА (Обязательно, чтобы вернуться на Mac)
exit
```

**После того как вы вышли (`exit`):**
Ваш терминал снова на Mac. Теперь вставьте команду заливки:

```bash
# 4. Запустить на Mac для заливки файлов
rsync -avz --progress --exclude 'venv' --exclude 'node_modules' --exclude '.git' ~/Desktop/beauty-crm/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/
```

---

## 📜 Просмотр журнала событий (логов) на сервере

Чтобы видеть в реальном времени, что происходит с бэкендом (ошибки, запросы):

```bash
# Посмотреть живой журнал событий (на сервере)
sudo systemctl daemon-reload
sudo systemctl restart beauty_crm
sudo systemctl restart nginx
sudo journalctl -u beauty_crm -f
```


---

## 🚀 Команды для удаления и перезаливки ТОЛЬКО папки backend (ЗАПУСКАТЬ НА MAC)

Если вам нужно обновить только бэкенд, не трогая фронтенд:

```bash
# 1. Удалить папку backend на сервере
ssh ubuntu@91.201.215.32 "rm -rf /home/ubuntu/beauty_crm/backend"

# 2. Залить папку backend с вашего Mac (исключая venv)
rsync -avz --progress --exclude 'venv' --exclude '__pycache__' --exclude '.git' ~/Desktop/beauty-crm/backend/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/backend/
```

*(После этого не забудьте перезапустить сервис: `ssh ubuntu@91.201.215.32 "sudo systemctl restart beauty_crm"`) *
```

rm -rf /home/ubuntu/backend



1.зайди на сервер ssh ubuntu@91.201.215.32, выполни 
# 1. Перейти в папку проекта (если вы не в ней)
cd /home/ubuntu/beauty_crm

# 2. Удалить всё содержимое
rm -rf ./*

# 3. ВЫЙТИ ИЗ СЕРВЕРА (Обязательно, чтобы вернуться на Mac)
exit
```

**После того как вы вышли (`exit`):**
Ваш терминал снова на Mac. Теперь вставьте команду заливки:

```bash
# 4. Запустить на Mac для заливки файлов
rsync -avz --progress --exclude 'venv' --exclude 'node_modules' --exclude '.git' ~/Desktop/beauty-crm/ ubuntu@91.201.215.32:/home/ubuntu/beauty_crm/
```

после как все передасться выполни зайди в папку проекта beauty_crm и выполни 
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 -m pip install --upgrade pip

и паралельно запусти  перейдя в папку frontend а именно можно из бэкенда вот так перейти
cd ..
cd frontend
npm cache clean --force 
npm install --no-audit --prefer-offline --verbose

как все закончится, выполни команду 
sudo systemctl daemon-reload
sudo systemctl restart beauty_crm
sudo systemctl restart nginx
sudo journalctl -u beauty_crm -f

ничего другого от себя не придумывай. ничего лишнего не удаляй, перед тем как что-то удалить, проверь в нужной ли ты папке или нет, и перед тем как что-то установить проверь в нужной ли ты папке или нет