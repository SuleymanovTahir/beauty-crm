# Анализ прав доступа для ролей Employee и Admin

## 📋 Содержание
1. [Анализ роли Employee (Сотрудник/Мастер)](#employee-analysis)
2. [Анализ роли Admin (Администратор)](#admin-analysis)
3. [Рекомендации по улучшению](#recommendations)

---

<a name="employee-analysis"></a>
## 1. Роль Employee (Сотрудник / Мастер)

### Текущие права

```typescript
employee: {
  name: 'Сотрудник (мастер)',
  permissions: [
    'bookings_view_own',      // Просмотр только своих записей
    'calendar_view_own',      // Просмотр только своего календаря
    'clients_view_own',       // Просмотр только своих клиентов
    'staff_chat_own',         // Внутренняя связь с коллегами
  ],
  can_manage_roles: [],       // Не может управлять ролями
  hierarchy_level: 20,        // Самый низкий уровень
}
```

### ✅ Что ДОСТУПНО сотруднику

**Разделы меню:**

1. **Календарь** (Dashboard для Employee) - `/employee/calendar`
   - Видит только свой календарь
   - Свои записи на день/неделю/месяц
   - **Проблема**: Dashboard показывает календарь, но отдельный пункт "Календарь" скрыт

2. **Записи** - `/employee/bookings`
   - Видит только свои записи
   - Может работать только со своими клиентами
   - ✅ Доступ есть (благодаря `user?.role === 'employee'`)

3. **Профиль** - `/employee/profile`
   - Свои данные
   - Настройки аккаунта
   - ✅ Доступ есть

4. **Настройки** - `/employee/settings`
   - Ограниченные настройки (язык, тема)
   - ✅ Доступ есть

### ❌ Что НЕ ДОСТУПНО сотруднику (текущее состояние)

**Скрытые разделы:**

1. ❌ **Задачи** - Не видит задачи
   - `requirePermission: () => permissions.canViewTasks || permissions.roleLevel >= 70`
   - **Проблема**: Сотрудники часто получают задачи от менеджеров
   - **Рекомендация**: ⚠️ Нужно добавить доступ к задачам

2. ❌ **Чат** (Instagram и мессенджеры) - Не видит
   - `requirePermission: () => permissions.canViewInstagramChat || permissions.roleLevel >= 70`
   - **Проблема**: Сотрудники не могут общаться с клиентами
   - **Рекомендация**: ⚠️ Возможно нужен доступ для ответов клиентам

3. ❌ **Внутренняя связь** - Не видит
   - `requirePermission: () => user?.role === 'sales' || user?.role === 'marketer' || user?.role === 'manager'`
   - **Проблема**: Есть право `staff_chat_own`, но меню не показывается!
   - **Рекомендация**: ✅ **КРИТИЧНО** - Нужно добавить в условие employee

4. ❌ **Календарь всех** - Видит только свой
   - Не может видеть загруженность коллег
   - **Рекомендация**: ⚠️ Возможно стоит дать readonly доступ к календарю коллег

5. ❌ **Клиенты** - Не видит список
6. ❌ **Услуги** - Не видит каталог услуг
7. ❌ **Аналитика** - Никакой аналитики
8. ❌ **Пользователи** - Не видит коллег
9. ❌ **Телефония** - Не может звонить
10. ❌ **Настройки бота** - Не видит

### 🔥 КРИТИЧЕСКИЕ проблемы для Employee:

#### 1. **Внутренняя связь не работает!**
```typescript
// Текущее условие в MainLayout.tsx (строка 231):
requirePermission: () => user?.role === 'sales' || user?.role === 'marketer' || user?.role === 'manager'

// ❌ ПРОБЛЕМА: Employee имеет право 'staff_chat_own', но меню скрыто!
// ✅ ИСПРАВЛЕНИЕ: Добавить employee в условие
requirePermission: () => user?.role === 'sales' || user?.role === 'marketer' || user?.role === 'manager' || user?.role === 'employee'
```

#### 2. **Нет доступа к задачам**
```typescript
// Текущее:
permissions: [
  'bookings_view_own',
  'calendar_view_own',
  'clients_view_own',
  'staff_chat_own',
]

// Рекомендация:
permissions: [
  'bookings_view_own',
  'calendar_view_own',
  'clients_view_own',
  'staff_chat_own',
  'tasks_view_own',           // ✅ ДОБАВИТЬ: Свои задачи
]
```

### 💡 Рекомендации для Employee

#### Высокий приоритет (ОБЯЗАТЕЛЬНО):

1. ✅ **Исправить внутреннюю связь**
   - Добавить employee в условие для internal-chat
   - Сотрудники ДОЛЖНЫ общаться с менеджерами

2. ✅ **Добавить доступ к задачам**
   - Добавить право `tasks_view_own`
   - Сотрудники получают задачи от менеджеров

#### Средний приоритет (ЖЕЛАТЕЛЬНО):

3. ⚠️ **Просмотр услуг (readonly)**
   - Добавить право `services_view`
   - Сотрудники должны знать, какие услуги они оказывают
   - Не дать право на редактирование

4. ⚠️ **Просмотр календаря коллег (readonly)**
   - Добавить право `calendar_view_all_readonly`
   - Чтобы видеть загруженность коллег
   - Без права создавать записи другим

#### Низкий приоритет (ОПЦИОНАЛЬНО):

5. 📝 **Чат с клиентами**
   - Если сотрудники должны отвечать клиентам в Instagram
   - Добавить право `instagram_chat_view`

6. 📝 **Базовая статистика**
   - Видеть свою статистику (количество записей, доход)
   - Добавить право `analytics_view_own_stats`

---

<a name="admin-analysis"></a>
## 2. Роль Admin (Администратор)

### Текущие права

```typescript
admin: {
  name: 'Администратор',
  permissions: [
    'clients_view',              // Просмотр всех клиентов
    'clients_create',            // Создание клиентов
    'clients_edit',              // Редактирование клиентов
    'bookings_view',             // Просмотр всех записей
    'bookings_create',           // Создание записей
    'bookings_edit',             // Редактирование записей
    'services_view',             // Просмотр услуг
    'users_view',                // Просмотр пользователей
    'users_create',              // Создание пользователей
    'analytics_view_anonymized', // Аналитика без финансов
    'staff_chat_own',            // Внутренняя связь
    'calendar_view_all',         // Календарь всех сотрудников
  ],
  can_manage_roles: ['manager', 'sales', 'marketer', 'employee'],
  hierarchy_level: 80,
}
```

### ✅ Что ДОСТУПНО администратору

**Разделы меню:**

1. **Dashboard** - `/crm/dashboard`
   - Полная аналитика (без финансов)
   - Статистика по записям, клиентам

2. **Календарь** - `/crm/calendar`
   - Календарь всех сотрудников
   - Создание записей для любого сотрудника

3. **Записи** - `/crm/bookings`
   - Все записи системы
   - Создание, редактирование

4. **Задачи** - `/crm/tasks`
   - Просмотр всех задач
   - ✅ Доступ благодаря `roleLevel >= 70`

5. **Чат** - `/crm/chat`
   - Instagram и другие мессенджеры
   - ✅ Доступ благодаря `roleLevel >= 70`

6. **Клиенты** - `/crm/clients`
   - Полный список клиентов с контактами
   - Создание, редактирование

7. **Услуги** - `/crm/services`
   - Каталог услуг
   - **Проблема**: Может только просматривать, не редактировать

8. **Пользователи** - `/crm/users`
   - Управление сотрудниками
   - Создание новых пользователей
   - Изменение ролей (кроме director)

9. **Аналитика** - `/crm/analytics`
   - Полная аналитика без финансовых деталей
   - Статистика продаж, конверсии

10. **Посетители** - `/crm/visitor-analytics`
    - Аналитика сайта
    - ✅ Доступ благодаря `canViewAnalytics`

11. **Публичный контент** - `/crm/public-content`
    - Управление контентом сайта
    - ✅ Доступ благодаря `roleLevel >= 80`

12. **Телефония** - `/crm/telephony`
    - Звонки клиентам
    - История звонков
    - ✅ Доступ благодаря `roleLevel >= 80`

13. **Внутренняя связь** - `/crm/internal-chat`
    - Общение с коллегами
    - ❌ **ПРОБЛЕМА**: НЕТ ДОСТУПА!

14. **Настройки** - `/crm/settings`
    - Настройки системы
    - ✅ Доступ благодаря `canViewSettings`

15. **Корзина** - `/crm/trash`
    - Восстановление удаленных данных
    - ✅ Доступ благодаря `roleLevel >= 80`

### ❌ Что НЕ ДОСТУПНО администратору

**Скрытые разделы:**

1. ❌ **Внутренняя связь** - Не видит!
   - `requirePermission: () => user?.role === 'sales' || user?.role === 'marketer' || user?.role === 'manager'`
   - **Проблема**: Есть право `staff_chat_own`, но меню скрыто!
   - **Рекомендация**: ✅ **КРИТИЧНО** - Админ ДОЛЖЕН общаться с командой

2. ❌ **Редактирование услуг**
   - Может только просматривать услуги
   - Нет права `services_edit`
   - **Рекомендация**: ⚠️ Админ должен уметь редактировать услуги

3. ❌ **Настройки бота**
   - Не видит настройки бота
   - `requirePermission: () => permissions.canViewBotSettings`
   - **Рекомендация**: ⚠️ Админ должен настраивать бота

4. ❌ **Логи аудита**
   - Только директор может видеть
   - **Рекомендация**: ⚠️ Админ должен видеть логи безопасности

5. ❌ **Удаление данных**
   - Нет прав на удаление клиентов, записей
   - **Рекомендация**: ⚠️ Админ должен уметь удалять

6. ❌ **Рассылки**
   - Не может отправлять рассылки
   - **Рекомендация**: ⚠️ Админ должен уметь делать рассылки

7. ❌ **Воронка**
   - Не видит воронку продаж
   - **Рекомендация**: ⚠️ Админ должен видеть воронку

### 🔥 КРИТИЧЕСКИЕ проблемы для Admin:

#### 1. **Внутренняя связь не работает!**
```typescript
// Текущее условие (строка 231):
requirePermission: () => user?.role === 'sales' || user?.role === 'marketer' || user?.role === 'manager'

// ❌ ПРОБЛЕМА: Admin имеет право 'staff_chat_own', но меню скрыто!
// ✅ ИСПРАВЛЕНИЕ:
requirePermission: () => permissions.roleLevel >= 40  // Включит admin (80), manager (60), sales (40)
```

#### 2. **Не может редактировать услуги**
```typescript
// Добавить в права:
permissions: [
  ...
  'services_view',
  'services_edit',        // ✅ ДОБАВИТЬ
]
```

#### 3. **Не может удалять данные**
```typescript
// Добавить в права:
permissions: [
  ...
  'clients_delete',       // ✅ ДОБАВИТЬ
  'bookings_delete',      // ✅ ДОБАВИТЬ
  'users_edit',           // ✅ ДОБАВИТЬ (сейчас есть только create)
]
```

### 💡 Рекомендации для Admin

#### Высокий приоритет (ОБЯЗАТЕЛЬНО):

1. ✅ **Исправить внутреннюю связь**
   - Изменить условие на `roleLevel >= 40`
   - Админ ДОЛЖЕН общаться с командой

2. ✅ **Добавить редактирование услуг**
   - Добавить право `services_edit`
   - Админ управляет прайс-листом

3. ✅ **Добавить права на удаление**
   - `clients_delete`, `bookings_delete`, `users_edit`
   - Админ должен управлять базой данных

#### Средний приоритет (ЖЕЛАТЕЛЬНО):

4. ⚠️ **Доступ к настройкам бота**
   - Добавить право `bot_settings_view`
   - Админ настраивает автоматизацию

5. ⚠️ **Доступ к рассылкам**
   - Добавить право `broadcasts_send`
   - Админ делает маркетинговые рассылки

6. ⚠️ **Доступ к воронке**
   - Воронка уже доступна через `canViewAnalytics`
   - Проверить, работает ли

7. ⚠️ **Доступ к логам аудита**
   - Добавить в условие `roleLevel >= 80`
   - Админ отвечает за безопасность

#### Низкий приоритет (ОПЦИОНАЛЬНО):

8. 📝 **Полная финансовая аналитика**
   - Изменить `analytics_view_anonymized` → `analytics_view_full`
   - Если админ должен видеть финансы

---

<a name="recommendations"></a>
## 3. Итоговые рекомендации и таблица

### Сравнительная таблица ДО и ПОСЛЕ

| Функция | Employee (ДО) | Employee (ПОСЛЕ) | Admin (ДО) | Admin (ПОСЛЕ) |
|---------|---------------|------------------|------------|---------------|
| Dashboard | ✅ Свой календарь | ✅ Свой календарь | ✅ Аналитика | ✅ Аналитика |
| Календарь | ✅ Только свой | ⚠️ + Просмотр всех (readonly) | ✅ Всех | ✅ Всех |
| Записи | ✅ Свои | ✅ Свои | ✅ Все | ✅ Все + удаление |
| Задачи | ❌ НЕТ | ✅ **ДОБАВИТЬ** свои | ✅ Все | ✅ Все |
| Чат | ❌ НЕТ | ⚠️ Опционально | ✅ Все | ✅ Все |
| Профиль | ✅ Свой | ✅ Свой | ✅ + Других | ✅ + Других |
| Клиенты | ❌ Только свои | ❌ Только свои | ✅ Все | ✅ Все + удаление |
| Услуги | ❌ НЕТ | ⚠️ **ДОБАВИТЬ** просмотр | ✅ Просмотр | ✅ **ДОБАВИТЬ** редактирование |
| Пользователи | ❌ НЕТ | ❌ НЕТ | ✅ Управление | ✅ Управление + редактирование |
| Аналитика | ❌ НЕТ | ⚠️ Своя статистика | ✅ Без финансов | ✅ Без финансов |
| Воронка | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ⚠️ **ДОБАВИТЬ** |
| Рассылки | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ⚠️ **ДОБАВИТЬ** |
| Телефония | ❌ НЕТ | ❌ НЕТ | ✅ Есть | ✅ Есть |
| Внутренняя связь | ❌ **БАГ!** | ✅ **ИСПРАВИТЬ** | ❌ **БАГ!** | ✅ **ИСПРАВИТЬ** |
| Настройки | ✅ Ограниченные | ✅ Ограниченные | ✅ Все | ✅ Все |
| Настройки бота | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ⚠️ **ДОБАВИТЬ** |
| Логи аудита | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ⚠️ **ДОБАВИТЬ** |
| Корзина | ❌ НЕТ | ❌ НЕТ | ✅ Есть | ✅ Есть |

### Приоритет изменений

#### 🔴 КРИТИЧНО (Исправить немедленно):

1. **Внутренняя связь для Employee и Admin** - БАГ в коде
2. **Задачи для Employee** - Без этого сотрудники не видят свои задания
3. **Удаление данных для Admin** - Админ не может управлять базой
4. **Редактирование услуг для Admin** - Админ не может менять прайс

#### 🟡 ВАЖНО (Добавить в ближайшее время):

5. **Просмотр услуг для Employee** - Сотрудники должны знать услуги
6. **Настройки бота для Admin** - Админ настраивает автоматизацию
7. **Рассылки для Admin** - Маркетинг
8. **Логи аудита для Admin** - Безопасность

#### 🟢 ЖЕЛАТЕЛЬНО (При наличии времени):

9. **Календарь коллег для Employee** (readonly)
10. **Воронка для Admin**
11. **Своя статистика для Employee**
12. **Чат с клиентами для Employee**

---

## ✅ ИЗМЕНЕНИЯ ВНЕСЕНЫ!

### Что было исправлено:

#### 🔴 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ (Выполнено):

1. ✅ **Внутренняя связь для Employee и Admin** - ИСПРАВЛЕНО
   - Изменено условие в `MainLayout.tsx` (строка 231)
   - Было: `user?.role === 'sales' || user?.role === 'marketer' || user?.role === 'manager'`
   - Стало: `permissions.canUseStaffChat`
   - Добавлен метод `canUseStaffChat()` в `PermissionChecker`

2. ✅ **Задачи для Employee** - ДОБАВЛЕНО
   - Добавлено право `tasks_view_own` в frontend и backend
   - Employee теперь видит свои задачи
   - Обновлен `canViewTasks()` для проверки `tasks_view_own`

3. ✅ **Редактирование услуг для Admin** - ДОБАВЛЕНО
   - Добавлено право `services_edit`
   - Admin теперь может редактировать прайс-лист

4. ✅ **Удаление данных для Admin** - ДОБАВЛЕНО
   - Добавлены права: `clients_delete`, `bookings_delete`, `users_edit`
   - Admin может полноценно управлять базой данных

5. ✅ **Настройки бота и рассылки для Admin** - ДОБАВЛЕНО
   - Добавлены права: `bot_settings_view`, `broadcasts_send`
   - Admin может настраивать бота и отправлять рассылки

6. ✅ **Логи аудита для Admin** - ДОБАВЛЕНО
   - Изменено условие в `MainLayout.tsx` (строка 236)
   - Было: `user?.role === 'director'`
   - Стало: `permissions.roleLevel >= 80` (включает admin и director)

#### 🟡 ВАЖНЫЕ УЛУЧШЕНИЯ (Выполнено):

7. ✅ **Просмотр услуг для Employee** - ДОБАВЛЕНО
   - Добавлено право `services_view`
   - Сотрудники теперь могут видеть каталог услуг (readonly)

8. ✅ **Воронка для Admin** - УЖЕ ДОСТУПНА
   - Воронка доступна через `permissions.canViewAnalytics`
   - Admin уже имеет это право

### Измененные файлы:

1. **frontend/src/utils/permissions.ts**
   - Добавлен метод `canUseStaffChat()` в `PermissionChecker`
   - Добавлено `canUseStaffChat` в хук `usePermissions`
   - Обновлены права роли `employee`: добавлены `tasks_view_own`, `services_view`
   - Обновлены права роли `admin`: добавлены `clients_delete`, `bookings_delete`, `services_edit`, `users_edit`, `bot_settings_view`, `broadcasts_send`

2. **frontend/src/components/layouts/MainLayout.tsx**
   - Исправлено условие для Internal Chat (строка 231)
   - Изменено с хардкод проверки ролей на `permissions.canUseStaffChat`
   - Исправлено условие для Audit Log (строка 236)
   - Изменено с `user?.role === 'director'` на `permissions.roleLevel >= 80`

3. **backend/core/config.py**
   - Обновлены права роли `employee`: добавлены `tasks_view_own`, `services_view`
   - Обновлены права роли `admin`: добавлены `clients_delete`, `bookings_delete`, `services_edit`, `users_edit`, `bot_settings_view`, `broadcasts_send`
   - Добавлены описания новых прав в `PERMISSION_DESCRIPTIONS`:
     - `tasks_view`, `tasks_view_own`, `tasks_create`, `tasks_edit`, `tasks_delete`
     - `broadcasts_send`, `broadcasts_view`

### Итоговая таблица прав (ОБНОВЛЕНО):

| Функция | Employee (БЫЛО) | Employee (СТАЛО) | Admin (БЫЛО) | Admin (СТАЛО) |
|---------|-----------------|------------------|--------------|---------------|
| Внутренняя связь | ❌ **БАГ!** | ✅ **ИСПРАВЛЕНО** | ❌ **БАГ!** | ✅ **ИСПРАВЛЕНО** |
| Задачи | ❌ НЕТ | ✅ **ДОБАВЛЕНО** (свои) | ✅ Все | ✅ Все |
| Услуги (просмотр) | ❌ НЕТ | ✅ **ДОБАВЛЕНО** | ✅ Да | ✅ Да |
| Услуги (редактирование) | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ✅ **ДОБАВЛЕНО** |
| Удаление клиентов | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ✅ **ДОБАВЛЕНО** |
| Удаление записей | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ✅ **ДОБАВЛЕНО** |
| Редактирование пользователей | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ✅ **ДОБАВЛЕНО** |
| Настройки бота | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ✅ **ДОБАВЛЕНО** |
| Рассылки | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ✅ **ДОБАВЛЕНО** |
| Логи аудита | ❌ НЕТ | ❌ НЕТ | ❌ НЕТ | ✅ **ДОБАВЛЕНО** |
| Воронка | ❌ НЕТ | ❌ НЕТ | ✅ Уже есть | ✅ Уже есть |

### ✅ Все КРИТИЧЕСКИЕ и ВАЖНЫЕ задачи выполнены!

#### 🟢 Опционально (НЕ требуется):

- **Календарь коллег для Employee** (readonly) - `calendar_view_all_readonly` - НЕ НУЖНО
- **Своя статистика для Employee** - `analytics_view_own_stats` - НЕ НУЖНО
- **Чат с клиентами для Employee** (Instagram) - `instagram_chat_view` - НЕ НУЖНО

### Итоговый статус:

**Employee имеет:**
- ✅ Свои записи (`bookings_view_own`)
- ✅ Свой календарь (`calendar_view_own`)
- ✅ Своих клиентов (`clients_view_own`)
- ✅ Внутреннюю связь с коллегами (`staff_chat_own`) - БАГ ИСПРАВЛЕН
- ✅ Свои задачи (`tasks_view_own`) - ДОБАВЛЕНО
- ✅ Каталог услуг (`services_view`) - ДОБАВЛЕНО

**Admin имеет:**
- ✅ Все права на клиентов (view, create, edit, delete)
- ✅ Все права на записи (view, create, edit, delete)
- ✅ Все права на услуги (view, edit)
- ✅ Управление пользователями (view, create, edit)
- ✅ Внутреннюю связь (`staff_chat_own`) - БАГ ИСПРАВЛЕН
- ✅ Настройки бота (`bot_settings_view`)
- ✅ Рассылки (`broadcasts_send`)
- ✅ Логи аудита (доступ открыт)
- ✅ Воронка (через `analytics_view_anonymized`)
- ✅ Корзину (`roleLevel >= 80`)
