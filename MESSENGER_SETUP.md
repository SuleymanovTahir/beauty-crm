# Настройка мессенджеров

Этот документ описывает как настроить интеграцию с различными мессенджерами в Beauty CRM.

## Общая информация

Система поддерживает 4 мессенджера:
- **Instagram** (встроенная интеграция, настройка не требуется)
- **WhatsApp** (открывает WhatsApp Web с номером клиента)
- **Telegram** (требует Bot Token)
- **TikTok** (в разработке)

## Управление мессенджерами

### Где настраивать

1. Откройте админ панель: http://localhost:5173/admin/settings
2. Перейдите на вкладку **"Мессенджеры"**
3. Вы увидите карточки всех доступных мессенджеров

### Включение/выключение мессенджера

- Используйте переключатель (Switch) справа от каждого мессенджера
- Включенные мессенджеры будут доступны:
  - В выпадающем меню "Чат" в боковой панели
  - На странице клиента (кнопки для связи с клиентом)

---

## Instagram

**Статус:** ✅ Настроен автоматически

Instagram использует существующую интеграцию через Instagram Graph API. Дополнительная настройка не требуется.

---

## WhatsApp

**Статус:** ✅ Работает без дополнительной настройки

### Как работает

- Открывает WhatsApp Web с номером телефона клиента
- Номер должен быть указан в профиле клиента
- Формат: любой (автоматически очищается до цифр)

### Настройка

1. Включите WhatsApp в настройках мессенджеров
2. Убедитесь, что у клиентов указаны номера телефонов

---

## Telegram

**Статус:** ⚠️ Требует настройки

### Быстрая настройка

#### Шаг 1: Создайте Telegram бота

1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Придумайте имя бота (например: "M.Le Diamant Beauty Bot")
   - Придумайте username бота (должен заканчиваться на "bot", например: "mlediamant_beauty_bot")
5. BotFather даст вам **Bot Token** вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
6. **Сохраните этот токен!**

#### Шаг 2: Добавьте токен в CRM

**✅ Текущая конфигурация (РЕКОМЕНДУЕТСЯ):**

Токен хранится в переменных окружения `.env.local` и `.env.production`:

```bash
# В файле backend/.env.local (для локальной разработки)
# В файле backend/.env.production (для production сервера)
TELEGRAM_BOT_TOKEN=6784705707:AAHmgFZ1GwvAZ443DNWd4fgT3s1O3sXkySI
```

**Преимущества:**
- ✅ Токен в одном месте с другими API ключами (Instagram, Gemini)
- ✅ Не нужно хранить в БД
- ✅ Проще управление конфигурацией
- ✅ Безопаснее (не попадает в git благодаря .gitignore)

**Альтернативные варианты:**

<details>
<summary>Вариант 1: Через веб-интерфейс</summary>

1. Откройте http://localhost:5173/admin/settings
2. Вкладка **"Мессенджеры"**
3. Найдите карточку **Telegram**
4. Включите Telegram (переключатель справа)
5. Нажмите **"Настроить"**
6. Вставьте Bot Token в поле "API Token"
7. Нажмите **"Сохранить"**

**Примечание:** Если токен есть в .env, он имеет приоритет над БД
</details>

<details>
<summary>Вариант 2: Через скрипт Python</summary>

```bash
cd /home/user/beauty-crm/backend
python scripts/setup/save_telegram_token.py
```

Вы увидите:
```
✅ Telegram token saved successfully!
   Type: telegram
   Enabled: True
   Token: 6784705707:AAHmgFZ1G...
```
</details>

#### Шаг 3: Настройка Webhook

**Что такое webhook?**

Webhook - это URL на котором ваш сервер будет получать сообщения от Telegram. Когда пользователь пишет боту, Telegram отправляет POST запрос на ваш webhook.

**✅ ТЕКУЩАЯ КОНФИГУРАЦИЯ:**

Webhook уже настроен на zrok URL:
```
https://yznjajbkmllc.share.zrok.io/webhooks/telegram
```

Для проверки или изменения используйте быстрый скрипт:

```bash
cd /home/user/beauty-crm/backend
python scripts/setup/quick_telegram_webhook.py
```

Скрипт автоматически:
- ✅ Проверит токен из .env
- ✅ Покажет информацию о боте (@aqildos_bot)
- ✅ Настроит webhook на zrok URL
- ✅ Проверит статус webhook

**Другие варианты настройки:**

<details>
<summary>А) Для production (реальный домен)</summary>

1. У вас должен быть публичный домен с HTTPS (например: `https://mlediamant.com`)
2. Отредактируйте `quick_telegram_webhook.py` и замените URL:
   ```python
   WEBHOOK_URL = "https://mlediamant.com/webhooks/telegram"
   ```
3. Запустите скрипт:
   ```bash
   python scripts/setup/quick_telegram_webhook.py
   ```

**Или используйте интерактивный скрипт:**

```bash
python scripts/setup/setup_telegram_webhook.py
```

Выберите опцию `1` (Set webhook) и введите URL.
</details>

<details>
<summary>Б) Для локального тестирования (ngrok)</summary>

Если используете ngrok вместо zrok:

1. Установите ngrok: https://ngrok.com/download
2. Запустите ngrok:
   ```bash
   ngrok http 8000
   ```
3. Ngrok даст вам временный URL: `https://abc123.ngrok.io`
4. Настройте webhook:
   ```bash
   cd /home/user/beauty-crm/backend
   python scripts/setup/setup_telegram_webhook.py
   ```
5. Введите: `https://abc123.ngrok.io/webhooks/telegram`

**Важно:** После каждого перезапуска ngrok/zrok URL меняется, нужно заново настраивать webhook!
</details>

#### Шаг 4: Проверка

**1. Проверьте статус webhook:**

```bash
cd /home/user/beauty-crm/backend
python scripts/setup/setup_telegram_webhook.py
# Выберите опцию 3 (Check webhook status)
```

Вы должны увидеть:
```
📊 Webhook Status:
   URL: https://yourdomain.com/webhooks/telegram
   Pending updates: 0
```

**2. Тест через API endpoint:**

Откройте в браузере:
```
http://localhost:8000/webhooks/telegram/test
```

Вы должны увидеть:
```json
{
  "status": "ok",
  "message": "Telegram webhook is ready",
  "bot": {
    "id": 6784705707,
    "username": "your_bot_username",
    "first_name": "Your Bot Name"
  }
}
```

**3. Протестируйте в Telegram:**

1. Найдите вашего бота в Telegram (по username из предыдущего шага)
2. Нажмите **Start** или отправьте `/start`
3. Бот должен ответить приветствием
4. Проверьте логи backend - должны появиться записи:

```
📨 TELEGRAM WEBHOOK: POST request received
✅ Telegram update processed successfully
```

**4. Проверьте UI:**

1. Включите Telegram в настройках мессенджеров
2. Откройте страницу любого клиента
3. Вы должны увидеть синюю кнопку **"Telegram"**
4. В боковом меню при наведении на "Чат" должен появиться пункт Telegram

### Расположение файлов

#### Конфигурация

**Telegram Bot Token:**
```
Файл: /home/user/beauty-crm/backend/.env.local (локально)
Файл: /home/user/beauty-crm/backend/.env.production (на сервере)
Переменная: TELEGRAM_BOT_TOKEN
```

**Настройки мессенджеров:**
```
Файл: /home/user/beauty-crm/backend/salon_bot.db
Таблица: messenger_settings
Поля: is_enabled, webhook_url, config_json
```

**Примечание:** Токен читается из .env (приоритет), затем из БД (fallback)

#### Код интеграции

**Telegram Bot класс:**
```
Файл: /home/user/beauty-crm/backend/integrations/telegram_bot.py
Содержит: TelegramBot класс с методами для отправки сообщений, обработки обновлений
Читает токен: из core.config.TELEGRAM_BOT_TOKEN (загружается из .env)
```

**Webhook endpoints:**
```
Файл: /home/user/beauty-crm/backend/webhooks/telegram.py
Endpoints:
  - POST /webhooks/telegram - получение сообщений от Telegram
  - GET  /webhooks/telegram/test - тест webhook
  - GET  /webhooks/telegram/info - информация о webhook
```

**Скрипты настройки:**
```
Файл: /home/user/beauty-crm/backend/scripts/setup/quick_telegram_webhook.py
Назначение: Быстрая настройка webhook (автоматически использует zrok URL)

Файл: /home/user/beauty-crm/backend/scripts/setup/setup_telegram_webhook.py
Назначение: Интерактивная настройка webhook (можно указать любой URL)
```

**API endpoints для мессенджеров:**
```
Файл: /home/user/beauty-crm/backend/api/messengers.py
Endpoints:
  - GET    /api/messengers/settings - все настройки
  - GET    /api/messengers/enabled - только включенные
  - PUT    /api/messengers/settings/{type} - обновить настройки
  - GET    /api/messengers/{type}/messages - история сообщений
  - POST   /api/messengers/{type}/messages - отправить сообщение
```

**Setup scripts:**
```
/home/user/beauty-crm/backend/scripts/setup/save_telegram_token.py - сохранить токен
/home/user/beauty-crm/backend/scripts/setup/setup_telegram_webhook.py - настроить webhook
```

---

## TikTok

**Статус:** 🚧 В разработке

TikTok интеграция планируется в будущих обновлениях.

---

## История сообщений

### Где хранятся сообщения

Все сообщения от разных мессенджеров хранятся в одной таблице:

```
Файл: /home/user/beauty-crm/backend/salon_bot.db
Таблица: messenger_messages
```

### Структура сообщений

```sql
CREATE TABLE messenger_messages (
    id INTEGER PRIMARY KEY,
    messenger_type TEXT,      -- 'instagram', 'telegram', 'whatsapp', 'tiktok'
    client_id TEXT,           -- ID клиента
    external_message_id TEXT, -- ID сообщения в мессенджере
    sender_type TEXT,         -- 'client' или 'admin'
    message_text TEXT,        -- Текст сообщения
    attachments_json TEXT,    -- JSON с вложениями
    is_read INTEGER,          -- 0 или 1
    created_at TEXT           -- Дата и время
);
```

### Просмотр истории

```bash
cd /home/user/beauty-crm/backend
sqlite3 salon_bot.db

# Все сообщения Telegram
SELECT * FROM messenger_messages WHERE messenger_type = 'telegram';

# Сообщения конкретного клиента
SELECT * FROM messenger_messages
WHERE client_id = 'CLIENT_ID'
ORDER BY created_at DESC;

# Непрочитанные сообщения
SELECT * FROM messenger_messages
WHERE is_read = 0 AND sender_type = 'client';
```

---

## API Endpoints

### Получить настройки мессенджеров

```
GET /api/messengers/settings
```

### Получить включенные мессенджеры

```
GET /api/messengers/enabled
```

Ответ:
```json
{
  "enabled_messengers": [
    {"type": "instagram", "name": "Instagram"},
    {"type": "telegram", "name": "Telegram"}
  ]
}
```

### Обновить настройки мессенджера

```
PUT /api/messengers/settings/{messenger_type}
```

Body:
```json
{
  "is_enabled": true,
  "api_token": "your_bot_token_here"
}
```

### Получить сообщения

```
GET /api/messengers/{messenger_type}/messages?client_id={id}
```

---

## Troubleshooting

### Telegram не отправляет сообщения

1. Проверьте что токен правильный:
   ```bash
   curl "https://api.telegram.org/bot<YOUR_TOKEN>/getMe"
   ```

2. Проверьте что токен сохранен в БД:
   ```sql
   SELECT api_token FROM messenger_settings WHERE messenger_type = 'telegram';
   ```

3. Проверьте что Telegram включен:
   ```sql
   SELECT is_enabled FROM messenger_settings WHERE messenger_type = 'telegram';
   ```

### Не вижу кнопку мессенджера

1. Проверьте что мессенджер включен в настройках
2. Перезагрузите страницу (Ctrl+F5)
3. Проверьте консоль браузера на ошибки

### История сообщений пустая

1. Убедитесь что клиент существует в таблице `clients`
2. Проверьте что `client_id` совпадает с `instagram_id` из таблицы clients
3. Проверьте логи backend на ошибки

---

## Быстрый тест

### Проверка настроек Telegram

```bash
# 1. Проверить БД
cd /home/user/beauty-crm/backend
sqlite3 salon_bot.db "SELECT * FROM messenger_settings WHERE messenger_type='telegram';"

# 2. Проверить через API
curl http://localhost:8000/api/messengers/enabled

# 3. Проверить токен
curl "https://api.telegram.org/bot<YOUR_TOKEN>/getMe"
```

Если все правильно, вы увидите:
- ✅ В БД: токен сохранен, is_enabled = 1
- ✅ В API: telegram в списке enabled_messengers
- ✅ Telegram API: информация о вашем боте

---

## Дополнительная помощь

Если возникли проблемы:

1. Проверьте логи backend: `backend/logs/`
2. Проверьте консоль браузера (F12)
3. Убедитесь что миграции запущены: `python -m db.migrations.run_all_migrations`

---

**Последнее обновление:** 2025-11-14
