# Руководство по обновлению страниц с проверкой прав

## 📋 Что нужно сделать

Все страницы в `frontend/src/pages` должны использовать централизованную систему прав для показа/скрытия функционала.

## ✅ Layouts - ГОТОВО

Все layout файлы уже обновлены и используют `usePermissions`:
- ✅ AdminLayout.tsx
- ✅ ManagerLayout.tsx
- ✅ EmployeeLayout.tsx
- ✅ SalesLayout.tsx
- ✅ MarketerLayout.tsx

## 🔄 Как обновить страницу

### 1. Импортировать хук

```tsx
import { usePermissions } from '../../utils/permissions';
// или
import { PermissionChecker } from '../../utils/permissions';
```

### 2. Получить права в компоненте

```tsx
function MyPage() {
  const { user } = useAuth(); // или откуда вы получаете пользователя
  const permissions = usePermissions(user?.role || 'employee');

  // Теперь можно использовать permissions
  if (!permissions.canEditUsers) {
    return <div>У вас нет доступа к этой странице</div>;
  }

  // ...остальной код
}
```

### 3. Условный рендеринг

```tsx
{/* Показать кнопку только если есть право */}
{permissions.canCreateUsers && (
  <button onClick={handleCreate}>Создать пользователя</button>
)}

{/* Скрыть целый раздел */}
{permissions.canViewAnalytics && (
  <div className="analytics-section">
    {/* Аналитика */}
  </div>
)}

{/* Отключить кнопку если нет прав */}
<button disabled={!permissions.canEditClients}>
  Редактировать
</button>
```

## 📂 Приоритетные страницы для обновления

### Критичные (должны быть обновлены):

1. **`admin/Users.tsx`** - управление пользователями
   - Показывать кнопку "Изменить роль" только если `canViewAllUsers`
   - Кнопка "Создать" только если `canCreateUsers`
   - Кнопка "Удалить" только если `canDeleteUsers`

2. **`admin/Services.tsx`** - управление услугами
   - Редактирование только если `canEditServices`
   - Создание только если `canEditServices` (админ/директор)

3. **`admin/Settings.tsx`** - настройки
   - Доступ только если `canViewSettings`
   - Редактирование только если `canEditSettings` (только директор!)

4. **`admin/BotSettings.tsx`** - настройки бота
   - Доступ только если `canEditSettings` (только директор!)

5. **`admin/Broadcasts.tsx`** - рассылки
   - Доступ только если `canSendBroadcasts` (director, admin, marketer)

### Важные (желательно обновить):

6. **`admin/Clients.tsx`** - клиенты
   - Создание только если `canCreateClients`
   - Редактирование только если `canEditClients`
   - Удаление только если `canDeleteClients`

7. **`admin/Bookings.tsx`** - записи
   - Создание только если `canCreateBookings`
   - Редактирование только если `canEditBookings`

8. **`admin/Analytics.tsx`** - аналитика
   - Доступ только если `canViewAnalytics`
   - Экспорт только если `canExportData`

### Второстепенные:

- Dashboard - обычно всем доступен
- Calendar - проверять `canViewAllCalendars`
- Chat - обычно всем доступен

## 🎯 Доступные проверки прав

### Пользователи
- `canViewAllUsers` - просмотр всех пользователей
- `canCreateUsers` - создание пользователей
- `canEditUsers` - редактирование пользователей
- `canDeleteUsers` - удаление пользователей

### Клиенты
- `canViewAllClients` - просмотр всех клиентов
- `canViewClientContacts` - просмотр контактов клиентов
- `canCreateClients` - создание клиентов
- `canEditClients` - редактирование клиентов
- `canDeleteClients` - удаление клиентов

### Записи
- `canViewAllBookings` - просмотр всех записей
- `canCreateBookings` - создание записей
- `canEditBookings` - редактирование записей
- `canDeleteBookings` - удаление записей

### Календарь
- `canViewAllCalendars` - просмотр календарей всех сотрудников

### Аналитика
- `canViewAnalytics` - просмотр аналитики
- `canViewFullAnalytics` - полная аналитика с персональными данными (только директор)
- `canExportData` - экспорт данных

### Услуги
- `canViewServices` - просмотр услуг
- `canEditServices` - редактирование услуг

### Настройки
- `canViewSettings` - просмотр настроек
- `canEditSettings` - редактирование настроек (только директор)

### Рассылки
- `canSendBroadcasts` - отправка рассылок (director, admin, marketer)

### Instagram
- `canViewInstagramChat` - просмотр Instagram чата

## 💡 Примеры использования

### Пример 1: Кнопка создания (Users.tsx)

```tsx
function Users() {
  const permissions = usePermissions(user?.role || 'employee');

  return (
    <div>
      <h1>Пользователи</h1>

      {/* Кнопка создания только для тех, у кого есть право */}
      {permissions.canCreateUsers && (
        <button onClick={() => navigate('/admin/users/create')}>
          Создать пользователя
        </button>
      )}

      {/* Таблица пользователей */}
      <table>
        {/* ... */}
      </table>
    </div>
  );
}
```

### Пример 2: Действия в строке таблицы

```tsx
{users.map(user => (
  <tr key={user.id}>
    <td>{user.name}</td>
    <td>
      {/* Кнопка редактирования */}
      {permissions.canEditUsers && (
        <button onClick={() => handleEdit(user.id)}>
          Редактировать
        </button>
      )}

      {/* Кнопка удаления */}
      {permissions.canDeleteUsers && (
        <button onClick={() => handleDelete(user.id)}>
          Удалить
        </button>
      )}
    </td>
  </tr>
))}
```

### Пример 3: Целая страница

```tsx
function Settings() {
  const permissions = usePermissions(user?.role || 'employee');

  // Если нет доступа - показываем сообщение
  if (!permissions.canViewSettings) {
    return (
      <div className="p-8 text-center">
        <h2>Доступ запрещен</h2>
        <p>У вас нет прав для просмотра настроек</p>
      </div>
    );
  }

  return (
    <div>
      <h1>Настройки</h1>

      {/* Форма редактирования только для директора */}
      {permissions.canEditSettings ? (
        <form onSubmit={handleSubmit}>
          {/* Поля формы */}
        </form>
      ) : (
        <div>Только для просмотра</div>
      )}
    </div>
  );
}
```

## 🚀 Следующие шаги

1. ✅ Layouts обновлены - ГОТОВО
2. 📝 Обновить критичные страницы (Users, Services, Settings, BotSettings, Broadcasts)
3. 📝 Обновить важные страницы (Clients, Bookings, Analytics)
4. 📝 Обновить второстепенные страницы
5. ✅ Протестировать с разными ролями

## 📊 Прогресс

- [x] AdminLayout
- [x] ManagerLayout
- [x] EmployeeLayout
- [x] SalesLayout
- [x] MarketerLayout
- [ ] admin/Users.tsx
- [ ] admin/Services.tsx
- [ ] admin/Settings.tsx
- [ ] admin/BotSettings.tsx
- [ ] admin/Broadcasts.tsx
- [ ] admin/Clients.tsx
- [ ] admin/Bookings.tsx
- [ ] admin/Analytics.tsx
- [ ] И остальные...

## ⚠️ Важно

- **НЕ удаляйте** существующие проверки прав, добавляйте новые рядом
- **Всегда** используйте хук `usePermissions` вместо прямой проверки роли
- **Тестируйте** каждое изменение под разными ролями
- **Документируйте** какие права требуются для каждой страницы
