# 🔧 Подробная инструкция по миграциям

## 📋 Обзор

Есть **2 способа** применить миграции:

1. **ОДИН ФАЙЛ** (рекомендуется) - запускает все миграции автоматически
2. **Вручную по одной** - если нужен контроль

---

## ✅ СПОСОБ 1: Запуск всех миграций одним файлом (РЕКОМЕНДУЕТСЯ)

```bash
cd /Users/tahir/Desktop/beauty-crm/backend
python3 run_all_migrations.py
```

**Что будет выполнено:**
1. ✅ Link employees positions (position_id для employees)
2. ✅ Preferred messenger
3. ✅ Telegram chat_id
4. ✅ Notification preferences
5. ✅ Client preferences
6. ✅ Master schedule
7. ✅ Fix master schedule (nullable times)
8. ✅ Loyalty program

**Результат:**
```
🎉 ВСЕ МИГРАЦИИ ПРИМЕНЕНЫ УСПЕШНО!
```

---

## 🔧 СПОСОБ 2: Запуск миграций вручную

Если хотите контроль или нужна только одна миграция:

### Миграция 1: position_id для employees
**Исправляет:** `no such column: e.position_id`

```bash
python3 run_migration.py
```

**Что делает:**
- Добавляет поле `position_id` в таблицу `employees`
- Связывает сотрудников с должностями

---

### Миграция 2: Preferred messenger

```bash
python3 run_migration_preferred_messenger.py
```

**Что делает:**
- Добавляет поле `preferred_messenger` в таблицу `clients`
- Позволяет клиентам выбрать предпочитаемый мессенджер

---

### Миграция 3: Telegram chat_id

```bash
python3 run_migration_telegram_chat_id.py
```

**Что делает:**
- Добавляет поле `telegram_chat_id` в таблицу `clients`
- Нужно для отправки уведомлений через Telegram

---

### Миграция 4: Notification preferences

```bash
python3 run_migration_notification_preferences.py
```

**Что делает:**
- Создает таблицу `notification_preferences`
- Хранит настройки уведомлений для мастеров

---

### Миграция 5: Client preferences

```bash
python3 run_migration_client_preferences.py
```

**Что делает:**
- Создает таблицу `client_preferences`
- Хранит предпочтения клиентов (время, мастер, услуги)

---

### Миграция 6: Master schedule

```bash
python3 run_migration_master_schedule.py
```

**Что делает:**
- Создает таблицу `master_schedule` (рабочие часы)
- Создает таблицу `master_time_off` (отпуска, выходные)
- Создает таблицу `available_slots` (доступные слоты)

---

### Миграция 7: Fix master schedule (ВАЖНО!)
**Исправляет:** `NOT NULL constraint failed: master_schedule.start_time`

```bash
python3 migration_fix_master_schedule_nullable.py
```

**Что делает:**
- Разрешает `NULL` для `start_time` и `end_time`
- Позволяет хранить выходные дни (когда мастер не работает)

**⚠️ ВАЖНО:** Эту миграцию нужно запустить ПОСЛЕ миграции #6!

---

### Миграция 8: Loyalty program

```bash
python3 run_migration_loyalty_program.py
```

**Что делает:**
- Создает таблицу `client_loyalty_points`
- Создает таблицу `loyalty_transactions`
- Создает таблицу `loyalty_levels`
- Добавляет 4 уровня: Bronze, Silver, Gold, Platinum

---

### Миграция 9: position_id для services (ОПЦИОНАЛЬНО)

```bash
python3 migration_add_position_to_services.py
```

**Что делает:**
- Добавляет поле `position_id` в таблицу `services`
- Позволяет указать какая должность мастера может выполнять услугу

**Пример использования:**
```
Услуга: Маникюр + покрытие
position_id: 2 (Мастер маникюра)

→ При записи клиента будут показаны только мастера с должностью #2
```

---

## 🧪 Проверка миграций

После применения миграций запустите:

```bash
python3 test_detailed.py
```

**Что проверяется:**
- ✅ Все таблицы созданы
- ✅ Все поля на месте
- ✅ position_id для employees
- ✅ position_id для services
- ✅ master_schedule с nullable полями
- ✅ Целостность БД

---

## 📊 Статус миграций

### Обязательные (должны быть применены):
- [x] position_id для employees
- [x] Master schedule
- [x] Fix master schedule (nullable)
- [x] Loyalty program

### Рекомендуемые:
- [x] Preferred messenger
- [x] Telegram chat_id
- [x] Notification preferences
- [x] Client preferences

### Опциональные:
- [ ] position_id для services (новая функция)

---

## ⚠️ Важные примечания

### 1. Порядок важен!
Некоторые миграции зависят друг от друга:

```
1. run_migration_master_schedule.py
   ↓
2. migration_fix_master_schedule_nullable.py
```

### 2. Миграции безопасны
Все миграции используют:
- `CREATE TABLE IF NOT EXISTS` - не упадет если таблица уже есть
- `ALTER TABLE ADD COLUMN IF NOT EXISTS` - не упадет если колонка уже есть
- Backup данных перед изменениями

### 3. Можно запускать повторно
Миграции можно безопасно запускать несколько раз - они не испортят данные.

### 4. Откат миграций
Если нужно откатить - сделайте backup БД до применения:

```bash
cp backend/salon_bot.db backend/salon_bot.db.backup
```

---

## 🔍 Диагностика проблем

### Ошибка: "no such column: e.position_id"
**Решение:**
```bash
python3 run_migration.py
```

### Ошибка: "NOT NULL constraint failed: master_schedule.start_time"
**Решение:**
```bash
python3 migration_fix_master_schedule_nullable.py
```

### Таблицы отсутствуют
**Решение:**
```bash
python3 run_all_migrations.py
```

---

## 📝 Логи миграций

Все миграции выводят подробные логи:

```
🔧 Creating master schedule tables...
✅ master_schedule table created
✅ master_time_off table created
✅ available_slots table created
🎉 Master schedule migration completed successfully!
```

Если видите `❌` - значит есть ошибка.

---

## ✅ Чек-лист

После применения миграций проверьте:

- [ ] Запустил `run_all_migrations.py` или все миграции вручную
- [ ] Нет ошибок `❌` в выводе
- [ ] Запустил `python3 test_detailed.py`
- [ ] Все тесты прошли ✅
- [ ] В БД есть все нужные таблицы

---

_Последнее обновление: 16 ноября 2025, 07:00_
