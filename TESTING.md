# 🧪 Руководство по тестированию Beauty CRM

Комплексная система тестирования для всех компонентов системы.

## 📋 Оглавление

- [Быстрый старт](#быстрый-старт)
- [Backend тесты](#backend-тесты)
- [Frontend тесты](#frontend-тесты)
- [Структура тестов](#структура-тестов)
- [Интерпретация результатов](#интерпретация-результатов)

---

## 🚀 Быстрый старт

### Запуск всех тестов

**Linux/Mac:**
```bash
chmod +x run_all_tests.sh
./run_all_tests.sh
```

**Windows:**
```cmd
run_all_tests.bat
```

Скрипт автоматически:
- ✅ Установит все необходимые зависимости
- ✅ Запустит backend тесты
- ✅ Запустит frontend тесты
- ✅ Проверит сборку
- ✅ Создаст подробный отчет

---

## 📦 Backend тесты

### Установка зависимостей

```bash
cd backend
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements-test.txt
```

### Запуск тестов

**Все тесты:**
```bash
python run_tests.py
```

**Только база данных:**
```bash
pytest -m database -v
```

**Только сотрудники:**
```bash
pytest -m employees -v
```

**Только должности:**
```bash
pytest -m positions -v
```

**Интеграционные тесты:**
```bash
pytest -m integration -v
```

### Категории тестов

#### 🗄️ Database Tests (`test_database.py`)
- Проверка структуры таблиц
- Проверка целостности данных
- Валидация ограничений (UNIQUE, NOT NULL и т.д.)

#### 👥 Employees & Positions Tests (`test_employees_positions.py`)
- ✅ **Критический тест**: Все сотрудники имеют должности
- ✅ **Критический тест**: Имена сотрудников в правильном формате (не ALL CAPS)
- ✅ **Критический тест**: Должности в правильном формате
- Проверка связи сотрудников и должностей
- Тест миграции seed_employees

---

## 🎨 Frontend тесты

### Установка зависимостей

```bash
cd frontend
npm install
npm install --save-dev vitest @vitest/ui @testing-library/react @testing-library/jest-dom
```

### Запуск тестов

**Все тесты:**
```bash
npm run test
```

**С UI:**
```bash
npm run test:ui
```

**Один раз (CI):**
```bash
npm run test:run
```

**С coverage:**
```bash
npm run test:coverage
```

### Категории тестов

#### 🔌 API Client Tests (`api.test.ts`)
- Аутентификация (login, logout)
- Получение информации о салоне
- CRUD операции с сотрудниками
- ✅ **Критический тест**: Проверка формата должностей сотрудников
- Работа с должностями
- Работа с услугами
- Создание бронирований
- Обработка ошибок (404, 500, network errors)

---

## 📁 Структура тестов

```
beauty-crm/
├── backend/
│   ├── tests/
│   │   ├── conftest.py                 # Фикстуры и конфигурация
│   │   ├── test_database.py            # Тесты базы данных
│   │   └── test_employees_positions.py # Тесты сотрудников/должностей
│   ├── run_tests.py                    # Запуск backend тестов
│   ├── pytest.ini                      # Конфигурация pytest
│   └── requirements-test.txt           # Зависимости для тестов
│
├── frontend/
│   ├── src/tests/
│   │   ├── setup.ts                    # Настройка тестов
│   │   └── api.test.ts                 # Тесты API клиента
│   ├── vitest.config.ts                # Конфигурация Vitest
│   └── package.test.json               # Тестовые зависимости
│
├── run_all_tests.sh                    # Запуск всех тестов (Linux/Mac)
├── run_all_tests.bat                   # Запуск всех тестов (Windows)
└── TESTING.md                          # Это руководство
```

---

## 📊 Интерпретация результатов

### ✅ Успешный тест

```
test_employee_has_position PASSED
✅ Сотрудник Maria Smith имеет должность: Hair Stylist
```

### ❌ Проваленный тест

```
test_employee_has_position FAILED
❌ У сотрудника нет должности

AssertionError: ❌ У сотрудника нет должности
```

### Отчет тестов

После запуска создается файл `backend/test_report_YYYYMMDD_HHMMSS.txt` с полным отчетом.

---

## 🎯 Критические тесты

Эти тесты **ОБЯЗАТЕЛЬНО** должны проходить:

### Backend
1. ✅ `test_user_creation` - Пользователи создаются с должностями
2. ✅ `test_employee_has_position` - У всех сотрудников есть должности
3. ✅ `test_employee_position_proper_case` - Должности в правильном формате
4. ✅ `test_seed_employees_creates_with_positions` - Миграция работает правильно

### Frontend
1. ✅ `should verify all employees have positions` - API возвращает сотрудников с должностями
2. ✅ API тесты для должностей

---

## 🔧 Частые проблемы и решения

### Проблема: Тесты падают с "no such table"

**Решение:**
Тесты используют отдельную тестовую базу данных. Убедитесь, что файл `test_salon.db` не заблокирован.

```bash
rm backend/test_salon.db
python backend/run_tests.py
```

### Проблема: "ModuleNotFoundError: No module named 'pytest'"

**Решение:**
```bash
cd backend
pip install -r requirements-test.txt
```

### Проблема: Frontend тесты не находят модули

**Решение:**
```bash
cd frontend
npm install
npm install --save-dev vitest @testing-library/react @testing-library/jest-dom
```

---

## 📝 Добавление новых тестов

### Backend

Создайте новый файл в `backend/tests/`:

```python
import pytest

@pytest.mark.your_category
class TestYourFeature:
    def test_something(self, db_connection):
        """Описание теста"""
        # Ваш тест
        assert True, "❌ Описание ошибки"
```

### Frontend

Создайте новый файл в `frontend/src/tests/`:

```typescript
import { describe, it, expect } from 'vitest';

describe('YourFeature', () => {
  it('should do something', () => {
    expect(true).toBe(true);
  });
});
```

---

## 🐛 Дебаггинг тестов

### Backend

Запуск с подробным выводом:
```bash
pytest -vv --tb=long
```

Запуск конкретного теста:
```bash
pytest backend/tests/test_employees_positions.py::TestEmployeesBasic::test_employee_has_position -v
```

### Frontend

Запуск в режиме watch:
```bash
npm run test
```

Дебаг в браузере:
```bash
npm run test:ui
```

---

## 📈 Метрики тестирования

После запуска тестов вы увидите:

```
📈 Статистика:
   ├─ Категорий тестов: 4
   ├─ Пройдено категорий: 4/4
   ├─ Время выполнения: 3.45 секунд
   └─ Статус: ✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ
```

---

## 💡 Советы

1. **Запускайте тесты перед коммитом** - убедитесь что ничего не сломалось
2. **Проверяйте отчеты** - даже если тесты прошли, проверьте предупреждения
3. **Пишите тесты для багов** - нашли баг? Напишите тест который его воспроизводит
4. **Используйте маркеры** - запускайте только нужные категории тестов

---

## 📞 Поддержка

Если тесты показывают ошибки:
1. Проверьте отчет в `backend/test_report_*.txt`
2. Запустите конкретный проваленный тест с `-vv`
3. Проверьте логи приложения

---

## ✨ Что тестируется

- ✅ Структура базы данных
- ✅ Создание и обновление данных
- ✅ Связи между таблицами (Foreign Keys)
- ✅ Уникальность полей
- ✅ **Форматирование имен и должностей**
- ✅ API эндпоинты
- ✅ Обработка ошибок
- ✅ Сборка проекта

---

**Удачного тестирования! 🚀**
