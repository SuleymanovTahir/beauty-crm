# Применение миграций базы данных

## Миграции для новых функций (1-4)

Для применения всех новых таблиц базы данных выполните следующие команды:

### Из корневой директории проекта:

```bash
# Перейти в директорию backend
cd /home/user/beauty-crm/backend

# 1. Миграция: Расписание мастеров (3 новые таблицы)
python3 run_migration_master_schedule.py

# 2. Миграция: Программа лояльности (3 новые таблицы)
python3 run_migration_loyalty_program.py
```

### Что будет создано:

#### Миграция 1: Расписание мастеров
- ✅ `master_schedule` - рабочие часы мастеров по дням недели
- ✅ `master_time_off` - отпуска, больничные, выходные
- ✅ `available_slots` - кэш доступных слотов

#### Миграция 2: Программа лояльности
- ✅ `client_loyalty_points` - баланс баллов клиентов
- ✅ `loyalty_transactions` - история начисления/списания
- ✅ `loyalty_levels` - уровни лояльности (Bronze, Silver, Gold, Platinum)

### Проверка успешности:

После выполнения вы увидите:
```
🎉 Master schedule migration completed successfully!
🎉 Loyalty program migration completed successfully!
```

### Проверка таблиц в БД:

```bash
# Открыть БД для проверки
sqlite3 /home/user/beauty-crm/backend/salon_bot.db

# Просмотреть все таблицы
.tables

# Выйти
.quit
```

### Если миграции уже были выполнены:

Миграции используют `CREATE TABLE IF NOT EXISTS`, поэтому их можно запускать повторно - они не удалят существующие данные.

## Дополнительные функции (не требуют миграций)

- Dashboard с KPI - использует существующие таблицы
- Автозаполнение окон - использует существующие таблицы + новые из миграции расписания
