# Исправления от 2025-11-14

## Что было исправлено

### 1. Переводы (Translations) ✅

#### Проблема:
- Файлы переводов для английского и арабского языков были неполными или дублировали русский текст
- В админ-панели отображались ключи переменных вместо переведенного текста (manage_crm_parameters, general, notifications и т.д.)
- Боковая панель была на русском, но остальное - на переменных

#### Решение:
1. Создан скрипт автоматической синхронизации переводов: `sync_translations.py`
2. Обновлены ключевые файлы:
   - `/frontend/src/locales/en/admin/settings.json` - полностью заполнен (128 ключей)
   - `/frontend/src/locales/ar/admin/Settings.json` - полностью заполнен (128 ключей)
   - `/frontend/src/locales/en/layouts/AdminLayout.json` - переведен на английский
   - `/frontend/src/locales/ar/layouts/AdminLayout.json` - переведен на арабский
3. Синхронизированы все остальные файлы переводов (42 файла)

#### Запуск:
```bash
python3 sync_translations.py
```

### 2. Скрипты для проверки данных ✅

Созданы скрипты для проверки и исправления данных в БД:
- `fix_data.py` - проверка настроек бота, пользователей и исправление пустых полей

#### Основные функции:
- `check_bot_settings()` - проверяет пустые поля в bot_settings
- `check_users()` - проверяет должности сотрудников
- `check_salon_settings()` - проверяет настройки салона (в т.ч. weekdays_hours)
- `fix_manager_consultation_prompt()` - исправляет пустое поле manager_consultation_prompt
- `fix_booking_data_collection()` - исправляет пустое поле booking_data_collection

#### Запуск:
```bash
# Запустите backend сервер сначала
cd backend
python3 -m uvicorn main:app --reload

# В другом терминале
python3 fix_data.py
```

## Оставшиеся задачи (TO DO)

### 1. Кнопка смены валюты ⏳
**Локация**: `/admin/settings`
- Найти где должна быть кнопка смены валюты
- Добавить/показать если скрыта

### 2. Проблема с weekdays_hours ⏳
**Проблема**: Показывает "1" вместо нормальных данных
- Проверить миграции для правильного формата данных
- Исправить отображение в UI

### 3. Список системного языка ⏳
- В /admin/settings и /admin/bot-settings есть дублирующиеся селекторы языка
- Возможно стоит оставить только один или уточнить их назначение

### 4. Настройки уведомлений ⏳
- Найти страницу настроек уведомлений (mentions о записи и т.д.)
- Была прозрачная кнопка - проверить где она

### 5. Расширенные настройки уведомлений 🆕
**Требование**: Возможность выбирать за сколько дней/часов отправлять уведомление
- За 1 день, 2 дня
- Несколько значений одновременно
- С указанием времени (например "за 0 дней и 3 часа")

### 6. Должности сотрудников и пароли ⏳
**Проблема**: В `/admin/users` поле должности пустое
- Проверить миграции создания сотрудников
- Добавить должности
- Сгенерировать/сбросить пароли
- Сохранить учетные данные в файл

### 7. Тестирование уведомлений 📧
- Создать тестовые записи на ii3391609@gmail.com
- Проверить отправку email с:
  - Временем
  - Датой
  - Локацией
  - Контактами

### 8. Запуск миграций автоматически ⏳
**Проблема**: Миграции не запускаются при старте main.py
- Интегрировать `run_all_migrations()` в процесс инициализации
- Или добавить в документацию обязательный шаг запуска миграций

## Как запустить проект

### Backend
```bash
cd backend
export PYTHONPATH=/home/user/beauty-crm/backend:$PYTHONPATH
python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

### Запуск миграций вручную
```bash
cd backend
export PYTHONPATH=/home/user/beauty-crm/backend:$PYTHONPATH
python3 db/migrations/run_all_migrations.py
```

## Полезные ссылки

- Frontend: http://localhost:5173
- Admin Settings: http://localhost:5173/admin/settings
- Bot Settings: http://localhost:5173/admin/bot-settings
- Users: http://localhost:5173/admin/users
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

## Следующие шаги

1. **Запустите backend и frontend**
2. **Проверьте переводы**: Переключите язык в админ-панели (английский/арабский) и убедитесь что все отображается правильно
3. **Проверьте настройки**: Откройте /admin/settings и убедитесь что все поля заполнены
4. **Проверьте должности**: Откройте /admin/users и проверьте что у сотрудников есть должности
5. **Настройте уведомления**: Найдите и протестируйте систему уведомлений

## Дополнительная информация

- Все переводы хранятся в `/frontend/src/locales/{lang}/`
- Миграции в `/backend/db/migrations/`
- Конфигурация бота в базе данных (таблица `bot_settings`)
- Конфигурация салона в базе данных (таблица `salon_settings`)
