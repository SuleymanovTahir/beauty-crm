# 🚀 Roadmap: Превращение в Супер-CRM для Beauty-салонов

## 🎯 Философия: От CRM к AI-помощнику салона

Текущая CRM - это хорошая база. Но супер-CRM должна **ПРЕДВИДЕТЬ, АВТОМАТИЗИРОВАТЬ и ОПТИМИЗИРОВАТЬ** работу салона.

---

## 💎 Tier 1: Must-Have (Критически важно)

### 1. **Умный AI-ассистент с памятью о клиенте** 🤖

**Проблема:** Бот сейчас общается с клиентом, но не помнит детали предыдущих разговоров.

**Решение:**
- **Память контекста:** Сохранение предпочтений клиента (любимый мастер, любимая услуга, аллергии)
- **Персонализированные предложения:** "Привет, Анна! Прошло 3 недели с последнего маникюра. Записать к Дженнифер как обычно?"
- **Умные вопросы:** Если клиент записывается впервые - задать вопросы о предпочтениях

**Техническая реализация:**
```python
# Таблица client_preferences
CREATE TABLE client_preferences (
    id INTEGER PRIMARY KEY,
    client_id TEXT,
    preferred_master TEXT,
    preferred_service TEXT,
    preferred_time TEXT,  -- "weekday_evening", "weekend_morning"
    allergies TEXT,
    notes TEXT,
    auto_book INTEGER DEFAULT 0,  -- Автоматическая запись каждый месяц
    FOREIGN KEY (client_id) REFERENCES clients(instagram_id)
)

# Таблица conversation_context
CREATE TABLE conversation_context (
    id INTEGER PRIMARY KEY,
    client_id TEXT,
    context_type TEXT,  -- 'booking_in_progress', 'asked_about_service', etc.
    context_data TEXT,  -- JSON с данными
    created_at TEXT,
    expires_at TEXT
)
```

**Фичи:**
- ✅ "Запомнить меня" - клиент один раз указывает предпочтения
- ✅ Автоматическое предложение записи каждые N недель
- ✅ "Ты обычно приходишь по субботам в 14:00, это время подходит?"

---

### 2. **Dashboard с реальной аналитикой** 📊

**Проблема:** Нет визуализации ключевых метрик.

**Решение - Главный Dashboard:**

**KPI виджеты:**
```
┌──────────────┬──────────────┬──────────────┬──────────────┐
│ Выручка      │ Новые        │ Загруженность│ No-Show Rate │
│ сегодня      │ клиенты      │ мастеров     │              │
│ ₽45,000 ↑15% │ 12 чел       │ 85%          │ 5% ↓2%       │
└──────────────┴──────────────┴──────────────┴──────────────┘

График выручки по дням (последние 30 дней)
График популярных услуг
Топ-5 мастеров по выручке
Предстоящие записи (временная шкала)
```

**Технически:**
```python
# API endpoint
GET /api/dashboard/kpi
{
  "today_revenue": 45000,
  "today_revenue_change": 15,  # %
  "new_clients_today": 12,
  "master_utilization": 85,  # %
  "no_show_rate": 5,
  "upcoming_bookings": [...],
  "popular_services": [...],
  "top_masters": [...]
}
```

---

### 3. **Умное расписание мастеров** 📅

**Проблема:** Нет управления рабочими часами и выходными мастеров.

**Решение:**
```sql
-- Таблица расписания
CREATE TABLE master_schedules (
    id INTEGER PRIMARY KEY,
    master_id INTEGER,
    day_of_week INTEGER,  -- 0=Пн, 6=Вс
    start_time TEXT,
    end_time TEXT,
    is_working INTEGER DEFAULT 1,
    FOREIGN KEY (master_id) REFERENCES users(id)
)

-- Таблица выходных/отпусков
CREATE TABLE master_time_off (
    id INTEGER PRIMARY KEY,
    master_id INTEGER,
    date_from TEXT,
    date_to TEXT,
    reason TEXT,
    FOREIGN KEY (master_id) REFERENCES users(id)
)
```

**Фичи:**
- 📅 Мастер устанавливает своё расписание
- 🏖️ Отметка выходных и отпусков
- ⚡ Автоматический расчет доступных слотов
- 🔄 Перенос записей при отмене рабочего дня

---

### 4. **Система лояльности и бонусов** 💰

**Проблема:** Нет мотивации для постоянных клиентов возвращаться.

**Решение:**
```sql
CREATE TABLE loyalty_program (
    id INTEGER PRIMARY KEY,
    client_id TEXT,
    points INTEGER DEFAULT 0,
    tier TEXT DEFAULT 'bronze',  -- bronze, silver, gold, platinum
    total_spent REAL DEFAULT 0,
    visits_count INTEGER DEFAULT 0,
    FOREIGN KEY (client_id) REFERENCES clients(instagram_id)
)

CREATE TABLE loyalty_transactions (
    id INTEGER PRIMARY KEY,
    client_id TEXT,
    booking_id INTEGER,
    points_earned INTEGER,
    points_spent INTEGER,
    description TEXT,
    created_at TEXT
)
```

**Логика:**
- 💎 1 рубль = 1 бонус
- 🎁 Каждые 1000 бонусов = 100 рублей скидки
- 🏆 Уровни: Bronze (0+), Silver (10,000+), Gold (50,000+), Platinum (100,000+)
- 🎂 Бонусы в день рождения
- 👥 Реферальная программа: "Приведи друга - получи 500 бонусов"

---

### 5. **Автоматическое заполнение окон** ⚡

**Проблема:** Когда клиент отменяет запись, окно пустует.

**Решение:**
```python
# Когда запись отменяется:
1. Найти клиентов из waitlist для этой услуги
2. Найти клиентов, которые обычно записываются на это время
3. Отправить push: "Освободилось время 18.11 в 15:00! Успей записаться!"

# Таблица waitlist
CREATE TABLE booking_waitlist (
    id INTEGER PRIMARY KEY,
    client_id TEXT,
    service_name TEXT,
    preferred_date TEXT,  -- NULL если любая дата
    preferred_time TEXT,  -- "morning", "afternoon", "evening"
    created_at TEXT,
    notified_at TEXT,
    FOREIGN KEY (client_id) REFERENCES clients(instagram_id)
)
```

**Фичи:**
- 📋 Клиент может добавить себя в waitlist
- ⚡ Автоматическое уведомление при освобождении слота
- 🎯 Приоритет по уровню лояльности
- ⏱️ Окно бронируется на 15 минут для клиента

---

### 6. **Прогнозирование No-Show с ML** 🔮

**Проблема:** Клиенты не приходят, мастер простаивает.

**Решение:**
```python
# Факторы риска:
- Новый клиент (нет истории) = +30% риска
- Последний no-show < 3 месяцев назад = +50%
- Не подтвердил запись = +40%
- Записался > 1 недели заранее = +20%
- Плохая погода в день записи = +10%
- Пятница вечер = -10% (обычно приходят)

# Действия:
if risk > 70%:
    - Требовать предоплату 30%
    - Напомнить за 24ч, 12ч, 2ч
    - Добавить в waitlist кого-то еще
```

**Таблица:**
```sql
CREATE TABLE no_show_predictions (
    id INTEGER PRIMARY KEY,
    booking_id INTEGER,
    risk_score REAL,  -- 0.0 to 1.0
    factors TEXT,  -- JSON с факторами
    requires_deposit INTEGER,
    predicted_at TEXT,
    actual_outcome TEXT  -- 'showed', 'no_show', 'cancelled'
)
```

---

## 🌟 Tier 2: Game-Changers (Мега-фичи)

### 7. **Онлайн-оплата и финансы** 💳

**Интеграции:**
- Stripe / Yookassa для приема платежей
- Предоплата при записи (для no-show клиентов)
- Автоматические счета
- Чаевые мастеру через QR-код

**Финансовый учет:**
```sql
CREATE TABLE transactions (
    id INTEGER PRIMARY KEY,
    booking_id INTEGER,
    amount REAL,
    type TEXT,  -- 'payment', 'refund', 'tip', 'deposit'
    method TEXT,  -- 'cash', 'card', 'online', 'points'
    status TEXT,  -- 'pending', 'completed', 'failed'
    created_at TEXT
)

CREATE TABLE master_commissions (
    id INTEGER PRIMARY KEY,
    master_id INTEGER,
    booking_id INTEGER,
    service_revenue REAL,
    commission_percent REAL,
    commission_amount REAL,
    paid INTEGER DEFAULT 0,
    paid_at TEXT
)
```

---

### 8. **Управление складом (расходники)** 📦

**Проблема:** Закончился лак/крем во время процедуры.

```sql
CREATE TABLE inventory (
    id INTEGER PRIMARY KEY,
    name TEXT,
    category TEXT,  -- 'polish', 'cream', 'tool'
    quantity INTEGER,
    unit TEXT,  -- 'ml', 'pcs', 'gr'
    min_quantity INTEGER,  -- Порог для уведомления
    supplier TEXT,
    cost_per_unit REAL,
    last_restocked TEXT
)

CREATE TABLE inventory_usage (
    id INTEGER PRIMARY KEY,
    inventory_id INTEGER,
    booking_id INTEGER,
    quantity_used INTEGER,
    used_at TEXT
)
```

**Фичи:**
- ⚠️ Автоматические уведомления при низком остатке
- 📊 Расход материалов по услугам
- 💰 Расчет себестоимости услуги
- 📋 История закупок

---

### 9. **Мобильное приложение для мастеров** 📱

**Функции:**
- 📅 Просмотр своего расписания
- ✅ Отметка выполненных услуг
- 💰 Просмотр своей выручки и комиссий
- 📞 Быстрый доступ к клиентам
- 📸 Портфолио работ (фото до/после)
- ⏰ Push-уведомления о новых записях

**Техническое решение:**
- React Native приложение
- Авторизация через API
- WebSocket для real-time уведомлений

---

### 10. **Умные рекомендации услуг** 🎯

**AI предлагает услуги на основе:**
```python
def recommend_services(client_id):
    # История клиента
    past_services = get_client_history(client_id)

    # Популярные комбинации
    # "90% клиентов после маникюра заказывают педикюр в течение недели"
    combinations = {
        'manicure': ['pedicure', 'hand_massage'],
        'haircut': ['coloring', 'styling'],
        'massage': ['facial', 'aromatherapy']
    }

    # Сезонные предложения
    # Зима: парафинотерапия, увлажняющие процедуры
    # Лето: педикюр, уход за ногами

    # Предложения друзей
    # "Ваша подруга Анна недавно сделала ламинирование ресниц и в восторге!"

    return recommended_services
```

---

### 11. **Интеграция с соц. сетями** 📸

**Автопостинг:**
- ✅ Автоматическая публикация "до/после" в Instagram (с согласия клиента)
- ✅ Stories с акциями
- ✅ Автоответы в комментариях
- ✅ Парсинг упоминаний бренда

**Таблица:**
```sql
CREATE TABLE social_media_posts (
    id INTEGER PRIMARY KEY,
    booking_id INTEGER,
    platform TEXT,  -- 'instagram', 'vk', 'facebook'
    post_type TEXT,  -- 'before_after', 'promotion', 'review'
    image_url TEXT,
    caption TEXT,
    posted_at TEXT,
    likes INTEGER,
    comments INTEGER,
    client_consent INTEGER
)
```

---

### 12. **Система отзывов и рейтингов** ⭐

```sql
CREATE TABLE reviews (
    id INTEGER PRIMARY KEY,
    booking_id INTEGER,
    client_id TEXT,
    master_id INTEGER,
    rating INTEGER,  -- 1-5
    comment TEXT,
    photos TEXT,  -- JSON массив URL
    created_at TEXT,
    is_published INTEGER,
    response TEXT,  -- Ответ салона
    responded_at TEXT
)
```

**Автоматизация:**
- 📧 Автоматический запрос отзыва через 24ч после визита
- 🎁 Бонусы за отзыв (100 баллов)
- ⚠️ Уведомление менеджера при плохом отзыве (<3 звезд)
- 📊 Средний рейтинг мастеров на дашборде

---

### 13. **A/B тестирование маркетинга** 🧪

**Тестируем:**
- Время отправки напоминаний (утро vs вечер)
- Текст рассылок (формальный vs дружеский)
- Скидки (10% vs 500₽)
- Каналы коммуникации (Telegram vs Instagram)

```sql
CREATE TABLE ab_tests (
    id INTEGER PRIMARY KEY,
    name TEXT,
    variant_a TEXT,
    variant_b TEXT,
    start_date TEXT,
    end_date TEXT,
    metric TEXT,  -- 'conversion_rate', 'booking_rate', 'response_rate'
    status TEXT
)

CREATE TABLE ab_test_participants (
    id INTEGER PRIMARY KEY,
    test_id INTEGER,
    client_id TEXT,
    variant TEXT,  -- 'A' or 'B'
    converted INTEGER,
    FOREIGN KEY (test_id) REFERENCES ab_tests(id)
)
```

---

## 🔥 Tier 3: Innovative (Инновационные фичи)

### 14. **Голосовой ассистент для записи** 🎤

```python
# Интеграция с WhatsApp Voice Messages
client: "Запиши меня на маникюр к Дженнифер"
bot: "На какое время?"
client: "Послезавтра в два часа дня"
bot: "Записала! 18 ноября в 14:00 к мастеру Дженнифер на маникюр"
```

---

### 15. **Динамическое ценообразование** 💸

**Идея:** Цены меняются в зависимости от спроса (как в Uber).

```python
# Низкий спрос (8-11 утра в будни): -15%
# Высокий спрос (18-20 вечера в пятницу): +20%
# Новый клиент: -10% на первое посещение
# VIP клиент: -5% постоянно

def calculate_price(service, datetime, client):
    base_price = service.price

    # Множитель времени
    hour = datetime.hour
    day = datetime.weekday()

    if hour in [8,9,10,11] and day in [0,1,2,3]:
        multiplier = 0.85  # Скидка в непопулярное время
    elif hour in [18,19,20] and day in [4,5]:
        multiplier = 1.20  # Наценка в популярное время
    else:
        multiplier = 1.0

    # Скидки клиента
    if client.is_new:
        multiplier *= 0.90
    if client.tier == 'platinum':
        multiplier *= 0.95

    return base_price * multiplier
```

---

### 16. **Предиктивная аналитика** 🔮

**AI предсказывает:**
- 📈 Выручку на следующий месяц
- 📅 Лучшее время для акций
- 👥 Риск оттока клиента ("Анна не записывалась 2 месяца, риск потери 85%")
- 💰 Оптимальные цены на услуги
- 📊 Необходимое количество мастеров на смену

```python
# Модель churn prediction
CREATE TABLE churn_predictions (
    id INTEGER PRIMARY KEY,
    client_id TEXT,
    risk_score REAL,  -- 0.0 to 1.0
    last_visit_days_ago INTEGER,
    predicted_at TEXT,
    action_taken TEXT  -- 'sent_promo', 'called', None
)

# Если риск > 0.7:
send_personalized_offer(client_id, "Скучаем по вам! Специальная скидка 20%")
```

---

### 17. **Telegram Mini App для клиентов** 📱

**Встроенное приложение в Telegram:**
- 📅 Просмотр своих записей
- ⏰ Запись онлайн через календарь
- 💰 Баланс бонусов
- 📸 Галерея работ мастеров
- 💬 Чат с менеджером
- 🎁 Персональные акции

---

### 18. **Геймификация для клиентов** 🎮

```sql
CREATE TABLE achievements (
    id INTEGER PRIMARY KEY,
    code TEXT UNIQUE,
    name TEXT,
    description TEXT,
    icon TEXT,
    bonus_points INTEGER
)

CREATE TABLE client_achievements (
    id INTEGER PRIMARY KEY,
    client_id TEXT,
    achievement_id INTEGER,
    unlocked_at TEXT
)
```

**Достижения:**
- 🏆 "Первый раз" - первое посещение (+100 баллов)
- 💎 "Постоянный гость" - 10 посещений (+500 баллов)
- 👑 "VIP" - потрачено 100,000₽ (+2000 баллов)
- 🎂 "С днем рождения!" - визит в день рождения (+200 баллов)
- 👥 "Приводи друга" - привел 3 друзей (+1000 баллов)
- ⏰ "Рассвет" - 5 записей до 10 утра (+300 баллов)

---

## 🛠️ Технические улучшения

### 19. **Архитектурные улучшения**

**Текущая проблема:** Монолитная архитектура, все в одной БД.

**Решение:**
```
beauty-crm/
├── backend/
│   ├── services/
│   │   ├── booking_service.py      # Бизнес-логика записей
│   │   ├── notification_service.py # Уведомления
│   │   ├── analytics_service.py    # Аналитика
│   │   ├── payment_service.py      # Платежи
│   │   └── ai_service.py           # AI рекомендации
│   ├── models/                     # ORM модели (SQLAlchemy)
│   ├── api/                        # API endpoints
│   ├── workers/                    # Фоновые задачи (Celery)
│   └── cache/                      # Redis кэш
```

**Использовать:**
- ✅ **SQLAlchemy ORM** вместо сырого SQL
- ✅ **Redis** для кэширования
- ✅ **Celery** для фоновых задач (отправка уведомлений, генерация отчетов)
- ✅ **WebSocket** для real-time обновлений
- ✅ **PostgreSQL** вместо SQLite (для продакшена)

---

### 20. **Мониторинг и логирование**

```python
# Sentry для отслеживания ошибок
# Grafana для метрик
# ELK Stack для логов

# Важные метрики:
- Response time API endpoints
- Количество активных пользователей
- Процент успешных записей
- Процент доставленных уведомлений
- Загрузка системы
```

---

## 📊 Приоритизация фич

### Критичность HIGH (Делать первым):
1. ✅ **Умный AI-ассистент с памятью** - драматически улучшит UX
2. ✅ **Dashboard с аналитикой** - владелец увидит бизнес
3. ✅ **Система лояльности** - удержание клиентов
4. ✅ **Расписание мастеров** - базовая функциональность
5. ✅ **No-Show prediction** - экономит деньги

### Критичность MEDIUM:
6. Онлайн-оплата
7. Автозаполнение окон
8. Складской учет
9. Система отзывов
10. Умные рекомендации

### Критичность LOW (Nice-to-have):
11. Мобильное приложение для мастеров
12. A/B тестирование
13. Голосовой ассистент
14. Динамическое ценообразование
15. Telegram Mini App

---

## 🎯 План реализации (Sprint Planning)

### Sprint 1 (2 недели): Умный бот + Аналитика
- [ ] Таблица client_preferences
- [ ] API для сохранения предпочтений
- [ ] Персонализированные предложения бота
- [ ] Dashboard с KPI
- [ ] Графики выручки

### Sprint 2 (2 недели): Лояльность + Расписание
- [ ] Таблица loyalty_program
- [ ] Начисление бонусов
- [ ] Уровни клиентов (Bronze/Silver/Gold)
- [ ] Расписание мастеров
- [ ] Управление выходными

### Sprint 3 (2 недели): No-Show + Автозаполнение
- [ ] ML модель предсказания no-show
- [ ] Требование предоплаты для рисковых
- [ ] Waitlist система
- [ ] Автоматические уведомления о свободных слотах

### Sprint 4 (2 недели): Финансы
- [ ] Онлайн-оплата
- [ ] Учет комиссий мастеров
- [ ] Финансовые отчеты
- [ ] Интеграция с банком

---

## 💡 Дополнительные идеи

### Интеграции:
- 🗓️ Google Calendar sync (мастера видят записи в своем календаре)
- 📧 SendGrid / Mailgun для email рассылок
- 💬 Intercom / Tawk.to для чата с клиентами
- 📊 Google Analytics для веб-версии
- 🔔 Firebase Cloud Messaging для push-уведомлений

### Безопасность:
- 🔐 2FA для админов
- 🔒 Шифрование персональных данных
- 📝 Логирование всех действий
- 👁️ RBAC (role-based access control)
- 🛡️ Rate limiting для API

### Оптимизация:
- ⚡ Кэширование популярных запросов
- 📦 Сжатие изображений
- 🚀 CDN для статики
- 🔄 Lazy loading в UI
- 📈 Database indexing

---

## 🎁 Killer Features (Что выделит нас среди конкурентов)

1. **"Умная запись"** - бот САМ предлагает лучшее время на основе истории
2. **"Автопилот"** - клиент ставит "записывать каждый месяц" и больше не думает
3. **"Семейный аккаунт"** - один аккаунт на всю семью с разными профилями
4. **"Подарочные сертификаты"** - продажа и активация онлайн
5. **"Виртуальная примерка"** - AR для выбора цвета волос/маникюра
6. **"Видео-консультация"** - мастер может проконсультировать до визита
7. **"Умный помощник мастера"** - подсказывает историю клиента, аллергии, предпочтения

---

## 📚 Ресурсы для изучения

**Вдохновение (похожие CRM):**
- Fresha (ведущая beauty CRM в мире)
- Yclients (российская)
- Square Appointments
- Acuity Scheduling

**Технологии:**
- FastAPI + SQLAlchemy + Alembic
- React + TanStack Query + Zustand
- Redis + Celery
- Sentry + DataDog
- Stripe / Yookassa

---

## 🚀 Что реализуем ПРЯМО СЕЙЧАС?

Начнем с самого impactful - **Умный AI-ассистент с памятью о клиенте**.

Это даст:
- ✅ WOW-эффект для клиентов
- ✅ Снижение нагрузки на администратора
- ✅ Повышение конверсии в запись
- ✅ Персонализацию на уровне лучших мировых сервисов
