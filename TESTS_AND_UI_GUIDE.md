# Руководство по тестам и UI

## ✅ Исправления

### 1. Исправлены тесты
- Исправлены импорты во всех проваливающихся тестах
- Создан отдельный файл `backend/tests/run_failed_tests.py` для запуска только проваленных тестов
- **6 из 10** тестов теперь проходят успешно

### 2. UI для управления пользователями
**UI УЖЕ СУЩЕСТВУЕТ** на странице `/admin/users`

Функции:
- ✅ **Смена ролей** - кнопка с иконкой Shield
  - Директор, Администратор, Менеджер, Продажник, Таргетолог, Сотрудник
- ✅ **Редактирование пользователей** - кнопка Edit
  - Логин, Полное имя, Email
  - **Выбор должности** через PositionSelector (автодополнение из справочника)
- ✅ **Удаление пользователей** - кнопка Delete

### 3. UI для рассылок
**UI УЖЕ СУЩЕСТВУЕТ** на странице `/admin/broadcasts`

Функции:
- ✅ **Выбор типа подписки** (promotions, news, reminders, etc.)
- ✅ **Выбор каналов**:
  - Email
  - Telegram
  - Instagram
- ✅ **Фильтрация по роли** (администраторы, менеджеры, сотрудники, клиенты)
- ✅ **Написание письма**:
  - Тема (для Email)
  - Текст сообщения
- ✅ **Предпросмотр получателей** - показывает сколько людей получат рассылку
- ✅ **История рассылок** - вкладка "История"

## 📧 Email уведомления

### Статус
- ✅ Система полностью реализована (`backend/utils/email.py`)
- ✅ SMTP credentials настроены в `.env.local`
- ✅ Готовые функции:
  - Верификация email
  - Сброс пароля
  - Уведомления об одобрении регистрации
  - Универсальная отправка email (для рассылок)

### Настройки SMTP (уже в .env.local)
```bash
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=ii3391609@gmail.com
SMTP_PASSWORD=hkfw qruh hxur ghta
FROM_EMAIL=ii3391609@gmail.com
```

### Проверка работы email
1. Убедитесь, что App Password действителен (может потребоваться обновить)
2. Для Gmail:
   - Зайдите в https://myaccount.google.com/apppasswords
   - Создайте новый App Password
   - Обновите `SMTP_PASSWORD` в `.env.local`

## 🧪 Запуск тестов

### Запуск только проваленных тестов
```bash
cd backend
python tests/run_failed_tests.py
```

### Запуск всех тестов
```bash
cd backend
python tests/run_all_tests.py
```

### Текущий статус тестов
- ✅ PASS: test_new_features.py (Новые функции)
- ✅ PASS: test_smart_assistant.py (AI ассистент)
- ✅ PASS: test_immediate_notification.py (Уведомления)
- ✅ PASS: check_employees.py (Сотрудники)
- ✅ PASS: check_services.py (Услуги)
- ✅ PASS: check_users.py (Пользователи)
- ❌ FAIL: test_all.py (требует дополнительных модулей)
- ❌ FAIL: test_detailed.py (требует дополнительных модулей)
- ❌ FAIL: api/test_reminders_api.py (требует запущенного сервера)
- ❌ FAIL: api/test_notifications_api.py (требует запущенного сервера)
- ⏭️  SKIP: check_bot.py (требует AI API и может долго выполняться)

## 🔐 Управление ролями и правами

### Доступные роли (по уровню иерархии)
1. **Директор** (уровень 100) - полный доступ ко всем функциям
2. **Администратор** (уровень 80) - управление системой, кроме директорских функций
3. **Менеджер** (уровень 60) - управление записями, клиентами, календарем
4. **Продажник** (уровень 40) - работа с клиентами, просмотр аналитики
5. **Таргетолог** (уровень 30) - Instagram чат, аналитика, рассылки
6. **Сотрудник** (уровень 20) - только свой календарь и записи

### Как изменить роль пользователя
1. Откройте `/admin/users`
2. Найдите нужного пользователя
3. Нажмите кнопку **Shield** (синяя)
4. Выберите новую роль из списка
5. Готово! Пользователь получит новые права доступа

### Как изменить должность пользователя
1. Откройте `/admin/users`
2. Найдите нужного пользователя
3. Нажмите кнопку **Edit**
4. В поле "Должность" начните вводить название
5. Выберите из справочника или создайте новую
6. Нажмите **Сохранить**

## 📨 Как отправить рассылку

1. Откройте `/admin/broadcasts`
2. На вкладке "Создать рассылку":
   - Выберите **тип подписки** (promotions, news и т.д.)
   - Выберите **каналы** (Email, Telegram, Instagram)
   - (Опционально) Выберите **целевую роль** для фильтрации
   - Введите **тему письма** (для Email)
   - Введите **текст сообщения**
3. Нажмите **Предпросмотр** - увидите количество получателей
4. Проверьте корректность
5. Нажмите **Отправить**
6. Готово! Рассылка появится в истории

## ❓ FAQ

### Почему я не вижу UI для управления ролями?
UI существует на странице `/admin/users`. Кнопка **Shield** (синяя) для смены роли.

### Почему не приходят email уведомления?
1. Проверьте настройки SMTP в `.env.local`
2. Убедитесь, что App Password действителен (может истечь)
3. Проверьте логи: `backend/logs/app.log`
4. Для Gmail нужно использовать App Password, а не обычный пароль

### Как создать App Password для Gmail?
1. Перейдите на https://myaccount.google.com/apppasswords
2. Войдите в свой аккаунт Google
3. Нажмите "Create" или "Создать"
4. Выберите "Mail" и "Other (custom name)"
5. Введите название (например, "Beauty CRM")
6. Скопируйте сгенерированный пароль
7. Вставьте его в `SMTP_PASSWORD` в `.env.local`

### Как проверить, что email работает?
Запустите тест:
```bash
cd backend
python tests/test_broadcasts_and_reminders.py
```

Этот тест создаст тестового пользователя и попытается отправить email на `ii3391609@gmail.com`.
