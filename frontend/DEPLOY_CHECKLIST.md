# Чеклист для деплоя оптимизаций производительности

## Перед деплоем

### 1. Локальная проверка
```bash
cd frontend
npm run build
npm run preview
```

**Откройте в браузере:**
- Landing page: http://localhost:4173/public_landing.html
- Admin panel: http://localhost:4173/

**Chrome DevTools проверка:**
1. Откройте DevTools (F12)
2. Перейдите в Lighthouse
3. Запустите audit для Performance
4. Проверьте метрики:
   - ✅ LCP < 2.5s (зеленый)
   - ✅ FCP < 1.8s (зеленый)
   - ✅ CLS < 0.1 (зеленый)

### 2. Проверка Network
**Откройте DevTools → Network:**
1. Перезагрузите landing page
2. Проверьте, что загружаются:
   - ✅ `logo-*.webp` с `Priority: High`
   - ✅ Только `index-*.css` (НЕ admin CSS)
   - ✅ Google Fonts загружаются асинхронно

3. Убедитесь, что НЕ загружаются:
   - ❌ `admin-analytics-*.css`
   - ❌ `admin-bookings-*.js`
   - ❌ Любые другие admin-* файлы

---

## Деплой на production

### 1. Push изменений
```bash
git status  # Проверить изменения
git log -1  # Проверить последний коммит
git push origin main
```

### 2. Backend деплой (если нужен)
```bash
cd backend
# Следуйте инструкциям в деплой файле
```

### 3. Frontend деплой
```bash
cd frontend
npm run build

# Копируем dist на сервер (зависит от вашего процесса)
# Например через rsync:
# rsync -avz dist/ user@server:/var/www/beauty-crm/
```

### 4. Проверка на сервере
```bash
# Проверить, что файлы скопированы
ssh user@server "ls -lh /var/www/beauty-crm/assets/*.css"

# Проверить gzip сжатие включено
curl -I -H "Accept-Encoding: gzip" https://mlediamant.com/assets/index-*.css
# Должно быть: Content-Encoding: gzip
```

---

## После деплоя

### 1. PageSpeed Insights проверка
**Откройте:** https://pagespeed.web.dev/

**Проверьте URL:** `https://mlediamant.com/`

**Целевые метрики:**
- ✅ LCP < 2.5s (хорошо) или < 4.0s (требует улучшений)
- ✅ FCP < 1.8s (хорошо) или < 3.0s (требует улучшений)
- ✅ CLS < 0.1 (хорошо) или < 0.25 (требует улучшений)
- ✅ INP < 200ms (хорошо) или < 500ms (требует улучшений)

**Проверьте разделы:**
- ✅ "Избегайте огромных сетевых нагрузок" - должно быть зеленым
- ✅ "Устраните ресурсы, блокирующие отрисовку" - должно быть улучшено
- ✅ "Правильно настройте размер изображений" - logo должен быть WebP

### 2. Real User Monitoring (опционально)
Если у вас настроен Google Analytics 4:

**Откройте:** Google Analytics → Reports → Engagement → Pages and screens

**Проверьте метрики:**
- Page Load Time
- First Contentful Paint
- Largest Contentful Paint

### 3. Проверка на разных устройствах
**Desktop:**
- Chrome
- Firefox
- Safari

**Mobile:**
- Android Chrome
- iOS Safari

---

## Откат изменений (если что-то пошло не так)

### Быстрый откат
```bash
# Откатить последний коммит (но сохранить изменения)
git reset --soft HEAD~1

# ИЛИ откатить полностью (удалить изменения)
git reset --hard HEAD~1

# Пересобрать старую версию
cd frontend
npm run build
```

### Откат на production
```bash
# Откатить на предыдущий коммит
git revert HEAD
git push origin main

# Пересобрать и передеплоить
cd frontend
npm run build
# Деплой на сервер
```

---

## Мониторинг после деплоя

### День 1-3: Активный мониторинг
- Проверяйте PageSpeed Insights ежедневно
- Следите за Google Analytics метриками
- Читайте отзывы пользователей о скорости

### Неделя 1-2: Периодический мониторинг
- Проверяйте PageSpeed Insights 2-3 раза в неделю
- Анализируйте Core Web Vitals в Google Search Console

### Постоянный мониторинг
- Настройте автоматические проверки Lighthouse CI
- Настройте alerts в Google Analytics для ухудшения метрик

---

## Дополнительные проверки

### Проверка Vite плагина
```bash
# Убедитесь, что плагин работает при сборке
cd frontend
npm run build 2>&1 | grep "Removed admin preloads"
# Должно вывести: ✅ Removed admin preloads from landing page
```

### Проверка размеров бандлов
```bash
cd frontend
npm run build 2>&1 | grep -E "(index-|main-|admin-).*css"

# Ожидаемые размеры:
# index-*.css      ~213 KB │ gzip: ~30 KB  (landing)
# main-*.css       ~246 KB │ gzip: ~34 KB  (admin)
# admin-*-*.css    <15 KB каждый (отдельные модули)
```

### Проверка HTML вывода
```bash
# Проверить, что admin CSS удалены из landing
cat frontend/dist/public_landing.html | grep -E "admin-"
# Должно быть пусто (ничего не найдено)

# Проверить, что preload логотипа есть
cat frontend/dist/public_landing.html | grep "preload.*logo"
# Должно показать строку с preload
```

---

## Контакты для вопросов

Если что-то пошло не так:
1. Проверьте логи сборки: `npm run build`
2. Проверьте console errors в браузере (F12 → Console)
3. Откатите изменения и свяжитесь с разработчиком

**Документация:**
- `PERFORMANCE_OPTIMIZATIONS.md` - подробное описание всех оптимизаций
- `vite-plugin-filter-preloads.ts` - код плагина с комментариями

---

## Успешный деплой выглядит так:

**Console вывод при сборке:**
```
✅ Removed admin preloads from landing page
✓ built in 4.78s
```

**PageSpeed Insights:**
- 🟢 Performance: 80-90+
- 🟢 LCP: < 2.5s
- 🟢 FCP: < 1.8s
- 🟢 CLS: < 0.1

**Network tab:**
- `logo-*.webp` - Priority: High ✅
- `index-*.css` - загружается ✅
- `admin-*.css` - НЕ загружается ✅

---

**Дата создания:** 21 января 2026
**Версия:** 1.0
**Статус:** ✅ Готов к использованию
